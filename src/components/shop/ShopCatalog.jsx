// Plan 05-04 Task 1 placeholder — full implementation lands in Task 2.
// Exists so the `Shop.jsx` lazy import resolves at build time (`pnpm build`
// statically analyzes dynamic-import targets, even though the runtime branch
// only fires when products.length > 0).
const ShopCatalog = () => null;

export default ShopCatalog;
