---
phase: 01-foundation-refactor-env-pinning
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - .nvmrc
  - netlify.toml
  - pnpm-lock.yaml
  - yarn.lock
autonomous: true
requirements: [FOUND-05]
must_haves:
  truths:
    - "A clean checkout of master succeeds at `pnpm install && pnpm build` against Node 20"
    - "Netlify reads NODE_VERSION from netlify.toml at build time, not from a dashboard setting"
    - "The repository advertises its Node + pnpm contract so contributors can reproduce locally"
    - "`packageManager` is pinned to the literal floor `pnpm@9.0.0` (not the locally-installed pnpm version) so corepack-driven environments converge on the same minor that produced the existing `lockfileVersion: '9.0'` lockfile"
    - "The committed `pnpm-lock.yaml` is byte-identical to the lockfile present in the working tree before `pnpm install` runs — Task 2 must NOT regenerate it"
  artifacts:
    - path: ".nvmrc"
      provides: "Node version pin (literal `20`)"
      contains: "20"
    - path: "netlify.toml"
      provides: "Netlify build config (pnpm command, publish dir, NODE_VERSION pin)"
      contains: "NODE_VERSION"
    - path: "package.json"
      provides: "engines + packageManager fields"
      contains: "\"engines\""
    - path: "pnpm-lock.yaml"
      provides: "Committed lockfile (was untracked) — committed verbatim, never regenerated"
      contains: "lockfileVersion: '9.0'"
  key_links:
    - from: "netlify.toml"
      to: "package.json scripts"
      via: "command = \"pnpm build\" → react-scripts build --openssl-legacy-provider"
      pattern: "command\\s*=\\s*\"pnpm build\""
    - from: ".nvmrc"
      to: "netlify.toml [build.environment]"
      via: "Both declare Node 20 — local nvm and Netlify CI agree"
      pattern: "NODE_VERSION\\s*=\\s*\"20\""
---

<objective>
Pin the build environment so `pnpm install && pnpm build` is reproducible on a clean checkout against Node 20 and so Netlify reads its Node + build contract from the repo (not from a dashboard setting). Commits the previously untracked `pnpm-lock.yaml` and finalizes the deletion of `yarn.lock` already in the working tree.

Purpose: Phase 1's first wave. Every later plan in this phase (and every later phase) assumes Node 20 + pnpm 9 + a committed lockfile. Without this, "works on my machine" drift is unbounded and Netlify can silently break on a Node major bump.

Output: `.nvmrc`, `netlify.toml`, updated `package.json`, committed `pnpm-lock.yaml`, deleted `yarn.lock`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-refactor-env-pinning/01-CONTEXT.md
@.planning/phases/01-foundation-refactor-env-pinning/01-PATTERNS.md
@./CLAUDE.md
@package.json

<interfaces>
<!-- Decisions D-19, D-20, D-21, D-22 from CONTEXT.md are locked — implement exactly. -->

D-19: `.nvmrc` contains literally `20` (major-only). `package.json` adds `"engines": { "node": ">=20" }` and `"packageManager": "pnpm@9.0.0"`.
D-20: `netlify.toml` declares `[build] command = "pnpm build"`, `publish = "build"`, and `[build.environment] NODE_VERSION = "20"`. Keep `--openssl-legacy-provider` in `package.json` scripts (Phase 4 owns its removal — VITE migration).
D-21: Commit `pnpm-lock.yaml` (currently untracked per `git status`); stage the already-deleted `yarn.lock`.
D-22: README is NOT rewritten in this phase. SEO-04 (Phase 2) owns it.

Current `package.json` head (verbatim, lines 1-5):
```json
{
  "name": "shapesmith-studio-web",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
```

Current `pnpm-lock.yaml` is at the repo root with `lockfileVersion: '9.0'` (pnpm v9 lockfile format). Per D-19, `packageManager` is pinned to the LITERAL floor `pnpm@9.0.0` (the minimum 9.x release that produced this lockfile format). Corepack will install exactly `9.0.0` from this pin, ensuring all contributors and CI converge on the same pnpm minor. DO NOT substitute the locally-installed pnpm version (e.g. `9.12.0`) — the literal floor is the contract.

Current `yarn.lock` is already deleted in the working tree (`git status` shows ` D yarn.lock`).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add `.nvmrc`, write `netlify.toml`, update `package.json` engines + packageManager</name>
  <files>.nvmrc, netlify.toml, package.json</files>
  <read_first>
    - package.json (current — confirms 2-space indent, sibling-of-`"private"` insertion point)
    - .planning/phases/01-foundation-refactor-env-pinning/01-CONTEXT.md (D-19, D-20)
    - .planning/phases/01-foundation-refactor-env-pinning/01-PATTERNS.md §"NEW: `.nvmrc`", §"NEW: `netlify.toml`", §"`package.json` (modified by FOUND-05)"
  </read_first>
  <action>
    Per D-19 and D-20:

    1. Create `.nvmrc` at repo root with the exact contents `20\n` (single line, literal `20`, LF terminator). No comments, no `v` prefix.

    2. Create `netlify.toml` at repo root with the exact contents (2-space indent inside tables, TOML convention):

       ```toml
       [build]
         command = "pnpm build"
         publish = "build"

       [build.environment]
         NODE_VERSION = "20"
       ```

       Do NOT add `--openssl-legacy-provider` here — `pnpm build` already invokes the existing `package.json` script `react-scripts build --openssl-legacy-provider`. Per D-20 the flag stays in `package.json` until Phase 4 (Vite migration) owns its removal.

    3. Edit `package.json` (preserving 2-space indent, blank-line-free, no trailing comma — match existing format). Insert two top-level fields immediately after `"private": true,` and before `"dependencies": {`:

       ```json
         "engines": {
           "node": ">=20"
         },
         "packageManager": "pnpm@9.0.0",
       ```

       Per D-19 use the LITERAL string `pnpm@9.0.0` — this is a floor pin (the minimum 9.x release that produced the existing `lockfileVersion: '9.0'` lockfile). DO NOT substitute the locally-installed pnpm version (e.g. `9.12.0`, `10.x`, etc.). The literal `9.0.0` is the contract: corepack reads this field and installs exactly `9.0.0`, so every contributor and CI runner converges on the same pnpm minor regardless of what they had installed locally.

    DO NOT touch the `scripts.start` or `scripts.build` lines — `--openssl-legacy-provider` stays per D-20. DO NOT update README — that is SEO-04 / Phase 2 per D-22. DO NOT environment-ize Sanity credentials — out of scope for Phase 1 (per CONTEXT.md §"code_context").
  </action>
  <verify>
    <automated>cat .nvmrc | grep -qx "20" && grep -q 'NODE_VERSION = "20"' netlify.toml && grep -q 'command = "pnpm build"' netlify.toml && grep -q 'publish = "build"' netlify.toml && grep -q '"node": ">=20"' package.json && grep -q '"packageManager": "pnpm@9.0.0"' package.json && grep -q -- '--openssl-legacy-provider' package.json && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `.nvmrc` exists and `cat .nvmrc` outputs exactly `20` (one line, no `v`).
    - `grep -c '^\[build\]' netlify.toml` returns `1`.
    - `grep -c '^\[build.environment\]' netlify.toml` returns `1`.
    - `grep -q 'NODE_VERSION = "20"' netlify.toml` succeeds.
    - `grep -q 'command = "pnpm build"' netlify.toml` succeeds.
    - `grep -q 'publish = "build"' netlify.toml` succeeds.
    - `grep -q '"engines"' package.json` succeeds AND `grep -q '"node": ">=20"' package.json` succeeds.
    - `grep -q '"packageManager": "pnpm@9.0.0"' package.json` succeeds — the LITERAL floor pin (not a substituted local version).
    - `grep -q -- '--openssl-legacy-provider' package.json` STILL succeeds (flag was NOT removed).
    - `node -e "JSON.parse(require('fs').readFileSync('package.json','utf8'))"` exits 0 (package.json remains valid JSON).
    - README.md `git diff` shows zero changes (D-22 honored).
  </acceptance_criteria>
  <done>`.nvmrc`, `netlify.toml`, and updated `package.json` exist with the exact contents specified; `packageManager` is the literal `pnpm@9.0.0` floor pin; `--openssl-legacy-provider` flag still present in scripts; README untouched.</done>
</task>

<task type="auto">
  <name>Task 2: Commit `pnpm-lock.yaml`, finalize `yarn.lock` deletion, verify clean install + build</name>
  <files>pnpm-lock.yaml, yarn.lock</files>
  <read_first>
    - .planning/phases/01-foundation-refactor-env-pinning/01-CONTEXT.md (D-21)
    - The pnpm-lock.yaml first 3 lines (confirm `lockfileVersion: '9.0'` is intact)
  </read_first>
  <action>
    Per D-21:

    1. Capture a hash of the existing `pnpm-lock.yaml` BEFORE any install runs, so a regression can be detected:

       ```bash
       LOCKHASH_BEFORE=$(shasum -a 256 pnpm-lock.yaml | awk '{print $1}')
       echo "$LOCKHASH_BEFORE" > /tmp/pnpm-lock-hash-before.txt
       ```

    2. Stage the pnpm-lock.yaml (currently untracked per `git status`): `git add pnpm-lock.yaml`. Do NOT regenerate it; the lockfile that already exists is the one to commit.

    3. Stage the already-deleted yarn.lock so the deletion lands in the same change: `git add -u yarn.lock` (or `git rm yarn.lock` if `git add -u` does not pick it up).

    4. Verify the lockfile is internally consistent against `package.json` (which now has new `engines` + `packageManager` fields) by running `pnpm install --frozen-lockfile` from the repo root. Expected outcome: succeeds with no lockfile modification.

       **On failure:** STOP. Do NOT mutate `pnpm-lock.yaml` automatically. Per Blocker #2 fix, the auto-refresh fallback is removed — with `packageManager: "pnpm@9.0.0"` literally pinned (Task 1) and `lockfileVersion: '9.0'` already present, the only realistic failure modes are corepack handshake errors (e.g., corepack not enabled on the runner) or genuine lockfile/package.json drift introduced by Task 1's edits. Both deserve human review, not silent mutation. Capture the full failure output, surface it in the SUMMARY under a "BLOCKED — needs human attention" section, and halt the task. The lockfile must remain byte-identical to its pre-install state.

    5. Confirm the lockfile was NOT regenerated:

       ```bash
       LOCKHASH_AFTER=$(shasum -a 256 pnpm-lock.yaml | awk '{print $1}')
       LOCKHASH_BEFORE=$(cat /tmp/pnpm-lock-hash-before.txt)
       test "$LOCKHASH_BEFORE" = "$LOCKHASH_AFTER" || { echo "FAIL: pnpm-lock.yaml was mutated by install (before=$LOCKHASH_BEFORE after=$LOCKHASH_AFTER) — invariant T-01-01 violated"; exit 1; }
       ```

    6. Run `pnpm build` to confirm the production build still succeeds end-to-end on Node 20 with the existing `--openssl-legacy-provider` flag. The build output goes to `build/` which is git-ignored.

    DO NOT bump any dependency versions; this task only commits files. DO NOT touch `node_modules`. DO NOT run `pnpm audit fix --force` — RESEARCH.md flagged this as a critical pitfall. DO NOT run `pnpm install` without `--frozen-lockfile` — that would regenerate `pnpm-lock.yaml` and defeat invariant T-01-01.
  </action>
  <verify>
    <automated>LOCKHASH_BEFORE=$(shasum -a 256 pnpm-lock.yaml | awk '{print $1}') && git ls-files pnpm-lock.yaml | grep -q pnpm-lock.yaml && ! git ls-files yarn.lock | grep -q yarn.lock && pnpm install --frozen-lockfile && LOCKHASH_AFTER=$(shasum -a 256 pnpm-lock.yaml | awk '{print $1}') && test "$LOCKHASH_BEFORE" = "$LOCKHASH_AFTER" && pnpm build && test -f build/index.html && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `git ls-files pnpm-lock.yaml` outputs `pnpm-lock.yaml` (lockfile is now tracked).
    - `git ls-files yarn.lock` outputs nothing (yarn.lock is no longer tracked).
    - `head -1 pnpm-lock.yaml` shows `lockfileVersion: '9.0'` (lockfile format preserved).
    - `pnpm install --frozen-lockfile` exits 0 (lockfile and package.json are consistent).
    - `shasum -a 256 pnpm-lock.yaml` returns the SAME hash before and after `pnpm install --frozen-lockfile` runs (lockfile was NOT regenerated mid-task — invariant T-01-01).
    - `pnpm build` exits 0 and `build/index.html` exists.
    - No `dangerously` / `--force` flags were used during install. No `pnpm install` (without `--frozen-lockfile`) was run.
  </acceptance_criteria>
  <done>`pnpm-lock.yaml` is tracked AND byte-identical to its pre-install state, `yarn.lock` deletion is staged, `pnpm install --frozen-lockfile` succeeds, `pnpm build` produces `build/index.html`. If `pnpm install --frozen-lockfile` fails, the task halts with a "BLOCKED" SUMMARY note rather than mutating the lockfile.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none introduced) | This plan only edits build-tooling config and the lockfile. No new external inputs, no auth, no DB, no new runtime dependencies. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Tampering | `pnpm-lock.yaml` integrity | mitigate | Lockfile is committed exactly as produced by pnpm with `lockfileVersion: '9.0'`; no manual edits. Task 2 captures a SHA-256 hash before `pnpm install --frozen-lockfile` and asserts it is unchanged after — any silent regeneration fails the task. CI integrity is enforced by `pnpm install --frozen-lockfile` continuing to succeed on every run. |
| T-01-02 | Tampering | `netlify.toml` build command | accept | `netlify.toml` is committed plaintext config; same trust boundary as `package.json`. Anyone with repo write can change either. |

No new external inputs, no auth, no DB, no new runtime dependencies; threat surface unchanged from baseline. ASVS L1 not triggered (no user-facing change).
</threat_model>

<verification>
- `.nvmrc` exists with literal `20`.
- `netlify.toml` exists with `[build]`, `[build.environment]`, `command = "pnpm build"`, `publish = "build"`, `NODE_VERSION = "20"`.
- `package.json` has `"engines": { "node": ">=20" }` AND the LITERAL `"packageManager": "pnpm@9.0.0"` AND still has `--openssl-legacy-provider` in `scripts.start` / `scripts.build`.
- `pnpm-lock.yaml` is tracked AND byte-identical (SHA-256) to its pre-install state.
- `yarn.lock` is no longer tracked.
- `pnpm install --frozen-lockfile && pnpm build` succeeds end-to-end.
</verification>

<success_criteria>
ROADMAP success criterion #4 ("`pnpm install && pnpm build` succeeds on a clean checkout against the pinned Node version") is owned by this plan. Verified by the `pnpm install --frozen-lockfile && pnpm build` automated check in Task 2.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-refactor-env-pinning/01-01-SUMMARY.md` recording: files added/deleted, the SHA-256 hash of the committed `pnpm-lock.yaml` (proving it was not regenerated), and any lockfile install failure that halted the task (with the full pnpm error output).
</output>
