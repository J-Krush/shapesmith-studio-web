---
phase: 01-foundation-refactor-env-pinning
plan: 05
type: execute
wave: 4
depends_on: ["01-03"]
files_modified:
  - src/data/services.js
  - src/data/projects.js
  - src/App.js
  - src/components/shared/AppHeader.jsx
  - src/components/projects/ProjectsFilter.jsx
  - src/components/projects/ProjectsGrid.jsx
autonomous: true
requirements: [FOUND-02]
must_haves:
  truths:
    - "A single `SERVICES` constant in `src/data/services.js` is the source of truth for service `key`, `urlSegment`, `navLabel`, `sanityType`, and `contactSubject`"
    - "The legacy `capabilitiesTitle` export no longer exists in `src/data/projects.js` and no file imports it"
    - "The `/styles` route and the `/styles/:capability` route still resolve identically to before (URL stability — `urlSegment: 'styles'` matches old value)"
    - "`SERVICES` declares both `laser` and `print` entries even though Phase 1 only consumes `laser` — Phase 2 lights up `print` with no schema change"
  artifacts:
    - path: "src/data/services.js"
      provides: "SERVICES constant — array of {key, urlSegment, navLabel, sanityType, contactSubject}"
      contains: "export const SERVICES"
    - path: "src/data/projects.js"
      provides: "Legacy projects array (left intact for VIS-05) WITHOUT the `capabilitiesTitle` export"
  key_links:
    - from: "src/App.js"
      to: "SERVICES.find(s => s.key === 'laser').urlSegment"
      via: "Route path derivation"
      pattern: "SERVICES"
    - from: "src/components/shared/AppHeader.jsx"
      to: "SERVICES.find(s => s.key === 'laser')"
      via: "Nav `<Link>` to + label"
      pattern: "SERVICES"
---

<objective>
Introduce `src/data/services.js` exporting a `SERVICES` constant with two entries (`laser`, `print`), migrate the four `capabilitiesTitle` consumers (`App.js`, `AppHeader.jsx`, `ProjectsFilter.jsx`, `ProjectsGrid.jsx`) to read from `SERVICES`, then delete the `capabilitiesTitle` export from `src/data/projects.js`. Phase 1 only USES the `laser` entry; the `print` entry is declared so Phase 2 can light it up without schema changes.

Purpose: Removes the load-bearing single-symbol coupling (`capabilitiesTitle = 'styles'` is currently the source of truth for the URL segment, the nav label, AND the route param key). Establishes the data shape that Phase 2's ServicesContext-vs-parallel decision will operate on. URL stability is preserved (`urlSegment: 'styles'` matches the old value verbatim).

Output: new `src/data/services.js`, edited `src/data/projects.js`, edited `src/App.js`, `src/components/shared/AppHeader.jsx`, `src/components/projects/ProjectsFilter.jsx`, `src/components/projects/ProjectsGrid.jsx`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-foundation-refactor-env-pinning/01-CONTEXT.md
@.planning/phases/01-foundation-refactor-env-pinning/01-PATTERNS.md
@./CLAUDE.md
@src/App.js
@src/components/shared/AppHeader.jsx
@src/components/projects/ProjectsFilter.jsx
@src/components/projects/ProjectsGrid.jsx
@src/data/projects.js

<interfaces>
<!-- Decisions D-12, D-13, D-14, D-15 from CONTEXT.md are locked. -->

D-12: `src/data/services.js` exports `SERVICES = [{ key, urlSegment, navLabel, sanityType, contactSubject }]` with two entries (`laser`, `print`). Only `laser` is consumed in Phase 1; `print` is declared for Phase 2.
D-13: Migrate all four `capabilitiesTitle` consumers in this phase: `App.js`, `AppHeader.jsx`, `ProjectsFilter.jsx`, `ProjectsGrid.jsx`. Delete the `capabilitiesTitle` export from `src/data/projects.js`.
D-14: Do NOT generalize `ProjectsContext` → `ServicesContext` in Phase 1. ServicesContext-vs-parallel is deferred to Phase 2.
D-15: `src/data/projects.js` still contains the legacy `capabilitiesData` array. Leave that untouched — VIS-05 (Phase 2) owns dead-data purge. ONLY remove `export const capabilitiesTitle = 'styles';` (line 22).

**Wave dependency:** This plan is in Wave 4 because it touches `App.js` AND `AppHeader.jsx` — both of which Plan 03 (FOUND-03 dark theme) ALSO touched. By executing after Plan 03, this plan's edits land on the post-FOUND-03 versions of those files (no `documentElement` mutation in App.js, no commented theme-toggle in AppHeader.jsx). The `capabilitiesTitle` import line in App.js (currently line 9) is OUT of scope of Plan 03; this plan owns its replacement.

Verbatim file body to write for the new `src/data/services.js` (per PATTERNS.md §"NEW: `src/data/services.js`"):

```js
export const SERVICES = [
	{
		key: 'laser',
		urlSegment: 'styles',
		navLabel: 'styles',
		sanityType: 'laser-style',
		contactSubject: 'Laser cutting',
	},
	{
		key: 'print',
		urlSegment: '3d-printing',
		navLabel: '3D Printing',
		sanityType: 'print-style',
		contactSubject: '3D printing',
	},
];
```

Notes from PATTERNS.md:
- `urlSegment: 'styles'` for `laser` is **identical** to the current `capabilitiesTitle = 'styles'` value — preserves URL stability.
- `navLabel: 'styles'` (lowercase) matches current header rendering — `AppHeader.jsx` renders `{capabilitiesTitle}` directly inside a Tailwind `capitalize` class. Lowercase here, capitalized at render — matches existing behavior.
- Tab indentation per CONVENTIONS.md.

Per-file edit summaries:

`src/App.js` (current relevant lines):
- Line 9: `import { capabilitiesTitle } from '../src/data/projects';` (ALSO fixes the awkward `'../src/'` prefix flagged in PATTERNS.md — change to `'./data/services'`)
- Line 38: `<Route path={`/${capabilitiesTitle}`} element={<Projects />} />`
- Line 40: `path={`/${capabilitiesTitle}/:capability`}`

`src/components/shared/AppHeader.jsx` (post-FOUND-03 — re-read from disk):
- Line 7: `import { capabilitiesTitle } from '../../data/projects';`
- Lines 96-103 (mobile nav): uses `${capabilitiesTitle}` and `{capabilitiesTitle}`
- Lines 149-155 (large-screen nav): uses `${capabilitiesTitle}` and `{capabilitiesTitle}`

`src/components/projects/ProjectsFilter.jsx`:
- Line 1: `import { capabilitiesTitle } from "../../data/projects";`
- Line 33: `All {capabilitiesTitle}` (rendered text)

`src/components/projects/ProjectsGrid.jsx`:
- Line 5: `import { capabilitiesTitle } from '../../data/projects';`
- Line 14: `Laser Cutting {capabilitiesTitle}` (rendered text)

`src/data/projects.js`:
- Line 22: `export const capabilitiesTitle = 'styles';` — DELETE this single line. The `import { ... } from './images'` block (lines 1-20) and the `export const capabilitiesData = [...]` array (line 24+) STAY untouched (D-15 — VIS-05 owns dead-data purge).
- Note: `capabilitiesData` items reference `capabilitiesTitle` internally in their `slug` strings (e.g., `slug: \`/${capabilitiesTitle}/layered-wall-art\``). After deletion these become `ReferenceError` at module-eval time IF the array is ever imported. Per the audit step in Task 3, in this codebase the audit is expected to find no consumer of `capabilitiesData` (only the export line itself in `src/data/projects.js:24` matches `grep -rn "capabilitiesData" src/`), so the inlining of `'styles'` into `slug` literals is unconditional. If somehow a consumer is found, halt and inline first.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Create `src/data/services.js`</name>
  <files>src/data/services.js</files>
  <read_first>
    - src/data/projects.js (current — confirms `.js` extension + `export const NAME = ...` named-export convention)
    - .planning/phases/01-foundation-refactor-env-pinning/01-CONTEXT.md (D-12)
    - .planning/phases/01-foundation-refactor-env-pinning/01-PATTERNS.md §"NEW: `src/data/services.js`"
  </read_first>
  <action>
    Create `src/data/services.js` with the exact body shown in `<interfaces>` above. Use **tabs** for indentation (matches `src/data/projects.js`). Single named export `SERVICES`. Two entries: `laser` and `print`. No default export.
  </action>
  <verify>
    <automated>test -f src/data/services.js && grep -q "export const SERVICES" src/data/services.js && grep -q "key: 'laser'" src/data/services.js && grep -q "key: 'print'" src/data/services.js && grep -q "urlSegment: 'styles'" src/data/services.js && grep -q "urlSegment: '3d-printing'" src/data/services.js && grep -q "sanityType: 'laser-style'" src/data/services.js && grep -q "sanityType: 'print-style'" src/data/services.js && grep -q "navLabel: 'styles'" src/data/services.js && grep -q "navLabel: '3D Printing'" src/data/services.js && grep -q "contactSubject: 'Laser cutting'" src/data/services.js && grep -q "contactSubject: '3D printing'" src/data/services.js && pnpm build && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `src/data/services.js` exists.
    - `grep -q "export const SERVICES" src/data/services.js` succeeds (exactly one named export).
    - `grep -q "export default" src/data/services.js` returns FALSE.
    - All five expected keys present in the `laser` entry: `key`, `urlSegment: 'styles'`, `navLabel: 'styles'`, `sanityType: 'laser-style'`, `contactSubject: 'Laser cutting'`.
    - All five expected keys present in the `print` entry: `key`, `urlSegment: '3d-printing'`, `navLabel: '3D Printing'`, `sanityType: 'print-style'`, `contactSubject: '3D printing'`.
    - `pnpm build` exits 0.
  </acceptance_criteria>
  <done>`src/data/services.js` exists with SERVICES export containing both `laser` and `print` entries, build green.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Migrate the four `capabilitiesTitle` consumers to read from SERVICES</name>
  <files>src/App.js, src/components/shared/AppHeader.jsx, src/components/projects/ProjectsFilter.jsx, src/components/projects/ProjectsGrid.jsx</files>
  <read_first>
    - src/App.js (current — note: this file was modified by Plan 03; re-read from disk to get post-FOUND-03 state before editing)
    - src/components/shared/AppHeader.jsx (current — also modified by Plan 03; re-read from disk)
    - src/components/projects/ProjectsFilter.jsx (current — only lines 1, 33 are touched)
    - src/components/projects/ProjectsGrid.jsx (current — only lines 5, 14 are touched)
    - src/data/services.js (just created in Task 1)
    - .planning/phases/01-foundation-refactor-env-pinning/01-CONTEXT.md (D-13)
    - .planning/phases/01-foundation-refactor-env-pinning/01-PATTERNS.md §"`src/App.js`" + §"`src/components/shared/AppHeader.jsx`" excerpts
  </read_first>
  <action>
    Apply the four edits below. After each file edit, do NOT run the full build — Task 3 will sweep the build at the end.

    **`src/App.js`:**
    - Replace the import line `import { capabilitiesTitle } from '../src/data/projects';` with `import { SERVICES } from './data/services';` (note: also fixes the `'../src/'` typo per PATTERNS.md).
    - Inside the `function App() {` body, BEFORE the `return (`, add a local lookup: `const laser = SERVICES.find((s) => s.key === 'laser');`
    - Inside the `<Routes>` block, replace the two route paths:
      - `<Route path={\`/${capabilitiesTitle}\`} element={<Projects />} />` → `<Route path={\`/${laser.urlSegment}\`} element={<Projects />} />`
      - `path={\`/${capabilitiesTitle}/:capability\`}` → `path={\`/${laser.urlSegment}/:capability\`}`
    - DO NOT change the route param name `:capability` — `ProjectSingle.jsx` reads `useParams().capability` (verified by code search), and renaming would cascade. The URL segment is what matters for stability; the param name is internal.

    **`src/components/shared/AppHeader.jsx`:**
    - Replace the import line `import { capabilitiesTitle } from '../../data/projects';` with `import { SERVICES } from '../../data/services';`.
    - At the top of the `AppHeader` component body (immediately after `const [showMenu, setShowMenu] = useState(false);`), add: `const laser = SERVICES.find((s) => s.key === 'laser');`
    - In the mobile nav `<Link>` (currently around lines 96-103 — re-read post-Plan-03), replace:
      - `to={\`/${capabilitiesTitle}\`}` → `to={\`/${laser.urlSegment}\`}`
      - `aria-label={capabilitiesTitle}` → `aria-label={laser.navLabel}`
      - `{capabilitiesTitle}` (the rendered text inside the Link) → `{laser.navLabel}`
    - In the large-screen nav `<Link>` (around lines 149-155), apply the same three replacements.
    - Preserve the `capitalize` Tailwind class on both `<Link>` elements — that's what makes the lowercase `'styles'` render as `Styles` (per PATTERNS.md note).

    **`src/components/projects/ProjectsFilter.jsx`:**
    - Replace the import line `import { capabilitiesTitle } from "../../data/projects";` with `import { SERVICES } from "../../data/services";` (preserve the existing double-quote convention this specific file uses).
    - At the top of the `ProjectsFilter` component body, add: `const laser = SERVICES.find((s) => s.key === 'laser');`
    - In the JSX, replace `All {capabilitiesTitle}` with `All {laser.navLabel}`.

    **`src/components/projects/ProjectsGrid.jsx`:**
    - Replace the import line `import { capabilitiesTitle } from '../../data/projects';` with `import { SERVICES } from '../../data/services';`.
    - At the top of the `ProjectsGrid` component body (after `const { projects } = useContext(ProjectsContext);`), add: `const laser = SERVICES.find((s) => s.key === 'laser');`
    - In the JSX, replace `Laser Cutting {capabilitiesTitle}` with `Laser Cutting {laser.navLabel}`.

    DO NOT touch any other line in these four files. DO NOT generalize ProjectsContext (D-14). DO NOT delete `import { capabilitiesData }` if any file imports it (none do per PATTERNS.md, but verify with grep before claiming).
  </action>
  <verify>
    <automated>! grep -rln "capabilitiesTitle" src/App.js src/components/shared/AppHeader.jsx src/components/projects/ProjectsFilter.jsx src/components/projects/ProjectsGrid.jsx && grep -q "from './data/services'" src/App.js && grep -q "from '../../data/services'" src/components/shared/AppHeader.jsx && grep -q "SERVICES.find" src/App.js && grep -q "SERVICES.find" src/components/shared/AppHeader.jsx && grep -q "SERVICES.find" src/components/projects/ProjectsFilter.jsx && grep -q "SERVICES.find" src/components/projects/ProjectsGrid.jsx && grep -q "laser.urlSegment" src/App.js && grep -q "laser.navLabel" src/components/shared/AppHeader.jsx && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `grep -l "capabilitiesTitle" src/App.js src/components/shared/AppHeader.jsx src/components/projects/ProjectsFilter.jsx src/components/projects/ProjectsGrid.jsx` lists nothing (all four consumers no longer import or render `capabilitiesTitle`).
    - All four files have `import { SERVICES } from ...services` (with appropriate relative path).
    - All four files have a `SERVICES.find((s) => s.key === 'laser')` lookup.
    - `grep -q "laser.urlSegment" src/App.js` succeeds (route path uses urlSegment).
    - `grep -q "laser.navLabel" src/components/shared/AppHeader.jsx` succeeds.
    - `grep -q "laser.navLabel" src/components/projects/ProjectsFilter.jsx` succeeds.
    - `grep -q "laser.navLabel" src/components/projects/ProjectsGrid.jsx` succeeds.
    - `grep -q ":capability" src/App.js` STILL succeeds (route param name preserved).
    - `grep -q "from '../src/data/projects'" src/App.js` returns FALSE (typo path fixed).
  </acceptance_criteria>
  <done>All four consumers import from `services` and read via `SERVICES.find(... === 'laser')`; no `capabilitiesTitle` references remain in any consumer.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Delete `capabilitiesTitle` export from `src/data/projects.js` and verify build + slugs</name>
  <files>src/data/projects.js</files>
  <read_first>
    - src/data/projects.js (current — confirms line 22 is the `export const capabilitiesTitle = 'styles';` and line 24+ is the dead `capabilitiesData` array)
    - .planning/phases/01-foundation-refactor-env-pinning/01-CONTEXT.md (D-15)
  </read_first>
  <action>
    1. **Audit `capabilitiesData` consumers:** Run `grep -rn "capabilitiesData" src/` for confidence — if any consumer is found, halt and inline the `'styles'` literal into the `slug` strings inside `capabilitiesData` BEFORE deleting `capabilitiesTitle`. In this codebase the audit is expected to return only the export line itself in `src/data/projects.js:24`; the deletion is unconditional and proceeds straight to step 2.

    2. **Edit `src/data/projects.js`:**
       - DELETE line 22: `export const capabilitiesTitle = 'styles';`
       - LEAVE the `import { ... } from './images';` block at the top alone (D-15 — VIS-05 owns the legacy purge).
       - LEAVE the `export const capabilitiesData = [ ... ];` array alone EXCEPT for one concern: the array's items reference `capabilitiesTitle` inside their `slug` template literals (e.g., `slug: \`/${capabilitiesTitle}/layered-wall-art\``, lines 29, 103, 135, 181, 211, 247). With `capabilitiesTitle` deleted, the module would throw `ReferenceError` at evaluation time IF anything imports `capabilitiesData`.
       - To keep the module internally consistent regardless of import status, inline the `'styles'` string at every `slug` template literal:
         - `slug: \`/${capabilitiesTitle}/layered-wall-art\`` → `slug: \`/styles/layered-wall-art\``
         - Apply the same `${capabilitiesTitle}` → `styles` replacement in every `slug:` line in `capabilitiesData`.

    3. Run `pnpm build` from the repo root. Expected outcome: succeeds. The route path `/styles/:capability` (now derived from `SERVICES.find(...).urlSegment === 'styles'`) is identical to the previous value, so all routes resolve identically.

    4. Final URL-stability check (grep-based, deterministic — no Node ESM-import gymnastics): `grep -q "urlSegment: 'styles'" src/data/services.js` confirms the new constant matches the old `capabilitiesTitle` value verbatim.

    DO NOT delete the `capabilitiesData` array (D-15 — VIS-05 owns). DO NOT delete the `import { ... } from './images';` block.
  </action>
  <verify>
    <automated>! grep -q "export const capabilitiesTitle" src/data/projects.js && ! grep -rln "capabilitiesTitle" src/ && grep -q "export const capabilitiesData" src/data/projects.js && grep -q "urlSegment: 'styles'" src/data/services.js && pnpm build && test -f build/index.html && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "export const capabilitiesTitle" src/data/projects.js` returns FALSE.
    - `grep -rln "capabilitiesTitle" src/` returns nothing (codebase-wide no remaining references).
    - `grep -q "export const capabilitiesData" src/data/projects.js` STILL succeeds (D-15: array preserved for VIS-05).
    - `grep -q "from './images'" src/data/projects.js` STILL succeeds (legacy import preserved per D-15).
    - `grep -q "\${capabilitiesTitle}" src/data/projects.js` returns FALSE (slug references inlined to literal `styles`).
    - `grep -q "urlSegment: 'styles'" src/data/services.js` STILL succeeds (URL stability invariant — Phase 1's `/styles` matches the old value verbatim).
    - `pnpm build` exits 0 AND `build/index.html` is generated.
  </acceptance_criteria>
  <done>`capabilitiesTitle` export deleted, no consumer references remain, `capabilitiesData` legacy array preserved (with internal `${capabilitiesTitle}` references resolved to literal `styles`), `urlSegment: 'styles'` confirmed in services.js, build green.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Manually verify `/styles` route still resolves and nav link reads correctly</name>
  <what-built>The single source of truth for service metadata is now `src/data/services.js#SERVICES`. The `/styles` URL is derived from `SERVICES.find(s => s.key === 'laser').urlSegment`, which equals `'styles'` — same value as the old `capabilitiesTitle`. The header nav now reads its label from `laser.navLabel`. The `capabilitiesTitle` symbol is gone from the codebase.</what-built>
  <how-to-verify>
    1. Run `pnpm start`. Wait for `Local: http://localhost:3000`.
    2. Open `http://localhost:3000/`. Confirm the homepage renders and the header nav shows the same links as before — including the "Styles" link (rendered capitalized by Tailwind's `capitalize` class on the underlying lowercase `styles`).
    3. Click the "Styles" nav link. Browser URL should change to `http://localhost:3000/styles`. The `Projects` page should render the grid as before.
    4. Click into any project style. Browser URL should change to `http://localhost:3000/styles/<some-slug>`. The `ProjectSingle` page should render.
    5. Open dev tools console. Confirm no warnings about missing imports or `capabilitiesTitle` references.
    6. Stop the dev server.

    If `/styles` 404s or the nav link is broken or any console warning appears, report back with specifics. Otherwise type `approved`.
  </how-to-verify>
  <resume-signal>Type "approved" or describe any routing/nav regression.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none introduced) | Refactor of an internal constant. No new external inputs, no auth, no DB, no new dependencies. URL surface unchanged. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-07 | Tampering | Route path derivation | accept | URL is now derived from `SERVICES.find(...)` — if `SERVICES` is mutated at runtime by an attacker (which would require a compromised bundle), routing breaks. Same trust model as the previous `capabilitiesTitle` const; no regression. |

No new external inputs, no auth, no DB, no new dependencies; threat surface unchanged from baseline. ASVS L1 not triggered.
</threat_model>

<verification>
- `src/data/services.js` exists with both `laser` and `print` entries.
- `grep -rln "capabilitiesTitle" src/` returns nothing.
- All four consumer files import `SERVICES` from `data/services` and read via `SERVICES.find(s => s.key === 'laser')`.
- `src/data/projects.js` still exports `capabilitiesData` (D-15) but no `capabilitiesTitle`.
- `pnpm build` succeeds.
- Manual smoke (Task 4) confirms `/styles` and `/styles/:capability` still resolve.
</verification>

<success_criteria>
This plan owns FOUND-02 (REQUIREMENTS.md). Phase success criteria covered: this plan does NOT directly own a numbered ROADMAP success criterion (those are FOUND-01, FOUND-03, FOUND-04, FOUND-05, FOUND-06 surface; FOUND-02 is foundational machinery for Phase 2). Verified by grep + manual route smoke.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-refactor-env-pinning/01-05-SUMMARY.md` recording: the new SERVICES constant shape, the four migrated consumers, the slug-literal inlining inside `capabilitiesData`, and a Phase 2 hand-off note that `print` entry is declared and ready for `/3d-printing` to consume.
</output>
