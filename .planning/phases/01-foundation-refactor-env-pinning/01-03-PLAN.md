---
phase: 01-foundation-refactor-env-pinning
plan: 03
type: execute
wave: 3
depends_on: ["01-01"]
files_modified:
  - public/index.html
  - src/App.js
  - src/components/shared/AppHeader.jsx
  - src/components/shared/AppBanner.jsx
  - src/hooks/useThemeSwitcher.jsx
  - tailwind.config.js
autonomous: true
requirements: [FOUND-03]
must_haves:
  truths:
    - "Dark theme is applied with no JS side effect — `<html class=\"dark\">` is the source of truth"
    - "No file in `src/` references `useThemeSwitcher`, and the hook file no longer exists"
    - "The site renders identically (dark) before and after this plan — no visual regression"
    - "The `pnpm build` succeeds AFTER the four `-light` color tokens are removed from tailwind.config.js"
  artifacts:
    - path: "public/index.html"
      provides: "Hardcoded `class=\"dark\"` on `<html>` tag"
      contains: "<html lang=\"en\" class=\"dark\">"
    - path: "src/App.js"
      provides: "App root with no render-time DOM mutation and no theme localStorage write"
    - path: "tailwind.config.js"
      provides: "Color palette with `-light` tokens removed; `darkMode: 'class'` preserved"
    - path: "src/hooks/useThemeSwitcher.jsx"
      provides: "DELETED — file no longer exists; previously held the dead theme-switching hook"
    - path: "src/components/shared/AppHeader.jsx"
      provides: "Theme-toggle markup/comments removed (`useThemeSwitcher` import, `[activeTheme, setTheme]` call, FiMoon/FiSun JSX comment blocks); active nav otherwise unchanged"
    - path: "src/components/shared/AppBanner.jsx"
      provides: "Theme-toggle markup/comments removed (`useThemeSwitcher` call, `console.log('activeTheme...')`, commented `src={activeTheme === 'dark' ? ...}` ternaries); active brand and hero `<img>` tags otherwise unchanged"
  key_links:
    - from: "public/index.html"
      to: "tailwind.config.js darkMode: 'class'"
      via: "the `dark` class on <html> activates all `dark:` variants in components"
      pattern: "darkMode:\\s*'class'"
---

<objective>
Apply the dark theme without a render-time DOM mutation. Hardcode `class="dark"` on `<html>` in `public/index.html`, delete the imperative `root.classList.add('dark')` + `localStorage.setItem('theme','dark')` lines from `App.js`, delete the `useThemeSwitcher` hook entirely along with all commented theme-toggle markup in `AppHeader.jsx` and `AppBanner.jsx`, and remove the four unused `-light` color tokens from `tailwind.config.js`.

Purpose: The current `App.js` mutates `document.documentElement` during render — a textbook React anti-pattern that StrictMode flags. The toggle code is commented-out residue from a deleted feature. Phase 1 commits to dark-only (per ROADMAP); this is the cleanup.

Output: edited `public/index.html`, `src/App.js`, `src/components/shared/AppHeader.jsx`, `src/components/shared/AppBanner.jsx`, `tailwind.config.js`; deleted `src/hooks/useThemeSwitcher.jsx`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-foundation-refactor-env-pinning/01-CONTEXT.md
@.planning/phases/01-foundation-refactor-env-pinning/01-PATTERNS.md
@./CLAUDE.md
@public/index.html
@src/App.js
@src/components/shared/AppHeader.jsx
@src/components/shared/AppBanner.jsx
@src/hooks/useThemeSwitcher.jsx
@tailwind.config.js

<interfaces>
<!-- Decisions D-06, D-07, D-08 from CONTEXT.md are locked. -->

D-06: Hardcode `class="dark"` on `<html>` in `public/index.html`. Delete `root.classList.add('dark')` AND `localStorage.setItem('theme', 'dark')` from `src/App.js`.
D-07: Delete `src/hooks/useThemeSwitcher.jsx`. Delete commented theme-toggle markup in `AppHeader.jsx` (commented import line 4, commented hook call line 13, commented `<FiMoon>`/`<FiSun>` blocks lines 44-55 and 187-198) and `AppBanner.jsx` (lines 8, 10, 25-26, 66-69 — all commented theme references).
D-08: Phase 1 only removes the `-light` keys (`primary-light`, `secondary-light`, `ternary-light`, `secondary-section-light`) from `tailwind.config.js`. Keep `darkMode: 'class'`, `*-dark` keys, `accent`, `accent-highlight`, `*-section-dark`. DO NOT strip `dark:` Tailwind variants from component files (Phase 2 spruce sweep will revisit).

PATTERNS.md discrepancy #3: removing `-light` keys may break the JIT build. The sequencing in Task 3 below MUST run `pnpm build` after the deletion AND BEFORE committing. If the build fails, fall back per Task 3's error-recovery clause.

PATTERNS.md discrepancy #4: `App.js:31` has `bg-secondary-light` in its className. After token removal it becomes a no-op (Tailwind JIT silently drops). The class string will be left as-is in this plan; Phase 2's VIS-05 dead-code purge owns rewriting these classNames.

Verbatim line targets (read these from disk before editing — line numbers from PATTERNS.md may have shifted if any earlier plan touched these files):

`public/index.html` — change `<html lang="en">` (line 2) to `<html lang="en" class="dark">`.

`src/App.js` — current lines 25-27 to DELETE (whole block):
```jsx
	const root = window.document.documentElement;
	root.classList.add('dark');
	localStorage.setItem('theme', 'dark');
```

`src/components/shared/AppHeader.jsx` — DELETE these specific commented blocks:
- Line 4: `// import useThemeSwitcher from '../../hooks/useThemeSwitcher';`
- Line 13: `	// const [activeTheme, setTheme] = useThemeSwitcher();`
- Lines 36 (the `// src={activeTheme === 'dark' ? logoLight : logoDark}` comment inside the `<img>` for the logo)
- Lines 44-55: the `{/* Theme switcher small screen */}` block (entire JSX comment + commented `<div onClick={() => setTheme(activeTheme)}...>` block including `FiMoon`/`FiSun`)
- Lines 187-198: the `{/* Theme switcher large screen */}` block (same shape, second instance)

Also remove the now-unused `FiMoon` / `FiSun` from the `react-icons/fi` import on line 2 IF AND ONLY IF they are not referenced anywhere else in this file. Current line 2 is `import { FiMenu, FiX } from 'react-icons/fi';` — `FiMoon`/`FiSun` are only inside the commented blocks being deleted, so this import line does NOT need to change.

`src/components/shared/AppBanner.jsx` — DELETE these specific commented blocks:
- Line 8: `	// const [activeTheme] = useThemeSwitcher();`
- Line 10: `	// console.log('activeTheme: ', activeTheme);`
- Line 25: `								// src={activeTheme === 'dark' ? brandLight : brandDark}` (one-line inline comment inside the brand `<img>`)
- Lines 66-69: the multi-line commented `// src={activeTheme === 'dark' ? heroLight : heroDark}` inside the hero `<img>`

The active `<img src={brandMultiFont} />` and `<img src={heroLight} />` lines stay — only the dead `// src=...` ternaries get removed.

`src/hooks/useThemeSwitcher.jsx` — DELETE the entire file.

`tailwind.config.js` — DELETE these four lines (currently lines 32, 33, 34, 36):
```js
				'primary-light': '#F7F8FC',
				'secondary-light': '#FFFFFF',
				'ternary-light': '#f6f7f8',

				'secondary-section-light': '#d1d1d1ff',
```

Keep the blank line on what was line 35 between the deleted blocks (cosmetic — match existing format of one blank line between groups).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Hardcode `class="dark"` on `<html>` and delete the App.js side-effect</name>
  <files>public/index.html, src/App.js</files>
  <read_first>
    - public/index.html (current — confirms `<html lang="en">` is line 2)
    - src/App.js (current — confirms the three lines to delete are inside the `function App() {` block immediately before `return (`)
    - .planning/phases/01-foundation-refactor-env-pinning/01-CONTEXT.md (D-06)
  </read_first>
  <action>
    1. **`public/index.html`**: change `<html lang="en">` (line 2) to `<html lang="en" class="dark">`. Single character-set change. No other edits to this file (the Netlify hidden form on lines 34-45 is owned by CTC-02 in Phase 2 — do not touch).

    2. **`src/App.js`**: delete the three lines inside `function App() {` that read `documentElement` and call `localStorage`. After deletion the function body should jump straight from `function App() {` to the blank line before `return (`. Verify by reading the file post-edit:

       Before:
       ```jsx
       function App() {

       	const root = window.document.documentElement;
       	root.classList.add('dark');
       	localStorage.setItem('theme', 'dark');

       	return (
       ```

       After:
       ```jsx
       function App() {

       	return (
       ```

    DO NOT touch the JSX inside `App.js` — `bg-secondary-light` on line 31 stays (Phase 2 VIS-05 owns dead-class cleanup per PATTERNS.md discrepancy #4). DO NOT touch the `import { capabilitiesTitle }` line — that's owned by Plan 04 (FOUND-02). DO NOT add `<React.StrictMode>` wrappers anywhere (already in `src/index.js`).
  </action>
  <verify>
    <automated>grep -q '<html lang="en" class="dark">' public/index.html && ! grep -q "documentElement" src/App.js && ! grep -q "localStorage.setItem" src/App.js && ! grep -q "classList.add" src/App.js && pnpm build && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q '<html lang="en" class="dark">' public/index.html` succeeds.
    - `grep -c '<html' public/index.html` returns `1` (didn't accidentally duplicate the tag).
    - `grep -q "documentElement" src/App.js` returns FALSE.
    - `grep -q "localStorage" src/App.js` returns FALSE.
    - `grep -q "classList" src/App.js` returns FALSE.
    - `grep -q "function App()" src/App.js` succeeds (function declaration intact).
    - `grep -q "return (" src/App.js` succeeds (return preserved).
    - `pnpm build` exits 0.
  </acceptance_criteria>
  <done>`<html>` carries `class="dark"`, App.js has no DOM-mutation or localStorage side effect, build green.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Delete `useThemeSwitcher` hook and all commented theme-toggle markup in AppHeader and AppBanner</name>
  <files>src/hooks/useThemeSwitcher.jsx, src/components/shared/AppHeader.jsx, src/components/shared/AppBanner.jsx</files>
  <read_first>
    - src/hooks/useThemeSwitcher.jsx (current — full file, confirms it's only the hook to delete)
    - src/components/shared/AppHeader.jsx (current — confirms locations of the commented blocks listed in `<interfaces>`)
    - src/components/shared/AppBanner.jsx (current — confirms locations of the commented theme references)
    - .planning/phases/01-foundation-refactor-env-pinning/01-CONTEXT.md (D-07)
  </read_first>
  <action>
    1. **Delete `src/hooks/useThemeSwitcher.jsx`** entirely (`git rm` or filesystem delete + `git add`). The hook is never imported (verified by grep: only references are in commented-out lines in AppHeader/AppBanner which are also being deleted in this task, plus the file itself).

    2. **Edit `src/components/shared/AppHeader.jsx`** — delete EXACTLY these blocks (line numbers from the version on disk; re-read to confirm before editing):
       - Line 4 (single-line JS comment): `// import useThemeSwitcher from '../../hooks/useThemeSwitcher';`
       - Line 13 (single-line JS comment): `	// const [activeTheme, setTheme] = useThemeSwitcher();`
       - The `// src={activeTheme === 'dark' ? logoLight : logoDark}` line inside the brand `<img>` (line 36 in current file)
       - Lines 44-55: the `{/* Theme switcher small screen */}` JSX comment AND the commented `<div onClick={() => setTheme(activeTheme)} ...>` block following it (the one containing `FiMoon` / `FiSun` ternary)
       - Lines 187-198: the second `{/* Theme switcher large screen */}` block, same shape

       Preserve everything else. The active nav `<Link>` elements, the `<motion.nav>`, the `<svg>` hamburger, and the active brand `<img src={logoDark} />` all stay untouched. The `// import logoLight from '../../assets/logo-flower-of-life-light.png';` comment on line 8 STAYS in this plan (PATTERNS.md owns commented-asset cleanup in VIS-05).

       After this edit:
       - `grep -q "useThemeSwitcher" src/components/shared/AppHeader.jsx` must return FALSE.
       - `grep -q "Theme switcher" src/components/shared/AppHeader.jsx` must return FALSE.
       - `grep -q "FiMoon" src/components/shared/AppHeader.jsx` must return FALSE.
       - `grep -q "FiSun" src/components/shared/AppHeader.jsx` must return FALSE.

    3. **Edit `src/components/shared/AppBanner.jsx`** — delete EXACTLY:
       - Line 8: `	// const [activeTheme] = useThemeSwitcher();`
       - Line 10: `	// console.log('activeTheme: ', activeTheme);`
       - The single-line `// src={activeTheme === 'dark' ? brandLight : brandDark}` comment inside the brand `<img>` block (line 25)
       - Lines 66-69: the multi-line commented `// src={activeTheme === 'dark' ? heroLight : heroDark}` block inside the hero `<img>`

       Preserve everything else — including the active `<img src={brandMultiFont} />` and `<img src={heroLight} />` lines, all `motion.*` wrappers, all classNames.

       After this edit:
       - `grep -q "useThemeSwitcher" src/components/shared/AppBanner.jsx` must return FALSE.
       - `grep -q "activeTheme" src/components/shared/AppBanner.jsx` must return FALSE.

    DO NOT delete the active `import { FiMenu, FiX } from 'react-icons/fi';` in AppHeader (those are still used). DO NOT touch `import { capabilitiesTitle } from '../../data/projects';` — that belongs to Plan 04 (FOUND-02).
  </action>
  <verify>
    <automated>! test -e src/hooks/useThemeSwitcher.jsx && ! grep -rln "useThemeSwitcher" src/ && ! grep -q "Theme switcher" src/components/shared/AppHeader.jsx && ! grep -q "FiMoon" src/components/shared/AppHeader.jsx && ! grep -q "FiSun" src/components/shared/AppHeader.jsx && ! grep -q "activeTheme" src/components/shared/AppBanner.jsx && grep -q "import { FiMenu, FiX }" src/components/shared/AppHeader.jsx && pnpm build && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `test -e src/hooks/useThemeSwitcher.jsx` returns FALSE (file deleted).
    - `grep -rln "useThemeSwitcher" src/` returns nothing (no surviving references in src/).
    - `grep -q "FiMoon\|FiSun" src/components/shared/AppHeader.jsx` returns FALSE.
    - `grep -q "Theme switcher" src/components/shared/AppHeader.jsx` returns FALSE.
    - `grep -q "activeTheme" src/components/shared/AppBanner.jsx` returns FALSE.
    - `grep -q "import { FiMenu, FiX } from 'react-icons/fi'" src/components/shared/AppHeader.jsx` STILL succeeds (active icons preserved).
    - `grep -q "src={brandMultiFont}" src/components/shared/AppBanner.jsx` STILL succeeds (active brand image preserved).
    - `grep -q "src={heroLight}" src/components/shared/AppBanner.jsx` STILL succeeds (active hero image preserved).
    - `pnpm build` exits 0.
  </acceptance_criteria>
  <done>`useThemeSwitcher.jsx` deleted; AppHeader/AppBanner have no commented theme-toggle markup; active markup preserved; build green.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Remove the four `-light` color tokens from `tailwind.config.js` and verify build</name>
  <files>tailwind.config.js</files>
  <read_first>
    - tailwind.config.js (current — confirms locations of the four `-light` keys at lines 32, 33, 34, 36 of the active config block)
    - .planning/phases/01-foundation-refactor-env-pinning/01-CONTEXT.md (D-08)
    - .planning/phases/01-foundation-refactor-env-pinning/01-PATTERNS.md §"`tailwind.config.js`" (lists what to keep and what to delete)
  </read_first>
  <action>
    Edit `tailwind.config.js`. Inside the `theme.extend.colors` object, DELETE EXACTLY these four entries (keep their indentation pattern in mind so the diff is surgical):

    ```js
    			'primary-light': '#F7F8FC',
    			'secondary-light': '#FFFFFF',
    			'ternary-light': '#f6f7f8',

    			'secondary-section-light': '#d1d1d1ff',
    ```

    Preserve EVERYTHING else inside the file:
    - The disabled commented module.exports block at the top (lines 1-19) — leave as-is.
    - `darkMode: 'class'` (this is what makes `dark:` variants work; deleting it would break the entire dark theme).
    - All `*-dark` color keys (`primary-dark`, `secondary-dark`, `ternary-dark`).
    - `secondary-section-dark`, `ternary-section-dark`.
    - `accent`, `accent-highlight`.
    - `container.padding` block.
    - `variants.extend.opacity`.
    - `plugins: ['@tailwindcss/forms']`.

    After the edit, run `pnpm build` from the repo root. Expected outcome: succeeds with no errors. Tailwind v3 JIT silently treats unknown utilities as no-ops, so any active `dark:text-primary-light` className in components becomes a no-op (the visual is dark mode anyway, so the rendered class is irrelevant — the dark style is whatever the `<html class="dark">` activates).

    **Error recovery (per PATTERNS.md discrepancy #3):** If `pnpm build` FAILS with a Tailwind error specifically referencing one of the deleted `*-light` tokens (rare in v3 JIT — it usually warns silently), restore ONLY the failing token's line and document it in the SUMMARY for VIS-05 to revisit during Phase 2's spruce sweep. Do NOT roll back the other three. Do NOT mass-edit component files in this phase to remove `dark:*-light` classNames — that is explicitly out of scope per D-08.

    DO NOT touch any consumer file's className. DO NOT delete `darkMode: 'class'`. DO NOT add new color tokens.
  </action>
  <verify>
    <automated>! grep -q "'primary-light'" tailwind.config.js && ! grep -q "'secondary-light'" tailwind.config.js && ! grep -q "'ternary-light'" tailwind.config.js && ! grep -q "'secondary-section-light'" tailwind.config.js && grep -q "darkMode: 'class'" tailwind.config.js && grep -q "'primary-dark'" tailwind.config.js && grep -q "'accent'" tailwind.config.js && pnpm build && test -f build/index.html && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "'primary-light'" tailwind.config.js` returns FALSE.
    - `grep -q "'secondary-light'" tailwind.config.js` returns FALSE.
    - `grep -q "'ternary-light'" tailwind.config.js` returns FALSE.
    - `grep -q "'secondary-section-light'" tailwind.config.js` returns FALSE.
    - `grep -q "darkMode: 'class'" tailwind.config.js` STILL succeeds.
    - `grep -q "'primary-dark'" tailwind.config.js` STILL succeeds.
    - `grep -q "'accent'" tailwind.config.js` STILL succeeds.
    - `grep -q "@tailwindcss/forms" tailwind.config.js` STILL succeeds.
    - `pnpm build` exits 0 AND `build/index.html` is generated.
  </acceptance_criteria>
  <done>Four `-light` color tokens removed from tailwind.config.js, all `*-dark` and `accent*` tokens preserved, `pnpm build` succeeds.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Visual smoke — header, footer, hero render dark with correct contrast</name>
  <what-built>The dark theme is now applied via static HTML attribute (`<html class="dark">`) instead of a render-time DOM mutation. Theme-toggle code is gone; light-mode color tokens are gone from Tailwind config. The site should look identical to before — same dark background, same text contrast, same accent color on the Contact button.</what-built>
  <how-to-verify>
    1. Run `pnpm start` from the repo root. Wait for the dev server to print `Local: http://localhost:3000`.
    2. Open `http://localhost:3000/` in a browser.
    3. Confirm the page is dark-themed (background should be the existing primary-dark `#291c30` / dark navy variants, not white). Header logo visible, nav links readable.
    4. Open dev tools, inspect the `<html>` element. Confirm `class="dark"` is set as a static attribute (no React-injected attribute, no class flicker on load).
    5. Open dev tools console. Confirm no warnings about Tailwind unknown utilities. Confirm no React warnings about `setState` during render.
    6. Click into `/styles` (or whatever path the nav shows). Open at least one project (style) detail page and confirm the gallery still renders. Plan 04 (FOUND-04) runs in the same wave (Wave 3) and may or may not have landed by the time you visually verify; do NOT test modal open/close behavior here — only verify the page mounts and renders dark. Modal interaction is owned by Plan 04 and its own visual checkpoint.
    7. Visit `/about`, `/contact`, `/materials`. Confirm each page renders with no white flash on load and no obvious contrast regressions.
    8. Stop the dev server (Ctrl-C).

    If any step shows a regression (e.g., a section turns white where it was previously dark), note the specific element / className and respond with the issue. Otherwise type `approved`.
  </how-to-verify>
  <resume-signal>Type "approved" or describe any visual regressions / console warnings observed.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none introduced) | This plan only edits HTML attribute, JS side-effect, JSX comments, and the Tailwind config. No new external inputs, no auth, no DB, no new dependencies. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-05 | Tampering | `localStorage` write removed | accept | Removing the unread `localStorage.setItem('theme','dark')` reduces attack surface (one fewer mutation per render). No consumer reads `localStorage.getItem('theme')` (verified by grep — only the deleted `useThemeSwitcher` did, and it's gone). |

No new external inputs, no auth, no DB, no new dependencies; threat surface unchanged from baseline. ASVS L1 not triggered.
</threat_model>

<verification>
- `<html class="dark">` is the static source of truth.
- `src/App.js` no longer reads `documentElement` or writes `localStorage`.
- `src/hooks/useThemeSwitcher.jsx` does not exist.
- `grep -rln "useThemeSwitcher" src/` returns nothing.
- `tailwind.config.js` has no `*-light` color keys but retains `darkMode: 'class'`, all `*-dark` keys, `accent`, `accent-highlight`.
- `pnpm build` succeeds.
- Manual smoke (Task 4) confirms no visual regression. (Plan 04 modal behavior is verified by Plan 04's own checkpoint, not this one.)
</verification>

<success_criteria>
ROADMAP success criterion #2 ("dark theme applied without a render-time side effect, `useThemeSwitcher` fully removed, site behaves identically (dark-only, committed)") is owned by this plan. Verified by Task 1 grep + Task 2 file-delete + Task 4 visual smoke.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-refactor-env-pinning/01-03-SUMMARY.md` recording: HTML attribute change, App.js side-effect removal, hook deletion, comment-cleanup file list, tailwind token deletions, AND any single token that had to be restored per the error-recovery clause in Task 3 (with explicit handoff note for VIS-05).
</output>
