---
phase: 02-bundle-1-relaunch-3d-printing-spruce-content-seo-shop-stub
plan: 04
type: execute
wave: 4
depends_on: ["02-01", "02-02", "02-03"]
files_modified:
  - src/pages/Home.jsx
  - src/pages/AboutMe.jsx
  - src/pages/Contact.jsx
  - src/pages/Projects.jsx
  - src/pages/ProjectSingle.jsx
  - src/pages/NotFound.jsx
  - public/index.html
  - public/robots.txt
  - public/og-default.png
  - scripts/generate-sitemap.cjs
  - package.json
autonomous: true
requirements: [SEO-01, SEO-02, SEO-03]
must_haves:
  truths:
    - "Every public route except /shop mounts <SEOHead> after this plan (Plan 05 wires /shop). Static routes (/, /about, /contact, /404) use hardcoded defaults; Sanity-driven routes (/styles, /styles/:slug, /3d-printing, /3d-printing/:slug) source title + description + og:image from the Sanity seo block on laser-style/print-style docs."
    - "Homepage embeds <JsonLdLocalBusiness /> sourcing studio-info from Sanity; the LocalBusiness JSON-LD validates in Google Rich Results Test"
    - "public/index.html has the static SEO defaults that crawlers without JS see (title, meta description, og: tags, og:image, theme-color #291c30)"
    - "build/sitemap.xml is generated postbuild from a STATIC_ROUTES list + dynamic Sanity slug query (laser-style + print-style); package.json has a postbuild script wired to scripts/generate-sitemap.cjs"
    - "public/robots.txt references the sitemap URL"
    - "public/og-default.png exists as the studio-branded fallback og:image"
    - "Wave atomicity (D-32): existing /styles and /styles/:slug routes render laser content without regression — `pnpm test -- --watchAll=false` passes and `pnpm build` (which runs `react-scripts build --openssl-legacy-provider` per package.json) completes successfully."
    - "Decision citations (Phase 2 CONTEXT.md decisions covered by this plan): D-19: per-page SEO via `react-helmet-async` — hybrid sourcing — hardcoded `<title>` + `<meta description>` + `og:` defaults in route components for static routes (/, /about, /contact, /404), and Sanity-driven `seo` block (`metaTitle`, `metaDescription`, `ogImage`) on `laser-style`/`print-style`/`studio-info` docs for content-driven routes (/styles, /styles/:slug, /3d-printing, /3d-printing/:slug). D-20: og:image strategy — single studio-branded fallback `public/og-default.png` is used for any route without its own image; per-service override applies when a Sanity doc has an image. D-21: `LocalBusiness` JSON-LD on the homepage only, sourced from `studio-info` Sanity singleton (name, description, url, telephone, address, areaServed, makesOffer). D-22: `sitemap.xml` is generated at build time by a custom postbuild Node script (`scripts/generate-sitemap.cjs`) — reads STATIC_ROUTES list + queries Sanity for `laser-style` and `print-style` slugs — and `public/robots.txt` references the sitemap URL."
  artifacts:
    - path: "scripts/generate-sitemap.cjs"
      provides: "Postbuild Node script that writes build/sitemap.xml"
      contains: "createClient"
    - path: "public/robots.txt"
      provides: "robots.txt referencing the sitemap"
      contains: "Sitemap:"
    - path: "public/og-default.png"
      provides: "Studio-branded fallback og:image"
    - path: "src/pages/Home.jsx"
      provides: "Mounts SEOHead + JsonLdLocalBusiness"
      contains: "JsonLdLocalBusiness"
  key_links:
    - from: "scripts/generate-sitemap.cjs"
      to: "Sanity CDN"
      via: "@sanity/client createClient with same projectId/dataset/apiVersion as src/utilities/sanityClient.jsx"
      pattern: "createClient"
    - from: "package.json#scripts.postbuild"
      to: "scripts/generate-sitemap.cjs"
      via: "node scripts/generate-sitemap.cjs"
      pattern: "node scripts/generate-sitemap"
    - from: "src/pages/Home.jsx"
      to: "src/components/shared/JsonLdLocalBusiness"
      via: "JsonLdLocalBusiness mounted once on homepage only (D-21)"
      pattern: "<JsonLdLocalBusiness"
    - from: "public/robots.txt"
      to: "https://shapesmith.studio/sitemap.xml"
      via: "Sitemap: directive"
      pattern: "Sitemap: https"
---

<objective>
Light up the SEO surface — mount `<SEOHead />` on every route with the right title/description/og: tags (hardcoded for static routes, Sanity-sourced for `/styles*` and `/3d-printing*`), mount `<JsonLdLocalBusiness />` on the homepage so search engines pick up the LocalBusiness structured data, generate `sitemap.xml` postbuild from Sanity slugs, and update `public/robots.txt` to reference the sitemap. Add the static SEO defaults to `public/index.html` so non-JS crawlers see something useful.

Purpose: SEO is the discoverability layer the relaunch needs to actually drive traffic. Local search needs structured data; link previews on Slack/Twitter/iMessage need og:tags; search-engine indexing needs `sitemap.xml`. This plan bridges Plan 01's scaffolds (`SEOHead`, `JsonLdLocalBusiness`) with real Sanity data and adds the build-time and static-asset pieces.

Output:
- `<SEOHead>` mounted on `Home`, `Projects` (both /styles and /3d-printing via the generalized page), `ProjectSingle`, `AboutMe`, `Contact`, `NotFound` (replacing the inline Helmet)
- `<JsonLdLocalBusiness>` mounted on `Home`
- `scripts/generate-sitemap.cjs` NEW — postbuild Node script
- `package.json` MODIFIED — `postbuild` script
- `public/index.html` MODIFIED — static meta description, og: defaults, theme-color
- `public/robots.txt` MODIFIED — Sitemap: directive
- `public/og-default.png` NEW — studio-branded fallback og:image
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@./CLAUDE.md
@.planning/phases/02-bundle-1-relaunch-3d-printing-spruce-content-seo-shop-stub/02-CONTEXT.md
@.planning/phases/02-bundle-1-relaunch-3d-printing-spruce-content-seo-shop-stub/02-RESEARCH.md
@.planning/phases/02-bundle-1-relaunch-3d-printing-spruce-content-seo-shop-stub/02-PATTERNS.md
@.planning/phases/02-bundle-1-relaunch-3d-printing-spruce-content-seo-shop-stub/02-UI-SPEC.md
@.planning/phases/02-bundle-1-relaunch-3d-printing-spruce-content-seo-shop-stub/02-03-SUMMARY.md
@src/components/shared/SEOHead.jsx
@src/components/shared/JsonLdLocalBusiness.jsx
@src/utilities/sanityClient.jsx
@src/context/SingleServiceContext.jsx
@src/pages/Home.jsx
@src/pages/AboutMe.jsx
@src/pages/Contact.jsx
@src/pages/Projects.jsx
@src/pages/ProjectSingle.jsx
@src/pages/NotFound.jsx
@public/index.html
@public/robots.txt
@package.json

<interfaces>
<!-- Locked decisions: D-19, D-20, D-21, D-22 -->

<!-- ===== SEOHead component (Plan 01) ===== -->
{ title?, description?, ogImage?: { url, altText }, ogUrl?, noindex? }
fullTitle = title ? `${title} — Shapesmith Studio` : 'Shapesmith Studio'

<!-- ===== JsonLdLocalBusiness component (Plan 01) ===== -->
No props. Self-fetches studio-info; renders <Helmet><script type="application/ld+json">...</script></Helmet>.

<!-- ===== Per-route SEO meta (D-19 hybrid sourcing) ===== -->
Static routes (hardcoded defaults):
  /:        title=null,                description="Custom laser cutting and 3D printing for local hobbyists and small businesses.",            ogUrl="https://shapesmith.studio/"
  /about:   title="About",              description="Shapesmith Studio is a one-person creative studio offering laser cutting and 3D printing.",  ogUrl="https://shapesmith.studio/about"
  /contact: title="Contact",            description="Get in touch about a laser cutting or 3D printing project.",                              ogUrl="https://shapesmith.studio/contact"
  /shop:    title="Shop — coming soon", description="Pre-made laser-cut and 3D-printed pieces, ready to take home.",                            ogUrl="https://shapesmith.studio/shop"
  /404:     title="Page not found",     noindex=true,                                                                                          (no ogUrl needed)

Sanity-driven routes (read from singleService.seo or fallback to singleService.title/description):
  /styles:                     title=service.navLabel ('Laser cutting'),  description="Custom laser cutting in wood, acrylic, leather, and more.",              ogUrl based on urlSegment
  /3d-printing:                title=service.navLabel ('3D printing'),    description="Custom 3D printing in PLA, PETG, TPU, and more.",                         ogUrl based on urlSegment
  /styles/:slug, /3d-printing/:slug:
    title       = singleService.seo?.metaTitle       ?? singleService.title
    description = singleService.seo?.metaDescription ?? singleService.description
    ogImage     = singleService.seo?.ogImage?.asset  → { url: asset.url, altText: ogImage.altText }
                  fallback to { url: '/og-default.png', altText: 'Shapesmith Studio' }
    ogUrl       = `https://shapesmith.studio/${service.urlSegment}/${singleService.slug?.current ?? singleService.slug}`

<!-- ===== Homepage JSON-LD ===== -->
Mount once: <JsonLdLocalBusiness /> in src/pages/Home.jsx (D-21).
DO NOT mount on /styles/:slug or any other route — homepage only.

<!-- ===== Sitemap script (D-22) ===== -->
File: scripts/generate-sitemap.cjs (CommonJS, .cjs extension required because package.json has no "type":"module").
Mechanism: pnpm runs npm `postbuild` lifecycle hook automatically after `pnpm build`.
Output: build/sitemap.xml (NOT public/sitemap.xml — postbuild writes to build/, gitignored).
STATIC_ROUTES = ['/', '/styles', '/3d-printing', '/about', '/contact', '/shop'].
DYNAMIC: query Sanity for laser-style + print-style slugs.
BASE_URL = 'https://shapesmith.studio'.

<!-- ===== robots.txt (D-22) ===== -->
User-agent: *
Disallow:

Sitemap: https://shapesmith.studio/sitemap.xml

<!-- ===== public/index.html static SEO defaults (D-19 belt-and-suspenders) ===== -->
Replace current <meta name="description" content="Web site created using create-react-app" /> with:
  <meta name="description" content="Shapesmith Studio — custom laser cutting and 3D printing for local hobbyists and small businesses." />
Add og: defaults:
  <meta property="og:title" content="Shapesmith Studio" />
  <meta property="og:description" content="Custom laser cutting and 3D printing." />
  <meta property="og:image" content="/og-default.png" />
  <meta property="og:type" content="website" />
Update theme-color: <meta name="theme-color" content="#291c30" />  (matches primary-dark token)
Update <title>: "Shapesmith Studio" (replaces current "React App").

<!-- ===== og-default.png (D-20) ===== -->
A studio-branded fallback image. PATTERNS marks this as "owner-supplied or planner-supplied logo on dark bg." For Plan 04: use the existing brand wordmark `src/images/brand-horizontal-multi-font.png` AS the og-default. Either copy it to `public/og-default.png` OR generate a recommended 1200x630 og-image from it. Recommended: copy the existing brand wordmark to public/ as og-default.png as a placeholder; flag in SUMMARY.md that owner may want to provide a higher-resolution dedicated og-image (1200x630, PNG/JPG ≤8MB per Open Graph spec).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Mount SEOHead on every route + JsonLdLocalBusiness on homepage + extend public/index.html static SEO defaults + place og-default.png</name>
  <files>src/pages/Home.jsx, src/pages/AboutMe.jsx, src/pages/Contact.jsx, src/pages/Projects.jsx, src/pages/ProjectSingle.jsx, src/pages/NotFound.jsx, public/index.html, public/og-default.png</files>
  <read_first>
    - .planning/phases/02-bundle-1-relaunch-3d-printing-spruce-content-seo-shop-stub/02-RESEARCH.md §"Pattern 4" (lines 636–712) + §"Code Examples Example 2" (lines 1267–1337) + §"Pattern 4 SPA crawler caveat" (lines 698–712)
    - .planning/phases/02-bundle-1-relaunch-3d-printing-spruce-content-seo-shop-stub/02-PATTERNS.md §"src/components/shared/SEOHead.jsx" + §"public/index.html"
    - .planning/phases/02-bundle-1-relaunch-3d-printing-spruce-content-seo-shop-stub/02-CONTEXT.md D-19, D-20, D-21
    - src/components/shared/SEOHead.jsx (the component being mounted)
    - src/components/shared/JsonLdLocalBusiness.jsx (the component being mounted)
    - src/pages/Home.jsx (current shape — composition target)
    - src/pages/Projects.jsx (current shape from Plan 02)
    - src/pages/ProjectSingle.jsx (current shape from Plan 02 — note inner ServiceDetailComposition uses useSingleService)
    - src/pages/AboutMe.jsx, src/pages/Contact.jsx, src/pages/NotFound.jsx (current shapes)
    - public/index.html (current static meta — replace CRA defaults)
    - src/images/brand-horizontal-multi-font.png (the source for the og-default fallback)
  </read_first>
  <action>
    Step 1 — Modify `src/pages/Home.jsx`:
      Add at the top of the page composition:
        ```jsx
        import SEOHead from '../components/shared/SEOHead';
        import JsonLdLocalBusiness from '../components/shared/JsonLdLocalBusiness';
        // ... in the JSX:
        <>
          <SEOHead
            title={null}
            description="Custom laser cutting and 3D printing for local hobbyists and small businesses."
            ogUrl="https://shapesmith.studio/"
            ogImage={{ url: '/og-default.png', altText: 'Shapesmith Studio' }}
          />
          <JsonLdLocalBusiness />
          {/* existing AppBanner + QuickInfo + OurProcess + QuickSpecs + Collaborations etc. */}
        </>
        ```
      Note: `title={null}` so SEOHead emits "Shapesmith Studio" without the em-dash decoration (homepage = bare brand title).

    Step 2 — Modify `src/pages/AboutMe.jsx`:
      Add at the top of the page composition:
        ```jsx
        import SEOHead from '../components/shared/SEOHead';
        // ... in the JSX (wrap existing content in a fragment):
        <>
          <SEOHead
            title="About"
            description="Shapesmith Studio is a one-person creative studio offering laser cutting and 3D printing."
            ogUrl="https://shapesmith.studio/about"
            ogImage={{ url: '/og-default.png', altText: 'Shapesmith Studio' }}
          />
          {/* existing AboutMeBio etc. */}
        </>
        ```

    Step 3 — Modify `src/pages/Contact.jsx`:
      Add at the top of the page composition:
        ```jsx
        import SEOHead from '../components/shared/SEOHead';
        // ... in the JSX:
        <>
          <SEOHead
            title="Contact"
            description="Get in touch about a laser cutting or 3D printing project."
            ogUrl="https://shapesmith.studio/contact"
            ogImage={{ url: '/og-default.png', altText: 'Shapesmith Studio' }}
          />
          {/* existing ContactForm + ContactDetails etc. */}
        </>
        ```

    Step 4 — Modify `src/pages/Projects.jsx` (the GRID page) — Sanity-driven SEO. The page receives `serviceKey` and consumes `useServices()` to read `service.navLabel`. The cleanest pattern is an inner component wrapped by `<ServicesProvider>` (mirroring how Plan 02 used `ServiceDetailComposition` for `ProjectSingle`):
      ```jsx
      import ServicesGrid from '../components/services/ServicesGrid';
      import MaterialsSection from '../components/services/MaterialsSection';
      import FAQ from '../components/services/FAQ';
      import TrustCopyBlock from '../components/services/TrustCopyBlock';
      import { ServicesProvider, useServices } from '../context/ServicesContext';
      import SEOHead from '../components/shared/SEOHead';

      const SEO_DESCRIPTIONS = {
        laser: 'Custom laser cutting in wood, acrylic, leather, and more.',
        print: 'Custom 3D printing in PLA, PETG, TPU, and more.',
      };

      const SEO_TITLES = {
        laser: 'Laser cutting',
        print: '3D printing',
      };

      const ProjectsInner = ({ serviceKey }) => {
        const { service } = useServices();
        return (
          <>
            <SEOHead
              title={SEO_TITLES[serviceKey]}
              description={SEO_DESCRIPTIONS[serviceKey]}
              ogUrl={`https://shapesmith.studio/${service.urlSegment}`}
              ogImage={{ url: '/og-default.png', altText: SEO_TITLES[serviceKey] }}
            />
            <div className="container mx-auto">
              <ServicesGrid />
              <TrustCopyBlock />
              <MaterialsSection serviceKey={serviceKey} />
              <FAQ serviceKey={serviceKey} />
            </div>
          </>
        );
      };

      const Projects = ({ serviceKey }) => (
        <ServicesProvider serviceKey={serviceKey}>
          <ProjectsInner serviceKey={serviceKey} />
        </ServicesProvider>
      );

      export default Projects;
      ```

    Step 5 — Modify `src/pages/ProjectSingle.jsx` to mount `<SEOHead>` inside the existing `ServiceDetailComposition` inner component (which already uses `useSingleService`):
      ```jsx
      import SEOHead from '../components/shared/SEOHead';
      import { useServices } from '../context/ServicesContext';
      // ... inside ServiceDetailComposition:
      const ServiceDetailComposition = ({ serviceKey }) => {
        const { service } = useServices();
        const { singleService } = useSingleService();
        const turnaround = singleService?.turnaround;
        const wontMakeScope = singleService?.wontMakeScope;

        // SEO meta sourced from Sanity seo block; fallback to title/description; og:image fallback to /og-default.png
        const seoTitle = singleService?.seo?.metaTitle ?? singleService?.title;
        const seoDescription = singleService?.seo?.metaDescription ?? singleService?.description;
        const seoOgImage = singleService?.seo?.ogImage?.asset
          ? { url: singleService.seo.ogImage.asset.url, altText: singleService.seo.ogImage.altText ?? singleService.title }
          : { url: '/og-default.png', altText: singleService?.title ?? 'Shapesmith Studio' };
        const slugStr = singleService?.slug?.current ?? singleService?.slug;
        const seoOgUrl = slugStr ? `https://shapesmith.studio/${service.urlSegment}/${slugStr}` : undefined;

        return (
          <>
            {singleService && (
              <SEOHead
                title={seoTitle}
                description={seoDescription}
                ogImage={seoOgImage}
                ogUrl={seoOgUrl}
              />
            )}
            <ServiceHeader />
            <ServiceGallery />
            <ServiceInfo />
            <TrustCopyBlock turnaround={turnaround} />
            <MaterialsSection serviceKey={serviceKey} />
            <FAQ serviceKey={serviceKey} />
            <WontMake wontMakeScope={wontMakeScope} />
          </>
        );
      };
      ```
    The SEOHead is conditionally rendered only when `singleService` is defined — avoids emitting a `<title>{slug} — Shapesmith Studio</title>` while data loads.

    Step 6 — Modify `src/pages/NotFound.jsx` to swap the inline `<Helmet>` (added in Plan 03 Task 3) for `<SEOHead noindex />`:
      ```jsx
      import { Link } from 'react-router-dom';
      import SEOHead from '../components/shared/SEOHead';

      const NotFound = () => (
        <>
          <SEOHead title="Page not found" noindex={true} />
          {/* existing section + h1 + body + Back to home CTA */}
        </>
      );
      ```
    Drop the inline `<Helmet>` block; remove `import { Helmet } from 'react-helmet-async';`. The `noindex={true}` prop emits `<meta name="robots" content="noindex" />` per Plan 01's SEOHead implementation.

    Step 7 — Modify `public/index.html`:

    Read the current file. The CRA-default `<head>` has:
      `<meta name="description" content="Web site created using create-react-app" />`
      `<title>React App</title>`
      `<meta name="theme-color" content="#000000">` (or similar)

    Replace with the Phase 2 static defaults per RESEARCH §"Pattern 4 SPA crawler caveat":
      ```html
      <meta name="description" content="Shapesmith Studio — custom laser cutting and 3D printing for local hobbyists and small businesses." />
      <meta property="og:title" content="Shapesmith Studio" />
      <meta property="og:description" content="Custom laser cutting and 3D printing." />
      <meta property="og:image" content="/og-default.png" />
      <meta property="og:type" content="website" />
      <meta name="theme-color" content="#291c30" />
      <title>Shapesmith Studio</title>
      ```
    Preserve the `class="dark"` attribute on `<html>` (Phase 1).
    Preserve the existing Netlify hidden-form prerender block (`<form name="contact-form" netlify ...>`) — Plan 05 extends it with the `service` field.

    Step 8 — Place `public/og-default.png`:

    The studio-branded fallback. Per PATTERNS, copy the existing brand wordmark from `src/images/brand-horizontal-multi-font.png` to `public/og-default.png` as a placeholder. Use `cp src/images/brand-horizontal-multi-font.png public/og-default.png`.
    Note in 02-04-SUMMARY.md: owner may want to provide a higher-resolution dedicated og-image (recommended Open Graph spec: 1200x630 PNG/JPG, ≤8MB). The brand wordmark is acceptable for relaunch but is not optimal aspect-ratio.

    Step 9 — `pnpm build` to confirm everything compiles, and check that `build/og-default.png` exists (CRA copies `public/` to `build/` verbatim).

    Constraints:
    - DO NOT add `<JsonLdLocalBusiness />` to any page other than `Home.jsx` (D-21).
    - DO NOT touch `src/pages/Shop.jsx` here — Plan 05 owns the Shop Coming Soon page (which includes its OWN inline `<SEOHead>` per RESEARCH Code Example 5).
    - DO NOT touch `ContactForm.jsx` here — Plan 05 owns the form changes.
    - The SEOHead Sanity-driven path for `/styles/:slug` and `/3d-printing/:slug` reads `singleService.seo.metaTitle/metaDescription/ogImage` (declared in Plan 01's GROQ projection). If owner hasn't filled those Sanity fields yet, fallback chain is: metaTitle ?? title; metaDescription ?? description; ogImage ?? /og-default.png. Test with Sanity returning empty seo block.
  </action>
  <verify>
    <automated>grep -q "SEOHead" src/pages/Home.jsx && grep -q "JsonLdLocalBusiness" src/pages/Home.jsx && grep -q "SEOHead" src/pages/AboutMe.jsx && grep -q "SEOHead" src/pages/Contact.jsx && grep -q "SEOHead" src/pages/Projects.jsx && grep -q "SEOHead" src/pages/ProjectSingle.jsx && grep -q "SEOHead" src/pages/NotFound.jsx && (! grep -q "import { Helmet }" src/pages/NotFound.jsx) && grep -q "noindex" src/pages/NotFound.jsx && grep -q "Shapesmith Studio — custom laser cutting" public/index.html && grep -q '"#291c30"' public/index.html && (! grep -q "Web site created using create-react-app" public/index.html) && (! grep -q "<title>React App</title>" public/index.html) && test -f public/og-default.png && pnpm build && test -f build/og-default.png && pnpm test -- --watchAll=false</automated>
  </verify>
  <acceptance_criteria>
    - All five static-route pages mount SEOHead: `for f in src/pages/Home.jsx src/pages/AboutMe.jsx src/pages/Contact.jsx src/pages/NotFound.jsx; do grep -q "SEOHead" "$f" || exit 1; done` (Shop is Plan 05's job — exclude here)
    - Sanity-driven pages mount SEOHead inside the provider boundary: `grep -q "SEOHead" src/pages/Projects.jsx && grep -q "SEOHead" src/pages/ProjectSingle.jsx`
    - `Home.jsx` mounts `JsonLdLocalBusiness`: `grep -q "<JsonLdLocalBusiness" src/pages/Home.jsx`
    - `JsonLdLocalBusiness` is mounted ONLY on Home: `for f in src/pages/AboutMe.jsx src/pages/Contact.jsx src/pages/Projects.jsx src/pages/ProjectSingle.jsx src/pages/NotFound.jsx src/pages/Shop.jsx; do grep -q "JsonLdLocalBusiness" "$f" && echo "FAIL: JSON-LD in $f" && exit 1 || true; done`
    - `NotFound.jsx` swapped inline Helmet for SEOHead: `grep -q "<SEOHead" src/pages/NotFound.jsx && grep -q "noindex={true}" src/pages/NotFound.jsx && ! grep -q "import { Helmet }" src/pages/NotFound.jsx`
    - `ProjectSingle.jsx` SEOHead reads from Sanity seo block with fallback chain: `grep -q "singleService?.seo?.metaTitle" src/pages/ProjectSingle.jsx && grep -q "singleService?.seo?.metaDescription" src/pages/ProjectSingle.jsx && grep -q "/og-default.png" src/pages/ProjectSingle.jsx`
    - `Projects.jsx` SEOHead title varies by serviceKey: `grep -q "SEO_TITLES" src/pages/Projects.jsx || grep -qE "title.*service.*navLabel" src/pages/Projects.jsx`
    - `public/index.html` has Phase 2 description: `grep -q "Shapesmith Studio — custom laser cutting" public/index.html`
    - `public/index.html` has og: defaults: `grep -q 'property="og:title"' public/index.html && grep -q 'property="og:description"' public/index.html && grep -q 'property="og:image"' public/index.html && grep -q 'property="og:type"' public/index.html`
    - `public/index.html` theme-color is `#291c30`: `grep -q '"#291c30"' public/index.html`
    - `public/index.html` title is "Shapesmith Studio" (NOT "React App"): `grep -q "<title>Shapesmith Studio</title>" public/index.html && ! grep -q "<title>React App</title>" public/index.html`
    - CRA boilerplate description is GONE: `! grep -q "Web site created using create-react-app" public/index.html`
    - `<html class="dark">` is preserved (Phase 1): `grep -q 'class="dark"' public/index.html`
    - `public/og-default.png` exists: `test -f public/og-default.png`
    - After `pnpm build`, `build/og-default.png` is copied: `test -f build/og-default.png`
    - `pnpm build` exits 0
    - `pnpm test -- --watchAll=false` exits 0
  </acceptance_criteria>
  <done>Every route mounts SEOHead with appropriate meta. Homepage embeds LocalBusiness JSON-LD. Static defaults in `public/index.html` cover non-JS crawlers. og-default.png is in place. Build succeeds and copies the asset.</done>
</task>

<task type="auto">
  <name>Task 2: Sitemap script + postbuild wiring + robots.txt</name>
  <files>scripts/generate-sitemap.cjs, package.json, public/robots.txt</files>
  <read_first>
    - .planning/phases/02-bundle-1-relaunch-3d-printing-spruce-content-seo-shop-stub/02-RESEARCH.md §"Pattern 5: Sitemap generation" (lines 714–786)
    - .planning/phases/02-bundle-1-relaunch-3d-printing-spruce-content-seo-shop-stub/02-PATTERNS.md §"scripts/generate-sitemap.cjs"
    - .planning/phases/02-bundle-1-relaunch-3d-printing-spruce-content-seo-shop-stub/02-CONTEXT.md D-22
    - src/utilities/sanityClient.jsx (matches projectId/dataset/apiVersion exactly)
    - public/robots.txt (current shape)
    - package.json (current scripts block — Plan 01 didn't add postbuild yet)
  </read_first>
  <action>
    Step 1 — Create the directory if it doesn't exist:
      `mkdir -p scripts`

    Step 2 — Create `scripts/generate-sitemap.cjs` per RESEARCH §"Pattern 5":
      ```js
      // Postbuild script — runs automatically after `pnpm build` via npm/pnpm lifecycle.
      // Outputs build/sitemap.xml. Static routes hardcoded; dynamic Sanity slugs queried at build time.
      // Hardcoded projectId/dataset/apiVersion match src/utilities/sanityClient.jsx — env-driven config
      // is deferred to Phase 4 Vite migration per CONTEXT.md.
      const fs = require('fs');
      const path = require('path');
      const { createClient } = require('@sanity/client');

      // STATIC_ROUTES: keep in sync with src/App.js when routes are added.
      // /materials and /404 are intentionally excluded (one is a redirect; the other is noindex).
      const STATIC_ROUTES = ['/', '/styles', '/3d-printing', '/about', '/contact', '/shop'];
      const BASE_URL = 'https://shapesmith.studio';

      const client = createClient({
        projectId: 'qx9kep1e',
        dataset: 'production',
        useCdn: true,
        apiVersion: '2023-06-16',
      });

      const QUERY = `{
        "laserSlugs": *[_type == "laser-style" && defined(slug.current)].slug.current,
        "printSlugs": *[_type == "print-style" && defined(slug.current)].slug.current
      }`;

      const escapeXml = (s) =>
        String(s)
          .replace(/&/g, '&amp;')
          .replace(/</g, '&lt;')
          .replace(/>/g, '&gt;')
          .replace(/"/g, '&quot;')
          .replace(/'/g, '&apos;');

      (async () => {
        try {
          const { laserSlugs = [], printSlugs = [] } = await client.fetch(QUERY);
          const dynamic = [
            ...laserSlugs.map((s) => `/styles/${s}`),
            ...printSlugs.map((s) => `/3d-printing/${s}`),
          ];
          const today = new Date().toISOString().slice(0, 10);
          const allRoutes = [...STATIC_ROUTES, ...dynamic];
          const urls = allRoutes
            .map((u) => `\t<url><loc>${escapeXml(BASE_URL + u)}</loc><lastmod>${today}</lastmod></url>`)
            .join('\n');
          const xml = `<?xml version="1.0" encoding="UTF-8"?>
<urlset xmlns="http://www.sitemaps.org/schemas/sitemap/0.9">
${urls}
</urlset>
`;
          const buildDir = path.join(__dirname, '..', 'build');
          if (!fs.existsSync(buildDir)) {
            console.error('build/ directory does not exist. Run `pnpm build` before `pnpm postbuild`.');
            process.exit(1);
          }
          fs.writeFileSync(path.join(buildDir, 'sitemap.xml'), xml);
          console.log(`sitemap.xml written with ${allRoutes.length} URLs`);
        } catch (err) {
          console.error('Sitemap generation failed:', err);
          process.exit(1);
        }
      })();
      ```
    Notes:
    - `escapeXml` mitigates the XML-injection risk if a Sanity slug ever contains `&`/`<`/`>` (sitemap injection mitigation — see threat_model below). Sanity slugs are typically alphanumeric + hyphens, but defensive escaping is one line.
    - Hardcoded `projectId: 'qx9kep1e'`, `dataset: 'production'`, `apiVersion: '2023-06-16'` — MUST match `src/utilities/sanityClient.jsx` exactly. Phase 4 Vite migration moves these to env vars.
    - File extension `.cjs` is REQUIRED — `package.json` has no `"type": "module"`, but `.cjs` makes the CommonJS intent explicit and future-proof if anyone adds `"type":"module"` later.

    Step 3 — Modify `package.json` `scripts` block to add `postbuild`:
      ```json
      "scripts": {
        "start": "react-scripts start --openssl-legacy-provider",
        "build": "react-scripts build --openssl-legacy-provider",
        "postbuild": "node scripts/generate-sitemap.cjs",
        "test": "react-scripts test",
        "eject": "react-scripts eject",
        "build:css": "postcss src/css/tailwind.css -o src/css/main.css"
      }
      ```
    `pnpm` runs `postbuild` automatically after `pnpm build` per npm/pnpm lifecycle hooks (RESEARCH §"Netlify behavior" lines 775).

    Step 4 — Modify `public/robots.txt`:
      Read the current file. Replace with:
      ```
      User-agent: *
      Disallow:

      Sitemap: https://shapesmith.studio/sitemap.xml
      ```
    The empty `Disallow:` allows all paths; the `Sitemap:` directive points crawlers at the postbuild-generated sitemap. CRA copies `public/robots.txt` to `build/robots.txt` verbatim.

    Step 5 — Run `pnpm build` and verify the postbuild ran:
      `pnpm build` should print `sitemap.xml written with N URLs` after the build output.
      `test -f build/sitemap.xml && head -5 build/sitemap.xml` should show the XML structure.
      `test -f build/robots.txt && grep -q "Sitemap:" build/robots.txt`.

    Constraints:
    - DO NOT commit `build/sitemap.xml` to git — it's generated postbuild and `build/` is gitignored.
    - DO NOT add a runtime `@sanity/client` dependency just for the script — it's already in `dependencies` (Phase 1).
    - DO NOT use `node-fetch` or any non-stdlib dependency in the script — keep it zero-new-dep.
  </action>
  <verify>
    <automated>test -f scripts/generate-sitemap.cjs && grep -q "createClient" scripts/generate-sitemap.cjs && grep -q "qx9kep1e" scripts/generate-sitemap.cjs && grep -q "STATIC_ROUTES" scripts/generate-sitemap.cjs && grep -q "escapeXml" scripts/generate-sitemap.cjs && grep -q '"postbuild"' package.json && grep -q "node scripts/generate-sitemap" package.json && grep -q "Sitemap: https://shapesmith.studio/sitemap.xml" public/robots.txt && pnpm build && test -f build/sitemap.xml && test -f build/robots.txt && grep -q "<urlset" build/sitemap.xml && grep -q "<loc>https://shapesmith.studio/</loc>" build/sitemap.xml && pnpm test -- --watchAll=false</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/generate-sitemap.cjs` exists and uses CommonJS: `test -f scripts/generate-sitemap.cjs && grep -q "require(" scripts/generate-sitemap.cjs && grep -q "createClient" scripts/generate-sitemap.cjs`
    - Sanity client config matches `src/utilities/sanityClient.jsx`: `grep -q "qx9kep1e" scripts/generate-sitemap.cjs && grep -q "production" scripts/generate-sitemap.cjs && grep -q "2023-06-16" scripts/generate-sitemap.cjs`
    - Static routes include all 6 expected: `for r in '"/"' '"/styles"' '"/3d-printing"' '"/about"' '"/contact"' '"/shop"'; do grep -q "$r" scripts/generate-sitemap.cjs || exit 1; done`
    - Dynamic GROQ queries laser + print slugs: `grep -q "laser-style" scripts/generate-sitemap.cjs && grep -q "print-style" scripts/generate-sitemap.cjs`
    - Sitemap injection mitigation present (`escapeXml` or equivalent): `grep -qE "escape|encode" scripts/generate-sitemap.cjs`
    - `package.json` scripts has `postbuild`: `grep -q '"postbuild": *"node scripts/generate-sitemap' package.json`
    - `public/robots.txt` references the sitemap URL: `grep -q "Sitemap: https://shapesmith.studio/sitemap.xml" public/robots.txt`
    - `public/robots.txt` allows crawling (empty Disallow): `grep -qE "^Disallow: *$" public/robots.txt`
    - After `pnpm build`, `build/sitemap.xml` exists and has the correct XML root: `test -f build/sitemap.xml && head -1 build/sitemap.xml | grep -q '<?xml'`
    - The sitemap contains the homepage URL: `grep -q "<loc>https://shapesmith.studio/</loc>" build/sitemap.xml`
    - The sitemap contains the laser + print listing routes: `grep -q "<loc>https://shapesmith.studio/styles</loc>" build/sitemap.xml && grep -q "<loc>https://shapesmith.studio/3d-printing</loc>" build/sitemap.xml`
    - `build/robots.txt` exists (CRA copies public/robots.txt verbatim): `test -f build/robots.txt && grep -q "Sitemap" build/robots.txt`
    - The build doesn't fail if Sanity returns empty slugs (script handles `[]`): `pnpm build` exits 0 even when `print-style` schema isn't published yet
    - `pnpm test -- --watchAll=false` exits 0
  </acceptance_criteria>
  <done>The postbuild sitemap script is wired and writes a real sitemap.xml after every build, populated with both static routes and dynamic Sanity slugs. robots.txt references the sitemap. Build + smoke test pass. The site is now fully crawlable with structured-data signals.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser ↔ DOM (SEOHead meta) | React renders title/description/og: tags via `<Helmet>`; Sanity strings flow through React text nodes (auto-escaped). |
| browser ↔ DOM (JSON-LD on homepage) | LocalBusiness JSON-LD includes Sanity strings; mitigated via JSON.stringify (T-02-01-01 from Plan 01). |
| build ↔ Sanity CDN | Postbuild Node script reads slugs at build time. Anonymous read access; no secrets. |
| browser ↔ /sitemap.xml | Static XML file served by Netlify; no auth, no user input. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-04-01 | Tampering / XSS | `<title>{seoTitle} — Shapesmith Studio</title>` in SEOHead | mitigate | Sanity-derived `seoTitle` flows through React text node — auto-escaped. The em-dash is a literal, not concatenated user input. NEVER use `dangerouslySetInnerHTML` for SEO meta. |
| T-02-04-02 | Tampering / XML injection | `scripts/generate-sitemap.cjs` writes Sanity slugs into `<loc>` elements | mitigate | `escapeXml` helper escapes `&`/`<`/`>`/`"`/`'` in any URL or slug before it lands in XML. Sanity slugs are typically alphanumeric + hyphens (a-zA-Z0-9-), but defensive escaping is one line and prevents future surprise. |
| T-02-04-03 | Information disclosure | Sitemap exposes ALL Sanity laser-style + print-style slugs at build time | accept | This is the purpose of the sitemap — search engines discover content. Sanity is the canonical content source; if a doc is published, it's intended to be indexed. Owner can mark a doc unpublished in Sanity if they don't want it crawled (Sanity drafts are excluded by `useCdn: true`). |
| T-02-04-04 | Tampering | Hardcoded `projectId`/`dataset` in sitemap script | accept | Same disposition as T-02-01-02 (Plan 01). Public-knowledge identifiers; read-only with no auth token. Phase 4 Vite migration moves to env-driven config (CONTEXT Deferred Idea). |
| T-02-04-05 | DoS | Sitemap script runs at build time; failure blocks deploy | mitigate | Script wraps the body in `try/catch` and `process.exit(1)` on failure — Netlify build fails loudly rather than silently shipping a stale sitemap. The `if (!fs.existsSync(buildDir)) { ... }` guard prevents writing to a missing directory. |
| T-02-04-06 | JSON-LD injection (cross-reference T-02-01-01 from Plan 01) | `<JsonLdLocalBusiness />` mounted on Home | mitigate | Already mitigated by `JSON.stringify(ld)` in Plan 01's component (auto-escapes `</script>` to `<\/script>`). Plan 04 surfaces the threat here because the component is now actually MOUNTED. No additional code changes — Plan 01 already mitigated. |
| T-02-04-07 | Information disclosure | `og:image` URL falls back to `/og-default.png` (the brand wordmark) | accept | Brand wordmark is intended for public link previews. No PII or sensitive material. Owner may upload a higher-resolution dedicated og-image later. |
| T-02-04-08 | Tampering | `public/index.html` static description / og: defaults | accept | Static editor-controlled copy in source. Not user-controllable. Reviewed and accepted. |
</threat_model>

<verification>
After all two tasks land:

1. **Every route mounts SEOHead with appropriate meta:**
   `for f in src/pages/Home.jsx src/pages/AboutMe.jsx src/pages/Contact.jsx src/pages/Projects.jsx src/pages/ProjectSingle.jsx src/pages/NotFound.jsx; do grep -q "SEOHead" "$f" || exit 1; done`.

2. **JSON-LD is on Home only:**
   `grep -q "JsonLdLocalBusiness" src/pages/Home.jsx && ! grep -rE "JsonLdLocalBusiness" src/pages/AboutMe.jsx src/pages/Contact.jsx src/pages/Projects.jsx src/pages/ProjectSingle.jsx src/pages/NotFound.jsx src/pages/Shop.jsx`.

3. **Static defaults in `public/index.html`:**
   `grep -q "Shapesmith Studio — custom laser cutting" public/index.html && ! grep -q "Web site created using create-react-app" public/index.html && ! grep -q "<title>React App</title>" public/index.html`.

4. **Sitemap generated postbuild:**
   `pnpm build && test -f build/sitemap.xml && grep -q "<urlset" build/sitemap.xml && grep -q "<loc>https://shapesmith.studio/</loc>" build/sitemap.xml`.

5. **robots.txt references sitemap:**
   `grep -q "Sitemap: https://shapesmith.studio/sitemap.xml" public/robots.txt && grep -q "Sitemap" build/robots.txt` (after build).

6. **Phase 1 smoke test still passes:**
   `pnpm test -- --watchAll=false` exits 0.

7. **No half-spruce regression:**
   `! grep -rE "indigo-[0-9]+" src/components/` (Plan 03 cleanup holds).

8. **Manual verification (executor performs once at deploy preview):**
   - Open dev tools on each page, inspect `<head>`. Verify `<title>` updates per route. Verify `og:title`, `og:description`, `og:image`, `og:url` all present.
   - On homepage, inspect `<head>` for `<script type="application/ld+json">` containing the LocalBusiness JSON-LD.
   - After deploy, paste the homepage URL into Google Rich Results Test (`search.google.com/test/rich-results`) — should validate `LocalBusiness` without errors (or warn ONLY about missing optional fields like reviews).
   - After deploy, fetch `https://shapesmith.studio/sitemap.xml` — should return XML with all routes.
   - After deploy, fetch `https://shapesmith.studio/robots.txt` — should include `Sitemap:` directive.
   - Paste the homepage URL into Twitter Card validator and Slack share — link preview should show og:title + og:description + og:image (the brand wordmark).
</verification>

<success_criteria>
- All requirements (SEO-01, SEO-02, SEO-03) addressed.
- Site is in deployable state with the SEO infrastructure in place. Plans 05 closes contact + shop + cleanup + README.
- D-32 wave-atomicity holds.
- Crawlers + link previews + structured-data validators all return positive results after deploy. The owner can confidently start posting links to the site knowing every route has a sensible link preview.
</success_criteria>

<output>
After completion, create `.planning/phases/02-bundle-1-relaunch-3d-printing-spruce-content-seo-shop-stub/02-04-SUMMARY.md` capturing:
- Whether Google Rich Results Test was run against the deploy preview homepage and what the result was. If the studio-info Sanity doc isn't populated yet, the JSON-LD will be near-empty — record what was returned.
- Whether the sitemap.xml was fetched from the deploy preview to verify it contains all expected routes.
- Whether og-default.png was the brand wordmark copy (placeholder) or an owner-provided dedicated image.
- The exact URL count emitted to sitemap.xml at first build (helps next plan verify nothing dropped). Likely range: 6 static + 0–N dynamic = 6+N URLs.
- Any deviations from the schema spec — e.g., if `singleService.seo` returned empty everywhere because owner hasn't filled it, the fallback chain (title/description/og-default.png) was exercised.
</output>
