---
phase: 04-vite-migration
plan: 03
type: execute
wave: 3
depends_on:
  - "04-02"
files_modified:
  - src/App.test.js
  - src/hooks/useLocalStorageState.test.jsx
  - src/utilities/quote/volumeAndBbox.test.js
  - src/utilities/quote/calculatePrice.test.js
  - src/utilities/quote/parseSvg.test.js
  - src/utilities/quote/formatQuoteText.test.js
  - src/utilities/quote/formatErrors.test.js
  - src/components/quote/QuoteSubmitForm.test.jsx
autonomous: true
requirements:
  - VITE-01
must_haves:
  truths:
    - "`pnpm test` exits 0 and reports all 8 test files passing under Vitest"
    - "Zero `jest.fn`, `jest.mock`, `jest.spyOn`, `jest.resetModules`, `jest.restoreAllMocks` references remain in `src/`"
    - "The `vi.mock('./utilities/sanityImage', ...)` workaround in `App.test.js` has been removed (or kept with a documented deviation if it turned out to still be needed)"
    - "All test files that use `vi.*` import `vi` explicitly from `'vitest'`"
  artifacts:
    - path: "src/App.test.js"
      provides: "App smoke test under Vitest"
      contains: "from 'vitest'"
      contains_also: "vi.mock"
    - path: "src/components/quote/QuoteSubmitForm.test.jsx"
      provides: "QuoteSubmitForm tests under Vitest"
      contains: "vi.mock('react-google-recaptcha-v3'"
    - path: "src/setupTests.js"
      provides: "DOM matcher setup (unchanged — `@testing-library/jest-dom` works under Vitest)"
      contains: "@testing-library/jest-dom"
  key_links:
    - from: "vite.config.js#test.setupFiles"
      to: "src/setupTests.js"
      via: "Vitest runs setup file before tests"
      pattern: "setupFiles: './src/setupTests.js'"
    - from: "all test files"
      to: "vitest namespace `vi`"
      via: "`import { vi } from 'vitest'`"
      pattern: "from 'vitest'"
---

<objective>
Migrate the existing 8 Jest test files to Vitest's `vi.*` API. The migration is mechanical (per RESEARCH.md Pattern 6 and PATTERNS.md `### Other test files (7)`): replace `jest.fn` → `vi.fn`, `jest.mock` → `vi.mock`, `jest.spyOn` → `vi.spyOn`, `jest.resetModules` → `vi.resetModules`, `jest.restoreAllMocks` → `vi.restoreAllMocks`, and add `import { vi } from 'vitest'` to each file. Optionally remove the `vi.mock('./utilities/sanityImage', ...)` workaround in `App.test.js` (Pitfall 8) since Vitest's resolver handles ESM natively — restore only if a real failure surfaces.

Purpose: Plan 04-02 left the test suite broken because `vitest` doesn't know what `jest.*` is. Phase 1's FOUND-06 deliberately landed a smoke test (`App.test.js`) and Phase 3 added 7 unit tests for quote logic; deleting them would regress two phases' worth of coverage. The migration is small (~30-50 line changes total across 8 files) and unblocks future phases that lean on tests.

Output: All 8 test files run green under `pnpm test` (which executes `vitest run`). Zero `jest.*` references remain in `src/`. The `App.test.js` `sanityImage` mock is removed if the test still passes without it; otherwise the mock stays with a deviation note.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/04-vite-migration/04-RESEARCH.md
@.planning/phases/04-vite-migration/04-PATTERNS.md
@.planning/phases/04-vite-migration/04-01-SUMMARY.md
@.planning/phases/04-vite-migration/04-02-SUMMARY.md
@./CLAUDE.md
@vite.config.js
@src/setupTests.js
@src/App.test.js
@src/hooks/useLocalStorageState.test.jsx
@src/utilities/quote/volumeAndBbox.test.js
@src/utilities/quote/calculatePrice.test.js
@src/utilities/quote/parseSvg.test.js
@src/utilities/quote/formatQuoteText.test.js
@src/utilities/quote/formatErrors.test.js
@src/components/quote/QuoteSubmitForm.test.jsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Mechanical Jest → Vitest API migration across all 8 test files</name>
  <files>src/App.test.js, src/hooks/useLocalStorageState.test.jsx, src/utilities/quote/volumeAndBbox.test.js, src/utilities/quote/calculatePrice.test.js, src/utilities/quote/parseSvg.test.js, src/utilities/quote/formatQuoteText.test.js, src/utilities/quote/formatErrors.test.js, src/components/quote/QuoteSubmitForm.test.jsx</files>
  <read_first>
    - All 8 test files (full contents — they need to be edited)
    - .planning/phases/04-vite-migration/04-RESEARCH.md (`### Pattern 6: Vitest migration of existing tests` — lines 308-330; `### Step 6: App.test.js Vitest migration` — lines 587-611; `### Pitfall 8: Existing jest.mock workaround for ESM dep no longer needed` — lines 417-421)
    - .planning/phases/04-vite-migration/04-PATTERNS.md (`### src/App.test.js` — lines 288-342; `### Other test files (7)` — lines 346-413; preserves the `mock`-prefix hoist convention; preserves TAB indentation)
    - vite.config.js (confirm `test.globals: true` is set; if so, only `vi` needs explicit import)
  </read_first>
  <action>
For EACH of the 8 test files, apply the following changes uniformly. Preserve TAB indentation throughout (all test files use tabs — verified `useLocalStorageState.test.jsx` lines 19-25). Preserve the `mock`-prefix convention on local consts that mock factories close over (Vitest has the same hoisting semantics as Jest for `vi.mock()`).

**Universal find/replace per file** (apply mechanically, file-by-file):
- `jest.fn(`        → `vi.fn(`
- `jest.fn ` (with trailing space, e.g. `jest.fn );`) → `vi.fn ` — handle remaining cases
- `jest.mock(`      → `vi.mock(`
- `jest.spyOn(`     → `vi.spyOn(`
- `jest.resetModules(`     → `vi.resetModules(`
- `jest.restoreAllMocks(`  → `vi.restoreAllMocks(`
- `jest.clearAllMocks(`    → `vi.clearAllMocks(`
- `jest.useFakeTimers(`    → `vi.useFakeTimers(`
- `jest.advanceTimersByTime(` → `vi.advanceTimersByTime(`

After the replacements, if a file uses ANY `vi.*` symbol, add at the top (after existing imports):
```javascript
import { vi } from 'vitest';
```

Place the import alphabetically among existing imports (e.g., between `'@testing-library/react'` and the local `./Foo` imports — `'vitest'` sorts after node_modules but before relative imports). If the file has no existing imports above the local imports, just add the import at the top.

`describe`, `test`, `it`, `expect`, `beforeEach`, `afterEach`, `beforeAll`, `afterAll` do NOT need explicit imports because `vite.config.js#test.globals` is `true` (verify by reading vite.config.js). Do not add them.

**Per-file specifics:**

1. **`src/App.test.js`** — also REMOVE the `jest.mock('./utilities/sanityImage', ...)` workaround block at lines 11-28 (the entire `jest.mock` call AND the explanatory comment block lines 11-17). Per RESEARCH.md Pitfall 8, Vitest uses Vite's resolver and handles `@sanity/image-url` ESM natively. After removing the mock, the file should look approximately:
```javascript
import { render, screen } from '@testing-library/react';
import { vi } from 'vitest';
import App from './App';

vi.mock('./utilities/sanityClient', () => ({
	__esModule: true,
	default: {
		fetch: vi.fn(() => Promise.resolve([])),
	},
}));

test('App mounts without crashing and renders persistent nav', async () => {
	render(<App />);
	const contactLinks = await screen.findAllByText(/contact/i);
	expect(contactLinks.length).toBeGreaterThan(0);
});
```
Run `pnpm test` after this change. If the test fails, RESTORE the `vi.mock('./utilities/sanityImage', ...)` block (mechanically converted from the original `jest.mock` form) and document the restoration in the SUMMARY as a deviation. If the test passes (expected outcome per RESEARCH.md), proceed.

2. **`src/components/quote/QuoteSubmitForm.test.jsx`** — preserve the `mockExecuteRecaptcha` / `mockUseGoogleReCaptchaState` const names exactly. Vitest's `vi.mock()` has the same hoist-safety rule that Jest's `jest.mock()` has: closure references to local variables must be prefixed `mock` to be allowed by the hoisting transform. Do NOT rename them. Also note the existing `const QuoteSubmitForm = require('./QuoteSubmitForm').default;` — leave it as-is. Vitest supports CJS `require` in test files.

3. **`src/hooks/useLocalStorageState.test.jsx`** — uses `jest.restoreAllMocks()` in `beforeEach`/`afterEach` (lines 18-26). Both become `vi.restoreAllMocks()`.

4. **`src/utilities/quote/*.test.js`** (5 files) — these are pure-logic tests. They likely use `jest.fn()`, `jest.mock()`, or no jest API at all. Apply the universal find/replace; if a file has zero `jest.*` references, do NOT add the `import { vi } from 'vitest'` line (no point importing what you don't use).

DO NOT modify `src/setupTests.js` — its single line (`import '@testing-library/jest-dom';`) works unchanged under Vitest. The library's name still says "jest-dom" but it works with Vitest's `expect` because Vitest's matcher API is Jest-compatible.

DO NOT add `vitest.config.ts` or `vitest.config.js` — the Vitest config is inline in `vite.config.js#test` (Plan 04-01).

DO NOT add coverage flags — `@vitest/coverage-v8` is not installed and is out of scope.

DO NOT change test bodies, assertions, or behavior — only the API namespace.

After all 8 files are migrated, verify zero drift:
- `grep -rn "jest\." src/` should return zero hits.
- `grep -rn "from 'vitest'" src/` should return matches only in test files that actually use `vi.*`.
  </action>
  <verify>
    <automated>[ "$(grep -rn 'jest\.' src/ 2>/dev/null | wc -l | tr -d ' ')" = "0" ] &amp;&amp; pnpm test</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rn "jest\." src/` returns 0 hits
    - `grep -rn "jest\.fn" src/` returns 0 hits
    - `grep -rn "jest\.mock" src/` returns 0 hits
    - `grep -rn "jest\.restoreAllMocks" src/` returns 0 hits
    - `grep -c "vi.fn" src/components/quote/QuoteSubmitForm.test.jsx` returns at least 1
    - `grep -c "vi.mock" src/components/quote/QuoteSubmitForm.test.jsx` returns at least 1
    - `grep -c "vi.mock" src/App.test.js` returns at least 1 (sanityClient mock)
    - All test files that use any `vi.*` symbol have `import { vi } from 'vitest';`
    - `pnpm test` exits 0
    - `pnpm test` reports 8 test files (or however many tests exist — verify count matches `find src -name "*.test.*" | wc -l`)
    - No new test files created; no existing test files deleted
    - `src/setupTests.js` is unchanged
    - Mock-prefix convention preserved in `QuoteSubmitForm.test.jsx` (`mockExecuteRecaptcha`, `mockUseGoogleReCaptchaState`)
  </acceptance_criteria>
  <done>All 8 test files use Vitest's `vi.*` API; `pnpm test` exits 0 with all suites green; the legacy `sanityImage` mock workaround is removed (or restored with a documented deviation if it turned out to still be needed); the FOUND-06 smoke test contract is preserved end-to-end on the new test runner.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test code → production code | Mocks intercept module resolution. A bad mock can hide a real regression. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-03-01 | Tampering (silent regression) | Removal of `vi.mock('./utilities/sanityImage', ...)` in App.test.js | mitigate | Per RESEARCH.md Pitfall 8: remove the mock, run the test. If green, the mock was indeed CRA-Jest workaround debt. If the test fails, restore the mock and document as deviation in SUMMARY — no information is lost. |
| T-04-03-02 | Tampering (mock hoisting) | `mock`-prefix convention in QuoteSubmitForm.test.jsx | mitigate | Vitest's `vi.mock()` has identical hoisting semantics to Jest's `jest.mock()`. The PATTERNS.md guidance preserves the `mock` prefix exactly. |
| T-04-03-03 | DoS (test runner) | Vitest config in vite.config.js | accept | Vitest reuses Vite's resolver; failure mode is the test failing fast (not hanging). Acceptable for a CI/local test run. |
</threat_model>

<verification>
- All 8 test files migrated mechanically (jest.* → vi.*)
- `pnpm test` exits 0
- Zero `jest.` references in `src/`
- `src/setupTests.js` unchanged
- `App.test.js` `sanityImage` mock removed (or restored if it was actually needed; document either way)
</verification>

<success_criteria>
- Test suite is green under Vitest (FOUND-06 smoke test contract preserved on new runner)
- Maps to VITE-01: build tool migration is incomplete without a working test runner; this plan closes the test-runner half of the migration
</success_criteria>

<output>
After completion, create `.planning/phases/04-vite-migration/04-03-SUMMARY.md` documenting:
- The 8 files migrated and the per-file count of `vi.*` substitutions made.
- Whether the `App.test.js` `sanityImage` mock was successfully removed (expected) or restored (deviation).
- `pnpm test` final output (test count, duration).
- Any tests that needed extra adjustments beyond mechanical replacement (document with reasoning).
</output>
