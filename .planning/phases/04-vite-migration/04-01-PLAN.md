---
phase: 04-vite-migration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - vite.config.js
  - index.html
  - public/index.html
autonomous: true
requirements:
  - VITE-01
must_haves:
  truths:
    - "A `vite.config.js` exists at repo root and configures the JSX-in-.js loader, build.outDir='build', and Vitest test block"
    - "An `index.html` exists at repo root with `%PUBLIC_URL%` tokens removed and an explicit `<script type='module' src='/src/index.js'>` tag"
    - "The two Netlify form blocks (`contact-form`, `shop-notify`) appear byte-identical at the new `index.html` location — no character drift in `<form>`, `<input>`, `<textarea>` lines"
    - "`public/index.html` no longer exists (moved, not duplicated)"
  artifacts:
    - path: "vite.config.js"
      provides: "Vite build + Vitest config"
      contains: "outDir: 'build'"
      contains_also: "loader: 'jsx'"
      contains_also: "environment: 'jsdom'"
    - path: "index.html"
      provides: "Vite build entry HTML at project root"
      contains: "<script type=\"module\" src=\"/src/index.js\"></script>"
      contains_also: "name=\"contact-form\""
      contains_also: "name=\"shop-notify\""
  key_links:
    - from: "vite.config.js"
      to: "src/index.js"
      via: "implicit entry resolution from index.html script tag"
      pattern: "type=\"module\" src=\"/src/index.js\""
    - from: "vite.config.js#build.outDir"
      to: "scripts/generate-sitemap.cjs (line 59)"
      via: "writes to `build/`"
      pattern: "outDir: 'build'"
---

<objective>
Lay the Vite scaffolding without flipping the build switch yet. Create `vite.config.js` at repo root with the JSX-in-`.js` loader override, `outDir: 'build'`, and inline Vitest config; move `public/index.html` → `index.html` at repo root with `%PUBLIC_URL%` tokens stripped and an explicit ES-module entry `<script>` tag added; preserve both Netlify form blocks byte-identical so deploy-time form prerender keeps working.

Purpose: Phase 4 must produce a build that "behaves identically to the CRA build from any user's perspective" (Success Criterion 1). The two highest-risk seams are (a) the Netlify form prerender (Pitfall 2 — silent submission drop if forms get rewritten) and (b) the JSX-in-`.js` parse error (Pitfall 1 — first build fails at the first `<` token). This plan handles both: it lands the canonical config from RESEARCH.md Step 1 and the byte-faithful `index.html` move from RESEARCH.md Step 4 + Pattern 2.

Output: Two new files at repo root (`vite.config.js`, `index.html`), one deleted file (`public/index.html`). No code changes in `src/`. No `package.json` changes (Plan 04-02 owns that flip).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-vite-migration/04-RESEARCH.md
@.planning/phases/04-vite-migration/04-PATTERNS.md
@./CLAUDE.md
@public/index.html
@scripts/generate-sitemap.cjs
@tailwind.config.js
@postcss.config.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create vite.config.js at repo root</name>
  <files>vite.config.js</files>
  <read_first>
    - .planning/phases/04-vite-migration/04-RESEARCH.md (`### Step 1: Final vite.config.js (project root)` — lines 425-454; copy verbatim)
    - .planning/phases/04-vite-migration/04-PATTERNS.md (`### vite.config.js` section — note: indent with TABS to match `tailwind.config.js`/`postcss.config.js` repo convention; module style is ESM, not CommonJS)
    - tailwind.config.js (read to confirm tab indentation convention used at repo-root config files)
    - postcss.config.js (read to confirm tab indentation convention)
    - scripts/generate-sitemap.cjs (line 59 — `path.join(__dirname, '..', 'build', 'sitemap.xml')`; this is why outDir MUST be `'build'`)
  </read_first>
  <action>
Create `vite.config.js` at the repo root (NOT in `src/` or `public/`) with the exact content from RESEARCH.md Step 1, re-indented with tabs to match the existing repo-root config convention (see `tailwind.config.js` lines 25-61 — tabs). The file MUST:

1. Use ESM syntax (`import` + `export default defineConfig`) — this is the first ESM-style root config in the repo and is required by Vite 7.
2. Import `defineConfig` from `vite` and `react` from `@vitejs/plugin-react`.
3. Set `plugins: [react()]`.
4. Set `build.outDir: 'build'` — MANDATORY. Do NOT accept Vite's default `'dist'`. Rationale: `netlify.toml` line 3 (`publish = "build"`) and `scripts/generate-sitemap.cjs` line 59 (`path.join(__dirname, '..', 'build', 'sitemap.xml')`) both pin `build/`. Changing outDir to `'dist'` would silently break the sitemap (Pitfall 3) and require coordinated changes in three places.
5. Set `esbuild.loader: 'jsx'`, `esbuild.include: /src\/.*\.jsx?$/`, `esbuild.exclude: []` — required because `src/App.js` and `src/index.js` contain JSX in `.js` files. Without this, the build crashes at the first `<` token (Pitfall 1). Per RESEARCH.md Anti-Pattern, do NOT solve this by renaming the files to `.jsx` — keep the diff minimal.
6. Set `optimizeDeps.esbuildOptions.loader: { '.js': 'jsx' }` — same JSX rule applied to dep pre-bundling so the dev server doesn't fail on the first `.js`-with-JSX file it encounters during pre-bundle.
7. Set `test.globals: true`, `test.environment: 'jsdom'`, `test.setupFiles: './src/setupTests.js'`, `test.include: ['src/**/*.{test,spec}.{js,jsx}']` — Vitest config inline (no separate `vitest.config.ts`). Pointing `setupFiles` at the existing `src/setupTests.js` reuses the `import '@testing-library/jest-dom'` line unchanged.

DO NOT add: `splitVendorChunkPlugin` (removed in Vite 7 — Anti-Pattern); `define: { 'process.env': '{}' }` (premature shim — Pitfall 4 says verify before adding); `optimizeDeps.include: ['@sanity/client']` (premature — Pitfall 5); `envPrefix: ['REACT_APP_', 'VITE_']` (Don't Hand-Roll — just rename the one var in Plan 04-02); SWC plugin (Anti-Pattern); coverage reporter (out of scope).

Indentation: tabs throughout, matching `tailwind.config.js`. No trailing comma inconsistencies — match the style in `postcss.config.js`.
  </action>
  <verify>
    <automated>test -f vite.config.js &amp;&amp; grep -q "outDir: 'build'" vite.config.js &amp;&amp; grep -q "loader: 'jsx'" vite.config.js &amp;&amp; grep -q "environment: 'jsdom'" vite.config.js &amp;&amp; grep -q "setupFiles: './src/setupTests.js'" vite.config.js &amp;&amp; grep -q "@vitejs/plugin-react" vite.config.js</automated>
  </verify>
  <acceptance_criteria>
    - `vite.config.js` exists at repo root (NOT in `src/` or `public/`)
    - File uses ESM syntax (`import` and `export default`) — `grep -c "^import " vite.config.js` returns 2 (vite + plugin-react)
    - File contains `outDir: 'build'` (literal string match)
    - File contains `loader: 'jsx'` AND `'.js': 'jsx'` (the optimizeDeps form)
    - File contains `environment: 'jsdom'`, `globals: true`, `setupFiles: './src/setupTests.js'`
    - File does NOT contain `splitVendorChunkPlugin` (removed in Vite 7)
    - File does NOT contain `'dist'` (the wrong outDir default)
    - File uses tabs for indentation (`grep -P '^\t' vite.config.js | head -1` returns a match)
  </acceptance_criteria>
  <done>vite.config.js exists at repo root with the canonical Vite 7 + Vitest config from RESEARCH.md Step 1, tab-indented, ESM, outDir pinned to 'build'. No build is run yet (Plan 04-02 wires the npm scripts and runs the first build).</done>
</task>

<task type="auto">
  <name>Task 2: Move public/index.html → ./index.html with byte-faithful Netlify form preservation</name>
  <files>index.html, public/index.html</files>
  <read_first>
    - public/index.html (full file, lines 1-85 — the source of truth for byte-identical preservation)
    - .planning/phases/04-vite-migration/04-RESEARCH.md (`### Pattern 2: index.html move + entry point script tag` — lines 211-229; `### Step 4: index.html migration` — lines 511-560; `### Pitfall 2: Netlify form prerender goes silent` — lines 381-386)
    - .planning/phases/04-vite-migration/04-PATTERNS.md (`### index.html (build-entry, static)` — lines 80-120; lists the exact 4 edits and the verbatim-preservation contract for lines 49-72)
  </read_first>
  <action>
Move `public/index.html` to `./index.html` (project root) using `git mv` so git tracks the rename, then apply exactly four edits to the moved file. NO other text in the file may change — Netlify's deploy-time form prerender scan parses this HTML and ANY drift in `<form>` / `<input>` / `<textarea>` markup will silently drop submissions (Pitfall 2 / D-28 contract).

Steps in order:
1. Run `git mv public/index.html ./index.html` (preserves git blame).
2. Apply edit 1 — line 5:
   - BEFORE: `    <link rel="icon" href="%PUBLIC_URL%/favicon.png" />`
   - AFTER:  `    <link rel="icon" href="/favicon.png" />`
3. Apply edit 2 — line 21:
   - BEFORE: `    <link rel="apple-touch-icon" href="%PUBLIC_URL%/logo192.png" />`
   - AFTER:  `    <link rel="apple-touch-icon" href="/logo192.png" />`
4. Apply edit 3 — line 26:
   - BEFORE: `    <link rel="manifest" href="%PUBLIC_URL%/manifest.json" />`
   - AFTER:  `    <link rel="manifest" href="/manifest.json" />`
5. Apply edit 4 — add an ES module script tag inside `<body>` AFTER the closing `</form>` of the `shop-notify` form (currently line 73) and BEFORE the trailing CRA-template comment block (currently lines 74-83). Insert:
```html
    <script type="module" src="/src/index.js"></script>
```
Use 4-space indentation to match the existing 2-space-then-2-space body indentation pattern in this file (the existing forms use 4 spaces inside `<body>`).

CRITICAL — DO NOT TOUCH:
- Lines 49-62: the entire `<form name="contact-form" netlify ...>` block (D-28 contract).
- Lines 64-72: the entire `<form name="shop-notify" netlify ...>` block (D-23 contract).
- Lines 43-48: the `<!--` Netlify-prerender explanatory comment (informs future maintainers).
- The CRA-template comments at lines 27-35 and 74-83 (these are harmless and not worth a separate cleanup task).
- All `<meta>` tags from lines 4-20 (theme-color, description, og: tags from Plan 02-04 — Vite serves these statically).

DO NOT delete the CRA `%PUBLIC_URL%` explanatory comment block (lines 27-35) — leaving it as-is keeps the diff small. RESEARCH.md says it's harmless residue.

After the 4 edits, the diff `git diff HEAD -- public/index.html index.html` should show the rename plus exactly 3 modified lines (the `%PUBLIC_URL%` removals) and 1 added line (the script tag) — nothing else.

The original `public/index.html` is now gone (the `git mv` already removed it).
  </action>
  <verify>
    <automated>test -f index.html &amp;&amp; ! test -f public/index.html &amp;&amp; grep -q "<script type=\"module\" src=\"/src/index.js\"></script>" index.html &amp;&amp; grep -q 'name="contact-form"' index.html &amp;&amp; grep -q 'name="shop-notify"' index.html &amp;&amp; grep -q 'name="bot-field"' index.html &amp;&amp; ! grep -q "%PUBLIC_URL%" index.html &amp;&amp; [ "$(grep -c 'netlify-honeypot="bot-field"' index.html)" = "2" ]</automated>
  </verify>
  <acceptance_criteria>
    - `index.html` exists at repo root
    - `public/index.html` does NOT exist (moved, not copied)
    - `grep -c "%PUBLIC_URL%" index.html` returns 0 (all 3 tokens replaced)
    - `grep -c 'name="contact-form"' index.html` returns 1
    - `grep -c 'name="shop-notify"' index.html` returns 1
    - `grep -c 'netlify-honeypot="bot-field"' index.html` returns 2 (one per form)
    - `grep -c 'name="bot-field"' index.html` returns 2 (honeypot input in each form)
    - `grep -c 'name="service"' index.html` returns 1 (CTC-02 / D-28 contract — the new service field added in Plan 02-05)
    - `grep -c '<script type="module" src="/src/index.js">' index.html` returns 1
    - `git status --porcelain | grep -E "^R " | grep -E "public/index\.html.*-> index\.html"` shows the rename was tracked by git
  </acceptance_criteria>
  <done>`index.html` is at the project root, `public/index.html` is gone, the two Netlify form blocks (contact-form + shop-notify) are byte-identical to the original, all three `%PUBLIC_URL%` tokens are replaced with absolute `/` paths, and exactly one `<script type="module" src="/src/index.js">` tag has been added inside `<body>`. Plan 04-05 will run a deploy preview that confirms Netlify still detects both forms with all expected fields.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Repo → Netlify deploy-time scan | Netlify reads `index.html` from the deployed bundle and registers any `<form name="..." netlify ...>` blocks. Drift = silent submission drop. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01-01 | Tampering | `index.html` Netlify form blocks | mitigate | Byte-faithful preservation of lines 49-72 from `public/index.html`. Acceptance criteria use `grep -c` to confirm form names + honeypot count + `bot-field` count + `service` field count match original exactly. |
| T-04-01-02 | DoS (silent failure) | Vite HTML transform | mitigate | The `<script type="module">` tag is the only injection; Vite's HTML transform copies static elements verbatim. No `%PUBLIC_URL%`-style template tokens remain that could be silently mis-handled. |
| T-04-01-03 | Information Disclosure | `vite.config.js` | accept | No secrets in the config file. `outDir`, plugin names, and test config paths are public. The config is committed to git as expected. |
</threat_model>

<verification>
- `vite.config.js` exists at repo root with `outDir: 'build'`, JSX loader, Vitest config (acceptance criteria above)
- `index.html` exists at repo root with byte-faithful Netlify forms and explicit module script tag
- `public/index.html` is removed (git tracks it as a rename, not delete-then-create)
- No package.json changes in this plan — Plan 04-02 owns that flip
</verification>

<success_criteria>
- vite.config.js + index.html exist at repo root
- public/index.html no longer exists
- Both Netlify forms preserved byte-identical (contact-form fields = bot-field, name, email, service, subject, message; shop-notify fields = bot-field, email)
- All three `%PUBLIC_URL%` tokens replaced with `/` absolute paths
- One `<script type="module" src="/src/index.js">` tag added
- Maps to VITE-01 (build tool migration foundation — config + entry HTML in place)
</success_criteria>

<output>
After completion, create `.planning/phases/04-vite-migration/04-01-SUMMARY.md` documenting the byte-faithful preservation diff (3 `%PUBLIC_URL%` removals + 1 script tag addition + git rename) and any deviations from the RESEARCH.md verbatim config.
</output>
