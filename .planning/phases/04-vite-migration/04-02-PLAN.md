---
phase: 04-vite-migration
plan: 02
type: execute
wave: 2
depends_on:
  - "04-01"
files_modified:
  - package.json
  - pnpm-lock.yaml
  - src/App.js
  - src/components/quote/QuoteSubmitForm.jsx
autonomous: true
requirements:
  - VITE-01
  - VITE-02
must_haves:
  truths:
    - "`pnpm build` runs Vite (not `react-scripts`) and exits 0"
    - "`pnpm build` produces `build/index.html` and a hashed `build/assets/index-*.js` module entry"
    - "No code in `src/` references `process.env.REACT_APP_*` — the only runtime env-var reference is now `import.meta.env.VITE_RECAPTCHA_SITE_KEY`"
    - "No script in `package.json` passes `--openssl-legacy-provider`"
    - "`react-scripts` is no longer in `package.json` dependencies; `vite`, `@vitejs/plugin-react`, `vitest`, `jsdom` are in devDependencies"
  artifacts:
    - path: "package.json"
      provides: "Vite-driven scripts and dependency tree"
      contains: "\"build\": \"vite build\""
      contains_also: "\"vite\": \"^7"
      contains_also: "\"vitest\": \"^4"
    - path: "src/App.js"
      provides: "App root with Vite-shaped env-var reference"
      contains: "import.meta.env.VITE_RECAPTCHA_SITE_KEY"
    - path: "build/index.html"
      provides: "Vite-built static SPA entry HTML"
      generated_by: "pnpm build"
  key_links:
    - from: "src/App.js (line 35)"
      to: "Netlify dashboard env var (renamed in Plan 04-06)"
      via: "Vite build-time replacement of `import.meta.env.VITE_RECAPTCHA_SITE_KEY`"
      pattern: "import\\.meta\\.env\\.VITE_RECAPTCHA_SITE_KEY"
    - from: "package.json#scripts.build"
      to: "vite.config.js"
      via: "Vite reads root config on build"
      pattern: "\"build\": \"vite build\""
---

<objective>
Flip the build switch atomically: swap `react-scripts` for Vite in `package.json` (scripts + deps), rename the single runtime `process.env.REACT_APP_RECAPTCHA_SITE_KEY` reference to `import.meta.env.VITE_RECAPTCHA_SITE_KEY` in `src/App.js`, update the matching comment block in `App.js` and the doc-comment in `QuoteSubmitForm.jsx`, then run `pnpm install` + `pnpm build` to prove the migration produces a deployable bundle in `build/`.

Purpose: This is the central plan of the migration — it's where the build tool actually changes. Plan 04-01 laid the config; this plan turns it on. Plan 04-03 (Vitest test migration) cannot start until `vitest` is installed via the changes here. The env-var rename ships in this same plan because the diff touches `App.js` directly and bundling them keeps the "Vite-shaped" change atomic.

Output: A working Vite production build at `build/index.html` + `build/assets/*`. No more `react-scripts`, no more `--openssl-legacy-provider`, no more `process.env.REACT_APP_*`. Tests are temporarily broken (still using `jest.*` API) — Plan 04-03 fixes them. This is intentional: a Wave-3 plan owns the test migration so context cost stays under 50%.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-vite-migration/04-RESEARCH.md
@.planning/phases/04-vite-migration/04-PATTERNS.md
@.planning/phases/04-vite-migration/04-01-SUMMARY.md
@./CLAUDE.md
@package.json
@src/App.js
@src/components/quote/QuoteSubmitForm.jsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Swap package.json scripts and dependencies (CRA → Vite)</name>
  <files>package.json, pnpm-lock.yaml</files>
  <read_first>
    - package.json (full file — verify current state matches what RESEARCH.md describes)
    - .planning/phases/04-vite-migration/04-RESEARCH.md (`### Step 2: package.json script changes` — lines 457-471; `### Step 3: package.json dependency changes` — lines 477-508; `### Pitfall 7` — `--openssl-legacy-provider` removal)
    - .planning/phases/04-vite-migration/04-PATTERNS.md (`### package.json (manifest)` — lines 162-216 — confirms 2-space JSON indentation convention)
  </read_first>
  <action>
Modify `package.json` in three blocks. Use 2-space indentation (current file convention — see line 5 `  "engines": {`).

**Block 1 — Replace the entire `scripts` object** (current `package.json` lines 29-36):

Current (CRA):
```json
"scripts": {
  "start": "react-scripts start --openssl-legacy-provider",
  "build": "react-scripts build --openssl-legacy-provider",
  "postbuild": "node scripts/generate-sitemap.cjs",
  "test": "react-scripts test",
  "eject": "react-scripts eject",
  "build:css": "postcss src/css/tailwind.css -o src/css/main.css"
}
```

Replace with (per RESEARCH.md Step 2):
```json
"scripts": {
  "dev": "vite",
  "start": "vite",
  "build": "vite build",
  "preview": "vite preview",
  "postbuild": "node scripts/generate-sitemap.cjs",
  "test": "vitest run",
  "test:watch": "vitest"
}
```

Notes:
- `start` is preserved as alias for `vite` so muscle memory + README references keep working.
- `dev` is the canonical Vite convention; `start` is a backward-compat alias.
- `eject` is removed — Vite has nothing to eject from.
- `build:css` is removed — Vite runs PostCSS natively from `postcss.config.js`. The orphan `src/css/main.css` file produced by this script is deleted in Plan 04-04.
- `postbuild` is UNCHANGED — `scripts/generate-sitemap.cjs` still writes to `build/sitemap.xml` after `pnpm build`.
- `--openssl-legacy-provider` flag is removed everywhere (Pitfall 7 — Node 22+ no longer accepts it).
- `test` runs `vitest run` (single-pass for CI / local default); `test:watch` runs `vitest` (watch mode). Test files are still using `jest.*` API as of this plan; they fail until Plan 04-03 migrates them — this is intentional.

**Block 2 — Remove dependencies**:

From `dependencies` (lines 9-28):
- Remove `"postcss-cli": "^10.1.0",` (was used by the deleted `build:css` script).
- Remove `"react-scripts": "5.0.1",`.

**Block 3 — Remove devDependency**:

From `devDependencies` (lines 55-62):
- Remove `"@babel/plugin-proposal-private-property-in-object": "^7.21.11",` (was a CRA 5 peer-dep workaround for `babel-preset-react-app`; vestigial once `react-scripts` is gone).

**Block 4 — Add devDependencies** (per RESEARCH.md Step 3):
- Add `"vite": "^7.3.3",`
- Add `"@vitejs/plugin-react": "^5.0.0",`
- Add `"vitest": "^4.1.5",`
- Add `"jsdom": "^25.0.0",`

Maintain alphabetical order in `devDependencies`. Final devDependency block should look like:
```json
"devDependencies": {
  "@tailwindcss/forms": "^0.5.3",
  "@testing-library/dom": "^9.3.0",
  "@vitejs/plugin-react": "^5.0.0",
  "autoprefixer": "^10.4.10",
  "jsdom": "^25.0.0",
  "postcss": "^8.4.16",
  "tailwindcss": "^3.1.8",
  "vite": "^7.3.3",
  "vitest": "^4.1.5"
}
```

Do NOT add `@vitest/coverage-v8` — coverage is out of scope per RESEARCH.md.

**After editing `package.json`, run `pnpm install`** to update `pnpm-lock.yaml`. Commit `pnpm-lock.yaml` as part of this task. Use `pnpm install` (not `pnpm install --frozen-lockfile`) since the lockfile is being intentionally regenerated.

Do NOT delete `@babel/preset-env`, `@babel/preset-react`, or any other Babel package — none are direct deps. They flow through `react-scripts` and disappear with the lockfile regeneration.

Do NOT touch `engines.node`, `packageManager`, `eslintConfig`, or `browserslist` blocks — the `eslintConfig` `"react-app"` extends still works as long as the package is in node_modules; if it's pulled out by the lockfile regen, lint errors are non-blocking and Plan 04-04 can address them if they appear (out of scope for this task).
  </action>
  <verify>
    <automated>! grep -q "react-scripts" package.json &amp;&amp; ! grep -q "openssl-legacy-provider" package.json &amp;&amp; ! grep -q "postcss-cli" package.json &amp;&amp; ! grep -q "@babel/plugin-proposal-private-property-in-object" package.json &amp;&amp; grep -q '"vite": "\^7' package.json &amp;&amp; grep -q '"@vitejs/plugin-react": "\^5' package.json &amp;&amp; grep -q '"vitest": "\^4' package.json &amp;&amp; grep -q '"jsdom": "\^25' package.json &amp;&amp; grep -q '"build": "vite build"' package.json &amp;&amp; grep -q '"test": "vitest run"' package.json &amp;&amp; grep -q '"dev": "vite"' package.json &amp;&amp; pnpm install --silent</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "react-scripts" package.json` returns 0
    - `grep -c "openssl-legacy-provider" package.json` returns 0
    - `grep -c "postcss-cli" package.json` returns 0
    - `grep -c "@babel/plugin-proposal-private-property-in-object" package.json` returns 0
    - `grep -c '"vite":' package.json` returns 1 (line in devDependencies)
    - `grep -c '"@vitejs/plugin-react":' package.json` returns 1
    - `grep -c '"vitest":' package.json` returns 1
    - `grep -c '"jsdom":' package.json` returns 1
    - `grep -c '"build:css"' package.json` returns 0 (script removed)
    - `grep -c '"eject"' package.json` returns 0 (script removed)
    - `grep -c '"build": "vite build"' package.json` returns 1
    - `grep -c '"test": "vitest run"' package.json` returns 1
    - `grep -c '"preview": "vite preview"' package.json` returns 1
    - `pnpm-lock.yaml` is updated (regenerated by `pnpm install`)
    - `node_modules/vite/package.json` exists after install
    - `node_modules/react-scripts` does NOT exist after install
  </acceptance_criteria>
  <done>`package.json` has Vite-only scripts and deps; `react-scripts`, `postcss-cli`, `@babel/plugin-proposal-private-property-in-object`, and `--openssl-legacy-provider` are all gone; `pnpm install` succeeds and regenerates `pnpm-lock.yaml`; `node_modules/vite` is on disk. Tests are temporarily broken (still using `jest.*`); Plan 04-03 fixes them.</done>
</task>

<task type="auto">
  <name>Task 2: Rename REACT_APP_RECAPTCHA_SITE_KEY → VITE_RECAPTCHA_SITE_KEY in src/App.js + update doc comments</name>
  <files>src/App.js, src/components/quote/QuoteSubmitForm.jsx</files>
  <read_first>
    - src/App.js (lines 27-35 — the comment block + the env-var reference)
    - src/components/quote/QuoteSubmitForm.jsx (lines 11-18 — the doc comment that references the env-var name)
    - .planning/phases/04-vite-migration/04-RESEARCH.md (`### Pattern 3: Environment variables` — lines 231-260; `### Pitfall 6: react-google-recaptcha-v3 reCAPTCHA script never loads after migration` — lines 405-409)
    - .planning/phases/04-vite-migration/04-PATTERNS.md (`### src/App.js` — lines 220-261; `### src/components/quote/QuoteSubmitForm.jsx` — lines 264-285)
  </read_first>
  <action>
Two files, three textual changes total. All edits preserve TAB indentation (both files use tabs — verified `src/App.js` lines 27-95).

**Edit 1 — `src/App.js` line 35 (the only runtime `process.env` reference in `src/`):**

BEFORE:
```javascript
	const reCaptchaKey = process.env.REACT_APP_RECAPTCHA_SITE_KEY;
```

AFTER:
```javascript
	const reCaptchaKey = import.meta.env.VITE_RECAPTCHA_SITE_KEY;
```

**Edit 2 — `src/App.js` lines 28-34 (the comment block above the env-var reference; update to reflect the Vite contract; runtime semantics are unchanged but the build-tool name is wrong):**

BEFORE:
```javascript
	// reCAPTCHA v3 site key — public, browser-safe; injected at build time by
	// `react-scripts build` from the REACT_APP_RECAPTCHA_SITE_KEY env var (CRA
	// contract). When unset (e.g. local dev without env vars), the provider
	// mounts but the reCAPTCHA script never loads — `executeRecaptcha` stays
	// undefined and QuoteSubmitForm surfaces a "spam protection isn't loaded
	// yet" inline message instead of throwing. This keeps the build green even
	// when owner-prep (Plan 03-03 Task 1) hasn't been completed yet.
```

AFTER:
```javascript
	// reCAPTCHA v3 site key — public, browser-safe; injected at build time by
	// Vite from the VITE_RECAPTCHA_SITE_KEY env var. When unset (e.g. local
	// dev without env vars), the provider mounts but the reCAPTCHA script
	// never loads — `executeRecaptcha` stays undefined and QuoteSubmitForm
	// surfaces a "spam protection isn't loaded yet" inline message instead of
	// throwing. This keeps the build green even when owner-prep (Plan 03-03
	// Task 1) hasn't been completed yet.
```

Word-wrap the prose at ~70 columns to match the existing comment style.

**Edit 3 — `src/components/quote/QuoteSubmitForm.jsx` line 15 (doc-comment env-var name; comment-only edit):**

BEFORE:
```javascript
//   - When REACT_APP_RECAPTCHA_SITE_KEY is unset (e.g. local dev without env
```

AFTER:
```javascript
//   - When VITE_RECAPTCHA_SITE_KEY is unset (e.g. local dev without env
```

Only line 15 changes. Do not touch lines 14, 16, 17, 18 — they describe behavior, not the env-var name. Do not touch any imports, JSX, or component code.

**Verification after edits:**
- `grep -rn "REACT_APP" src/ scripts/ public/` should return zero hits.
- `grep -rn "process.env" src/` should return zero hits.
- `grep -rn "import.meta.env.VITE_RECAPTCHA_SITE_KEY" src/` should return exactly 1 hit (in `App.js`).
- `grep -rn "VITE_RECAPTCHA_SITE_KEY" src/` should return exactly 2 hits (App.js code + comment, QuoteSubmitForm.jsx comment).

DO NOT add `define: { 'process.env': '{}' }` to `vite.config.js` — the codebase has no other `process.env.*` references and the auto-`process.env.NODE_ENV` substitution Vite ships with is sufficient. If a runtime `process is not defined` error surfaces in Plan 04-05's smoke build, address it then; pre-shimming is Pitfall 4.

DO NOT rename the Netlify Function-side env vars (`RESEND_API_KEY`, `RECAPTCHA_SECRET_KEY` in `netlify/functions/submit-quote/`). They have no `REACT_APP_` prefix; they are server-side; they are out of scope.

DO NOT update `scripts/generate-sitemap.cjs`'s comment that mentions "Phase 4 Vite migration moves these to env vars" — moving Sanity `projectId`/`dataset` to env vars is out of scope for this phase (RESEARCH §Open Questions; Sanity creds are public-knowledge identifiers and the existing hardcoding is accepted in T-02-04-04).
  </action>
  <verify>
    <automated>grep -q "import.meta.env.VITE_RECAPTCHA_SITE_KEY" src/App.js &amp;&amp; ! grep -q "process.env.REACT_APP" src/App.js &amp;&amp; ! grep -q "REACT_APP_RECAPTCHA_SITE_KEY" src/App.js &amp;&amp; ! grep -q "REACT_APP_RECAPTCHA_SITE_KEY" src/components/quote/QuoteSubmitForm.jsx &amp;&amp; grep -q "VITE_RECAPTCHA_SITE_KEY" src/components/quote/QuoteSubmitForm.jsx &amp;&amp; [ "$(grep -rn 'REACT_APP' src/ scripts/ public/ 2>/dev/null | wc -l | tr -d ' ')" = "0" ] &amp;&amp; [ "$(grep -rn 'process.env' src/ 2>/dev/null | wc -l | tr -d ' ')" = "0" ]</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rn "REACT_APP" src/ scripts/ public/` returns 0 hits (zero in source tree, scripts, public)
    - `grep -rn "process.env" src/` returns 0 hits (only `process.env.REACT_APP_RECAPTCHA_SITE_KEY` in App.js was the sole reference)
    - `grep -c "import.meta.env.VITE_RECAPTCHA_SITE_KEY" src/App.js` returns 1
    - `grep -c "VITE_RECAPTCHA_SITE_KEY" src/App.js` returns 2 (one code, one in updated comment)
    - `grep -c "VITE_RECAPTCHA_SITE_KEY" src/components/quote/QuoteSubmitForm.jsx` returns 1 (comment only)
    - `grep -c "react-scripts build" src/App.js` returns 0 (old comment text replaced)
    - The `App.js` comment block uses tab indentation matching surrounding code
    - No imports added or removed in either file
  </acceptance_criteria>
  <done>The single runtime env-var reference is now `import.meta.env.VITE_RECAPTCHA_SITE_KEY`; both doc-comments describe the Vite contract (not the CRA contract); zero `REACT_APP` strings remain anywhere in `src/`, `scripts/`, or `public/`; zero `process.env` references remain in `src/`. Plan 04-06 will rename the matching env var in the Netlify dashboard (owner action).</done>
</task>

<task type="auto">
  <name>Task 3: Run Vite production build and confirm artifacts</name>
  <files>(no files modified — verification only)</files>
  <read_first>
    - vite.config.js (Plan 04-01 output — confirm outDir is 'build')
    - .planning/phases/04-vite-migration/04-RESEARCH.md (`### Pitfall 1: JSX in .js files crashes the build` — lines 374-379; `### Pitfall 5: ESM/CJS interop on @sanity/client v6` — lines 399-403)
    - scripts/generate-sitemap.cjs (writes `build/sitemap.xml` post-build)
  </read_first>
  <action>
Run `pnpm build` from the repo root. This executes:
1. `vite build` (uses `vite.config.js` — JSX loader override, outDir='build').
2. `node scripts/generate-sitemap.cjs` (npm `postbuild` lifecycle hook — writes `build/sitemap.xml`).

Expected output:
- A `build/` directory exists (gitignored — that's fine).
- `build/index.html` is present and references hashed asset paths like `/assets/index-<hash>.js`.
- `build/assets/` contains at least one JS chunk (the entry module) and one CSS file (Tailwind output).
- `build/sitemap.xml` is present and contains 7+ `<url>` entries (per Plan 02-04 baseline: 7 static routes + any Sanity slugs).
- `build/favicon.png`, `build/manifest.json`, `build/og-default.png`, `build/robots.txt`, `build/_redirects` are all present (copied from `public/`).
- The two Netlify form blocks appear verbatim in `build/index.html` — verify with `grep -c 'name="contact-form"' build/index.html` returns 1 and `grep -c 'name="shop-notify"' build/index.html` returns 1.

If the build fails:
- "Transform failed with 1 error" + a `<` token reference → JSX-in-`.js` config issue. Re-verify `esbuild.loader: 'jsx'` and `optimizeDeps.esbuildOptions.loader: { '.js': 'jsx' }` are both present in `vite.config.js`. (Pitfall 1.)
- "Cannot find module '@sanity/client'" or `'X' is not exported by '@sanity/client'` → add `optimizeDeps.include: ['@sanity/client']` to `vite.config.js`. (Pitfall 5.) Note this as a deviation in the SUMMARY.
- `process is not defined` at module load → add `define: { 'process.env': '{}' }` to `vite.config.js`. (Pitfall 4.) Note as a deviation.
- `Sitemap generation failed:` + error → the postbuild script crashed, but the build itself succeeded. The sitemap is non-blocking for this task; document it but proceed. Plan 04-04 cleanup or Plan 04-05 verify can revisit.

DO NOT run `pnpm test` in this task — tests are still on `jest.*` API and will fail; Plan 04-03 owns the test migration.

DO NOT run `pnpm dev` — local dev-server smoke is fine but not a deliverable. The deploy preview in Plan 04-06 is the final verification.

After a successful build, leave `build/` on disk for visual inspection by the executor. Do not commit `build/` (it's gitignored).
  </action>
  <verify>
    <automated>pnpm build &amp;&amp; test -f build/index.html &amp;&amp; test -d build/assets &amp;&amp; ls build/assets/ | grep -q "\.js$" &amp;&amp; grep -q 'name="contact-form"' build/index.html &amp;&amp; grep -q 'name="shop-notify"' build/index.html &amp;&amp; grep -q '<script type="module"' build/index.html &amp;&amp; ! grep -q "%PUBLIC_URL%" build/index.html</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm build` exits 0
    - `build/index.html` exists
    - `build/assets/` contains at least one `.js` and one `.css` file
    - `grep -c 'name="contact-form"' build/index.html` returns 1
    - `grep -c 'name="shop-notify"' build/index.html` returns 1
    - `grep -c 'name="bot-field"' build/index.html` returns 2
    - `grep -c "%PUBLIC_URL%" build/index.html` returns 0 (Vite stripped or never substituted)
    - `grep -c '<script type="module"' build/index.html` returns at least 1
    - `build/sitemap.xml` exists and contains 7+ `<url>` entries (postbuild ran)
    - `build/_redirects`, `build/favicon.png`, `build/manifest.json`, `build/og-default.png`, `build/robots.txt` all exist (copied from public/)
  </acceptance_criteria>
  <done>`pnpm build` produces a deployable bundle in `build/` via Vite. The build succeeds, the entry HTML references hashed asset paths, both Netlify forms are byte-preserved in the output, and the postbuild sitemap script ran. The migration is functional; Plan 04-03 will restore green tests, Plan 04-04 cleans up orphan files, Plan 04-05 formalizes Netlify config, Plan 04-06 ships the deploy preview.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → Netlify Function | reCAPTCHA token mint depends on `import.meta.env.VITE_RECAPTCHA_SITE_KEY` being inlined at build time. Wrong env var name = silent script-not-loaded failure. |
| Build-time supply chain | New devDeps (`vite@^7`, `@vitejs/plugin-react@^5`, `vitest@^4`, `jsdom@^25`) enter the install graph. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-02-01 | Tampering | Build supply chain (new Vite-ecosystem deps) | mitigate | Version-pinned via `^N` in `package.json`; integrity enforced by `pnpm-lock.yaml` (committed). All four packages are first-party Vite-team or sister-package (`@vitejs/plugin-react`, `vitest`, `jsdom`) — no novel/unknown publishers. |
| T-04-02-02 | Information Disclosure | `import.meta.env.VITE_RECAPTCHA_SITE_KEY` inlined into client bundle | accept | The reCAPTCHA v3 site key is by design public-facing and browser-safe (Google docs explicitly state this). It's identical in nature to the previous CRA `REACT_APP_*` reference. No new exposure. |
| T-04-02-03 | DoS | Vite build process | accept | If build fails, deploy aborts — no production impact. Local dev is the only failure surface, and Pitfall 1/4/5 fallbacks (esbuild loader override, define shim, optimizeDeps.include) cover the known modes. |
| T-04-02-04 | Tampering (silent) | Netlify dashboard env var still named `REACT_APP_RECAPTCHA_SITE_KEY` after this plan | mitigate | Plan 04-06 owns the dashboard rename. Plan 04-05's local build succeeds with or without the env var (Pitfall 6: graceful degrade — `executeRecaptcha` undefined → inline "spam protection isn't loaded yet" message). The deploy preview in Plan 04-06 is where the dashboard rename gets verified end-to-end. |
</threat_model>

<verification>
- `package.json` has zero `react-scripts`, `postcss-cli`, `--openssl-legacy-provider`, `@babel/plugin-proposal-private-property-in-object` references
- `package.json` has `vite@^7.3.3`, `@vitejs/plugin-react@^5.0.0`, `vitest@^4.1.5`, `jsdom@^25.0.0` in devDependencies
- `pnpm-lock.yaml` is regenerated and committed
- `src/App.js` line 35 uses `import.meta.env.VITE_RECAPTCHA_SITE_KEY`
- Comments in `App.js` and `QuoteSubmitForm.jsx` reference Vite (not CRA) and `VITE_*` (not `REACT_APP_*`)
- `pnpm build` exits 0 and produces `build/index.html` + `build/assets/*.js`
- Both Netlify forms appear byte-preserved in `build/index.html`
</verification>

<success_criteria>
- The build tool is now Vite (not react-scripts) — VITE-01
- All `REACT_APP_*` env-var references are renamed to `VITE_*` in source — VITE-02
- `--openssl-legacy-provider` is no longer needed anywhere — VITE-02 success criterion 2
- `pnpm build` produces a working bundle via Vite — VITE-01 success criterion 1
</success_criteria>

<output>
After completion, create `.planning/phases/04-vite-migration/04-02-SUMMARY.md` documenting:
- Final `package.json` scripts + deps state (with deltas from Plan 04-01).
- The exact diff applied to `src/App.js` lines 28-35 + `QuoteSubmitForm.jsx` line 15.
- `pnpm build` output (size, chunk count, postbuild status).
- Any deviations (e.g., if `optimizeDeps.include: ['@sanity/client']` had to be added — Pitfall 5).
</output>
