---
phase: 04-vite-migration
plan: 04
type: execute
wave: 4
depends_on:
  - "04-03"
files_modified:
  - src/css/main.css
autonomous: true
requirements:
  - VITE-01
must_haves:
  truths:
    - "`src/css/main.css` no longer exists in the repo"
    - "No file in the repo imports `main.css` (verified before deletion)"
    - "`pnpm build` continues to exit 0 after the deletion (CSS pipeline still works via `src/index.css` + Tailwind)"
    - "`pnpm test` continues to exit 0 after the deletion"
    - "Zero `--openssl-legacy-provider` references remain anywhere in the repo"
  artifacts:
    - path: "(none — this plan is purely deletion + verification)"
      provides: "n/a"
  key_links:
    - from: "Tailwind v3 directives in `src/index.css`"
      to: "Vite PostCSS pipeline (auto-detected from `postcss.config.js`)"
      via: "src/index.js imports './index.css'"
      pattern: "@tailwind"
---

<objective>
Delete the orphan `src/css/main.css` file (a stale generated PostCSS output that nothing imports) and verify the cleanup is complete: no `main.css` consumers, no `--openssl-legacy-provider` references anywhere, build + tests still green. The dead deps (`postcss-cli`, `react-scripts`, `@babel/plugin-proposal-private-property-in-object`) and dead scripts (`eject`, `build:css`) were already removed in Plan 04-02 — this plan closes the loose ends.

Purpose: RESEARCH.md `## Runtime State Inventory` flagged `src/css/main.css` as an orphan generated artifact. The file was produced by the now-deleted `build:css` npm script (`postcss src/css/tailwind.css -o src/css/main.css`). RESEARCH.md verified `grep -rn "main.css" src/` returns no consumers. Leaving the file in the repo is dead weight that future contributors will trip over wondering "is this used?" The cleanup is part of the "no user-visible change" contract: the runtime CSS path is `src/index.js → src/index.css → @tailwind directives → PostCSS via Vite`; this stale file is parallel and unused.

Output: One file deleted (`src/css/main.css`). Zero deps changes (Plan 04-02 already removed `postcss-cli`). One verification sweep that `pnpm build` and `pnpm test` continue to pass.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/04-vite-migration/04-RESEARCH.md
@.planning/phases/04-vite-migration/04-PATTERNS.md
@.planning/phases/04-vite-migration/04-02-SUMMARY.md
@.planning/phases/04-vite-migration/04-03-SUMMARY.md
@./CLAUDE.md
@src/index.css
@src/css/App.css
@src/css/tailwind.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify no consumers, then delete src/css/main.css and run final smoke checks</name>
  <files>src/css/main.css</files>
  <read_first>
    - src/css/main.css (read first only to confirm it's the generated PostCSS output, not a hand-edited file the planner missed; it should look like compiled Tailwind v3 utility output — many short class definitions)
    - src/index.css (the actual canonical entry CSS — has `@tailwind` directives)
    - src/css/App.css (loaded from `src/App.js` — has `@font-face` rules and likely some component styles; UNRELATED to main.css)
    - src/css/tailwind.css (the source the deleted `build:css` script consumed; UNUSED at runtime — `index.css` is the runtime entry; tailwind.css is harmless residue but RESEARCH.md does NOT list it for deletion, so leave it)
    - .planning/phases/04-vite-migration/04-RESEARCH.md (`### Pattern 4: Tailwind v3 + PostCSS — drop-in compatibility` — lines 264-274; `## Runtime State Inventory` build artifacts row — line 367)
  </read_first>
  <action>
**Step 1 — Pre-deletion verification.** Before deleting anything, prove the file has zero consumers:

```bash
grep -rn "main\.css" src/ public/ scripts/ | grep -v "src/css/main\.css:"
```

The only acceptable result is zero hits OUTSIDE the file itself. If ANY hit appears (e.g., `src/index.js: import './css/main.css'`), STOP and surface as a deviation — RESEARCH.md's verification was wrong and the cleanup must be re-scoped.

Also verify no Vite/Tailwind/PostCSS config references it:
```bash
grep -rn "main\.css" vite.config.js postcss.config.js tailwind.config.js
```
Expected: zero hits.

**Step 2 — Delete the file.**
```bash
git rm src/css/main.css
```

Use `git rm` (not plain `rm`) so git tracks the deletion in the commit.

**Step 3 — Final cleanup sweep.** Verify all CRA-residue items are gone (most were handled by Plan 04-02; this is a sanity check):
```bash
grep -rn "openssl-legacy-provider" .  --exclude-dir=node_modules --exclude-dir=.git
grep -rn "react-scripts"          .  --exclude-dir=node_modules --exclude-dir=.git --exclude-dir=.planning
grep -rn "postcss-cli"             .  --exclude-dir=node_modules --exclude-dir=.git --exclude-dir=.planning
grep -rn "@babel/plugin-proposal-private-property-in-object" .  --exclude-dir=node_modules --exclude-dir=.git --exclude-dir=.planning
grep -rn "REACT_APP"                .  --exclude-dir=node_modules --exclude-dir=.git --exclude-dir=.planning
grep -rn "process\.env"             src/
```

ALL six greps must return zero hits. The `.planning/` directory is excluded because RESEARCH.md and SUMMARYs legitimately reference these names as historical context. The `.git/` directory is excluded for obvious reasons. `node_modules/` is excluded because third-party deps may still reference these names internally and that's not our concern.

If any of the six greps return hits in the source tree (`src/`, `scripts/`, `public/`, repo root configs), surface as a deviation — Plan 04-02 missed something.

**Step 4 — Smoke check the build + tests.**
```bash
pnpm build
pnpm test
```

Both must exit 0. The `pnpm build` should produce `build/index.html` + `build/assets/*` exactly as Plan 04-02 Task 3 did. The `pnpm test` should pass exactly as Plan 04-03 Task 1 did. If either regresses, the deletion broke something — restore `src/css/main.css` and surface as a deviation.

DO NOT delete `src/css/tailwind.css`. Even though the `build:css` script that consumed it is gone, the file is referenced indirectly by RESEARCH.md as harmless residue, and removing it is a separate decision (it would be obvious to remove if a future grep showed it was unused; for now, leave it). RESEARCH.md `## Runtime State Inventory` only lists `src/css/main.css` for deletion.

DO NOT delete `src/css/App.css` — it's actively imported by `src/App.js` line 9 (`import './css/App.css';`) and contains the `@font-face` rules.

DO NOT delete `src/index.css` — it's the canonical Tailwind entry point loaded from `src/index.js` line 3.

DO NOT modify `vite.config.js`, `postcss.config.js`, or `tailwind.config.js` in this task. The `@tailwindcss/forms` plugin bug in `tailwind.config.js` line 61 is documented in RESEARCH.md (Pattern 4 / Open Question 3) as a pre-existing latent bug that is OUT OF SCOPE for Phase 4 — fixing it would change visual output and violate the "no user-visible change" criterion. Note this in the SUMMARY as a deferred follow-up issue for a future phase or v2 cleanup.
  </action>
  <verify>
    <automated>! test -f src/css/main.css &amp;&amp; [ "$(grep -rn 'main\.css' src/ public/ scripts/ vite.config.js postcss.config.js tailwind.config.js 2>/dev/null | wc -l | tr -d ' ')" = "0" ] &amp;&amp; [ "$(grep -rn 'openssl-legacy-provider' . --exclude-dir=node_modules --exclude-dir=.git --exclude-dir=.planning 2>/dev/null | wc -l | tr -d ' ')" = "0" ] &amp;&amp; [ "$(grep -rn 'react-scripts' . --exclude-dir=node_modules --exclude-dir=.git --exclude-dir=.planning 2>/dev/null | wc -l | tr -d ' ')" = "0" ] &amp;&amp; pnpm build &amp;&amp; pnpm test</automated>
  </verify>
  <acceptance_criteria>
    - `src/css/main.css` does NOT exist
    - `git status --porcelain | grep -E "^D " | grep -E "src/css/main\.css"` confirms git tracked the deletion
    - `grep -rn "main\.css" src/ public/ scripts/ vite.config.js postcss.config.js tailwind.config.js` returns 0 hits
    - `grep -rn "openssl-legacy-provider" . --exclude-dir=node_modules --exclude-dir=.git --exclude-dir=.planning` returns 0 hits
    - `grep -rn "react-scripts" . --exclude-dir=node_modules --exclude-dir=.git --exclude-dir=.planning` returns 0 hits
    - `grep -rn "postcss-cli" . --exclude-dir=node_modules --exclude-dir=.git --exclude-dir=.planning` returns 0 hits
    - `grep -rn "@babel/plugin-proposal-private-property-in-object" . --exclude-dir=node_modules --exclude-dir=.git --exclude-dir=.planning` returns 0 hits
    - `grep -rn "REACT_APP" . --exclude-dir=node_modules --exclude-dir=.git --exclude-dir=.planning` returns 0 hits
    - `grep -rn "process\.env" src/` returns 0 hits
    - `pnpm build` exits 0 and produces `build/index.html`
    - `pnpm test` exits 0
    - `src/css/App.css` and `src/index.css` are unchanged (still exist; not modified)
    - `tailwind.config.js` is unchanged (the `@tailwindcss/forms` plugin bug is deferred per RESEARCH.md)
  </acceptance_criteria>
  <done>The orphan `src/css/main.css` is gone, all six CRA-residue greps return zero hits in source, `pnpm build` + `pnpm test` both still pass. The CRA → Vite migration's source-tree cleanup is complete; only `netlify.toml` (Plan 04-05) and the Netlify dashboard env var rename + deploy-preview verification (Plan 04-06) remain.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Build pipeline → static output | Deleting an unused file shouldn't affect output, but a hidden import would. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-04-01 | Tampering (silent regression) | Deletion of `src/css/main.css` | mitigate | Pre-deletion grep verifies zero consumers; post-deletion `pnpm build` + `pnpm test` confirm no functional regression. |
| T-04-04-02 | Information Disclosure | n/a | accept | No secrets in any deleted/touched file. |
</threat_model>

<verification>
- `src/css/main.css` deleted via `git rm`
- All six CRA-residue greps return zero hits in source tree
- `pnpm build` and `pnpm test` both exit 0
- `src/css/App.css`, `src/index.css`, `src/css/tailwind.css`, `tailwind.config.js`, `postcss.config.js`, `vite.config.js` all unchanged
</verification>

<success_criteria>
- Source tree cleanup complete — zero CRA residue
- Maps to VITE-01: full migration includes removing CRA-shaped scaffolding the new toolchain doesn't need
</success_criteria>

<output>
After completion, create `.planning/phases/04-vite-migration/04-04-SUMMARY.md` documenting:
- The deletion of `src/css/main.css` (size, last-modified date for context).
- The six CRA-residue greps and their zero-hit results (acts as a sentinel for future maintainers).
- Confirmation that `pnpm build` + `pnpm test` continue to pass.
- A "deferred follow-up" note for the `@tailwindcss/forms` plugin bug (`tailwind.config.js` line 61, plugin name as string instead of `require(...)`) — out of scope for Phase 4 but worth tracking for a future cleanup.
</output>
