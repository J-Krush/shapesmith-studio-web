---
phase: 03-auto-pricing-quote-tool
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - pnpm-lock.yaml
  - src/App.js
  - src/pages/Quote.jsx
  - src/components/quote/QuoteTabs.jsx
  - src/components/quote/FileDropzone.jsx
  - src/components/quote/GeometrySummary.jsx
  - src/utilities/quote/parseStl.js
  - src/utilities/quote/parseObj.js
  - src/utilities/quote/parseSvg.js
  - src/utilities/quote/formatErrors.js
  - src/components/shared/AppHeader.jsx
  - src/pages/Projects.jsx
  - src/pages/ProjectSingle.jsx
  - scripts/generate-sitemap.cjs
autonomous: true
requirements:
  - QTE-01
  - QTE-02
  - QTE-03
  - QTE-09  # Partial — memory-bounded parsing via 25MB cap (D-12); SVG sanitation deferred to Plan 03-03 per D-22
tags:
  - frontend
  - file-upload
  - parser
  - tabs
  - lazy-route
  - three.js

must_haves:
  truths:
    - "A visitor can navigate to /quote from the AppHeader nav and from each service page hero"
    - "A visitor can see two tabs labeled '3D Printing' and 'Laser Cutting' on /quote"
    - "Initial active tab is set from ?service= or document.referrer (mirrors ContactForm pre-fill)"
    - "A visitor can drop or pick an STL/OBJ on the 3D tab and an SVG on the laser tab"
    - "Files >25MB are rejected client-side with a friendly message (memory-bounded — QTE-09 partial)"
    - "Wrong-format files are rejected with a friendly message that names the expected formats"
    - "After a valid 3D file lands, volume (cm³ + in³ secondary), bbox (W×D×H mm), triangle count, and file size render in a definition-list panel"
    - "After a valid SVG lands, bbox (W×H mm), total cut length (mm), and file size render in a definition-list panel"
    - "An SVG whose units cannot be inferred surfaces a 'reading as mm' footnote inside the geometry panel"
    - "A unit-cube STL (10×10×10 mm) parses to volume = 1.00 cm³ ± 0.01"
    - "/quote appears in build/sitemap.xml after pnpm build"
    - "The /quote chunk is lazy-loaded (does NOT inflate the main bundle); chunk size is measured and recorded"
    - "The Plan 03-01 placeholder submit affordance is a disabled CTA — visitors do not hit a hung submit"
  artifacts:
    - path: "src/pages/Quote.jsx"
      provides: "Lazy-loaded /quote page shell with SEOHead + tabs"
      min_lines: 30
    - path: "src/components/quote/QuoteTabs.jsx"
      provides: "WAI-ARIA tabs with ?service=/referrer pre-fill cascade and framer-motion crossfade"
      min_lines: 80
    - path: "src/components/quote/FileDropzone.jsx"
      provides: "Native HTML5 drag-and-drop + file picker with format and size validation"
      min_lines: 70
    - path: "src/components/quote/GeometrySummary.jsx"
      provides: "Definition-list readout panel rendering 3D OR laser metrics"
      min_lines: 50
    - path: "src/utilities/quote/parseStl.js"
      provides: "STL parser with signed-tetrahedra volume + bbox"
      min_lines: 40
    - path: "src/utilities/quote/parseObj.js"
      provides: "OBJ parser sharing volume math with parseStl"
      min_lines: 30
    - path: "src/utilities/quote/parseSvg.js"
      provides: "SVG parser with offscreen-host getTotalLength + bbox + unit detection"
      min_lines: 40
  key_links:
    - from: "src/App.js"
      to: "src/pages/Quote.jsx"
      via: "React.lazy(() => import('./pages/Quote'))"
      pattern: "lazy\\(\\(\\) => import\\('\\./pages/Quote'\\)\\)"
    - from: "src/pages/Quote.jsx"
      to: "src/components/quote/QuoteTabs.jsx"
      via: "JSX import"
      pattern: "import QuoteTabs from"
    - from: "src/components/quote/QuoteTabs.jsx"
      to: "src/components/quote/FileDropzone.jsx"
      via: "JSX import inside active panel"
      pattern: "import FileDropzone from"
    - from: "src/components/quote/QuoteTabs.jsx"
      to: "src/utilities/quote/parseStl.js"
      via: "dynamic import after file accepted"
      pattern: "import\\('\\.\\./\\.\\./utilities/quote/parseStl'\\)|from '../../utilities/quote/parseStl'"
    - from: "src/components/shared/AppHeader.jsx"
      to: "/quote"
      via: "NAV_ITEMS entry"
      pattern: "to: '/quote'"
    - from: "scripts/generate-sitemap.cjs"
      to: "/quote"
      via: "STATIC_ROUTES array"
      pattern: "STATIC_ROUTES.*'/quote'"
---

<objective>
Ship the auto-pricing quote tool stub: a `/quote` route with two in-page tabs (3D / Laser), client-side file upload (drag-and-drop + picker), browser-side parsing of STL/OBJ (3D) and SVG (laser), and definition-list geometry readouts. NO submission, NO pricing math, NO Sanity schema — those land in Plans 03-02 / 03-03 / 03-04.

Purpose: Honor CONTEXT.md D-01 plan staging — the stub gives visitors a useful preview surface without blocking on pricing-rule schema, Resend setup, or reCAPTCHA registration. Each plan leaves the site deployable (D-02). The placeholder submit CTA is a disabled button, NOT a hung form.
Output: A lazy-loaded `/quote` chunk reachable from the nav and from each service page hero; STL/OBJ/SVG parsers with verified math (1cm³ unit-cube test); WAI-ARIA tabs that mirror the ContactForm pre-fill cascade; sitemap entry; a recorded `/quote` chunk size in the plan summary.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-auto-pricing-quote-tool/03-CONTEXT.md
@.planning/phases/03-auto-pricing-quote-tool/03-RESEARCH.md
@.planning/phases/03-auto-pricing-quote-tool/03-PATTERNS.md
@.planning/phases/03-auto-pricing-quote-tool/03-UI-SPEC.md
@.planning/codebase/CONVENTIONS.md
@.planning/codebase/STACK.md
@.planning/codebase/ARCHITECTURE.md
@src/App.js
@src/data/services.js
@src/components/contact/ContactForm.jsx
@src/components/shared/AppHeader.jsx
@src/components/shared/SEOHead.jsx
@src/components/services/MaterialsSection.jsx
@src/components/services/FAQ.jsx
@src/components/reusable/FormInput.jsx
@src/pages/Contact.jsx
@src/pages/Projects.jsx
@src/pages/ProjectSingle.jsx
@scripts/generate-sitemap.cjs
@public/index.html

<interfaces>
<!-- Key contracts the executor uses. Extracted from existing codebase. -->

From src/data/services.js:
```js
export const SERVICES = [
  { key: 'laser', urlSegment: 'styles', navLabel: 'styles', sanityType: 'laser-style', contactSubject: 'Laser cutting' },
  { key: 'print', urlSegment: '3d-printing', navLabel: '3D Printing', sanityType: 'print-style', contactSubject: '3D printing' },
];
```

From src/components/contact/ContactForm.jsx (D-24 pre-fill cascade — mirror verbatim):
```js
// Order: ?service= → document.referrer match → default
const SERVICE_PREFILL_RULES = SERVICES.map((s) => ({ match: `/${s.urlSegment}`, ... }));
useEffect(() => {
  const queryService = new URLSearchParams(window.location.search).get('service');
  if (queryService) {
    const m = SERVICES.find((s) => s.key === queryService || s.urlSegment === queryService);
    if (m) { setActive(...); return; }
  }
  const referrer = (typeof document !== 'undefined' && document.referrer) || '';
  const rule = SERVICE_PREFILL_RULES.find((r) => referrer.includes(r.match));
  if (rule) setActive(...);
}, []);
```

From src/components/shared/SEOHead.jsx:
```jsx
<SEOHead
  title="Get a Quote"
  description="Upload your STL, OBJ, or SVG and get an instant ballpark estimate."
  ogUrl="https://shapesmith.studio/quote"
  ogImage={{ url: '/og-default.png', altText: 'Shapesmith Studio quote tool' }}
/>
```

From src/components/shared/AppHeader.jsx (NAV_ITEMS — append entry):
```js
const NAV_ITEMS = [
  { to: '/', label: 'Home', match: '/' },
  ...SERVICES.map((s) => ({ to: `/${s.urlSegment}`, label: s.navLabel === 'styles' ? 'Laser Cutting' : s.navLabel, match: `/${s.urlSegment}` })),
  { to: '/about', label: 'About', match: '/about' },
  { to: '/shop', label: 'Shop', match: '/shop' },
  { to: '/quote', label: 'Get a Quote', match: '/quote' },  // ADD per UI-SPEC §Top-level CTA placement
];
```

Three.js loader subpath imports (Anti-pattern: NEVER `import * as THREE from 'three'`):
```js
import { STLLoader } from 'three/examples/jsm/loaders/STLLoader.js';
import { OBJLoader } from 'three/examples/jsm/loaders/OBJLoader.js';
```

Parser output contracts (consumed by GeometrySummary.jsx):
```ts
// 3D (STL + OBJ)
{ mode: '3d', triangleCount: number, volumeCm3: number, volumeIn3: number,
  bbox: { w: number, d: number, h: number }, fileSizeMB: number }

// Laser (SVG)
{ mode: 'laser', bbox: { w: number, h: number }, pathLengthMm: number,
  unitWarning: boolean, fileSizeMB: number }
```

Parser error contract:
```js
// On bad input, parsers throw `new Error('PARSE_FAILED')` so dropzone can render UI-SPEC's malformed-file copy.
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add three dependency, scaffold parsers + dropzone helpers (no JSX yet)</name>
  <files>package.json, pnpm-lock.yaml, src/utilities/quote/parseStl.js, src/utilities/quote/parseObj.js, src/utilities/quote/parseSvg.js, src/utilities/quote/formatErrors.js</files>
  <read_first>
    - package.json (current dependency list — must read before pnpm add to avoid drift)
    - .planning/phases/03-auto-pricing-quote-tool/03-RESEARCH.md (lines 850-1020 §Code Examples for STL/OBJ/SVG parsers, line 940 PX_PER_MM constant, lines 765-820 Pitfalls 1-5 — winding flip Math.abs, watertight assumption, SVG unit handling, SVG injection isolation, dragover preventDefault)
    - .planning/phases/03-auto-pricing-quote-tool/03-PATTERNS.md (lines 325-435 parseStl/Obj/Svg analog notes; "first parsers in repo" guidance)
    - .planning/phases/03-auto-pricing-quote-tool/03-UI-SPEC.md ("File-rejection error copy" table — exact strings for WRONG_FORMAT / TOO_LARGE / MALFORMED_STL / MALFORMED_OBJ / MALFORMED_SVG / generic; "Geometry summary" labels)
    - .planning/codebase/CONVENTIONS.md (.js for plain modules, single-quote JS, no console.log)
    - src/utilities/encodeFormData.jsx (module-shape analog — small pure named export)
  </read_first>
  <behavior>
    - `parseStl(arrayBuffer, fileSizeBytes)` returns `{ mode: '3d', triangleCount, volumeCm3, volumeIn3, bbox: { w, d, h }, fileSizeMB }`.
    - A 1cm³ unit-cube STL (10×10×10 mm) returns `volumeCm3 === 1.00` (±0.01) and `triangleCount === 12`.
    - Reversed-winding meshes still return positive volume (`Math.abs(signedVolumeMm3)`).
    - On malformed input, parser throws `new Error('PARSE_FAILED')`.
    - `parseObj(text, fileSizeBytes)` returns the same `mode: '3d'` shape using OBJLoader, sharing the same triangle-loop volume math.
    - `parseSvg(text, fileSizeBytes)` returns `{ mode: 'laser', bbox: { w, h }, pathLengthMm, unitWarning, fileSizeMB }`.
    - `parseSvg` uses `new DOMParser().parseFromString(text, 'image/svg+xml')`; throws `new Error('NOT_SVG')` if the root element is not `<svg>`.
    - `parseSvg` appends a clone to an offscreen host (`position:absolute;left:-99999px;top:-99999px;width:1px;height:1px;overflow:hidden`) for `getTotalLength`/`getBBox`, then removes it in `finally`. NEVER appends to a visible container (D-22 + Pitfall 5).
    - `parseSvg` reads `viewBox` and `width` attrs; if width has unit `mm/cm/in/pt/px` it normalizes to mm via `PX_PER_MM = 96 / 25.4`; if `width` and `viewBox` cannot be reconciled it sets `unitWarning: true`.
    - `parseSvg` sums `getTotalLength()` for `<path>` elements only (D-16: implicit shapes deferred — see action notes for the optional one-line extension).
    - `formatErrors(err)` returns the exact UI-SPEC strings keyed on `err.code`: `WRONG_FORMAT_3D`, `WRONG_FORMAT_LASER`, `TOO_LARGE`, `MALFORMED_STL`, `MALFORMED_OBJ`, `MALFORMED_SVG`, `GENERIC`. Each receives a small interpolation map (e.g. `{ ext, sizeMB }`).
  </behavior>
  <action>
    1. Run `pnpm add three@^0.184.0` — adds three to root `package.json` dependencies. Do NOT install `react-dropzone`, `parse-stl`, `obj-file-parser`, or `dompurify` per RESEARCH.md §Standard Stack recommendation. Do NOT install `@netlify/functions` (deferred to Plan 03-03 if needed).

    2. Create `src/utilities/quote/parseStl.js` — plain `.js`, no JSX, single-quote strings, tab indentation, no `console.log`. Subpath import only:
       ```js
       import { STLLoader } from 'three/examples/jsm/loaders/STLLoader.js';
       const loader = new STLLoader();
       export const parseStl = (arrayBuffer, fileSizeBytes) => {
         let geometry;
         try { geometry = loader.parse(arrayBuffer); }
         catch { throw new Error('PARSE_FAILED'); }
         const positions = geometry.attributes.position.array;
         if (!positions || positions.length === 0) throw new Error('PARSE_FAILED');
         const triangleCount = positions.length / 9;
         let signedVolumeMm3 = 0;
         let minX = Infinity, minY = Infinity, minZ = Infinity;
         let maxX = -Infinity, maxY = -Infinity, maxZ = -Infinity;
         for (let i = 0; i < positions.length; i += 9) {
           const v0x = positions[i],     v0y = positions[i+1], v0z = positions[i+2];
           const v1x = positions[i+3],   v1y = positions[i+4], v1z = positions[i+5];
           const v2x = positions[i+6],   v2y = positions[i+7], v2z = positions[i+8];
           const cx = v1y * v2z - v1z * v2y;
           const cy = v1z * v2x - v1x * v2z;
           const cz = v1x * v2y - v1y * v2x;
           signedVolumeMm3 += (v0x * cx + v0y * cy + v0z * cz) / 6;
           minX = Math.min(minX, v0x, v1x, v2x); maxX = Math.max(maxX, v0x, v1x, v2x);
           minY = Math.min(minY, v0y, v1y, v2y); maxY = Math.max(maxY, v0y, v1y, v2y);
           minZ = Math.min(minZ, v0z, v1z, v2z); maxZ = Math.max(maxZ, v0z, v1z, v2z);
         }
         const volumeMm3 = Math.abs(signedVolumeMm3); // Pitfall 2 winding-flip safe
         const volumeCm3 = volumeMm3 / 1000;
         return {
           mode: '3d',
           triangleCount,
           volumeCm3: Number(volumeCm3.toFixed(2)),
           volumeIn3: Number((volumeCm3 / 16.387).toFixed(2)),
           bbox: {
             w: Number((maxX - minX).toFixed(1)),
             d: Number((maxY - minY).toFixed(1)),
             h: Number((maxZ - minZ).toFixed(1)),
           },
           fileSizeMB: Number((fileSizeBytes / 1024 / 1024).toFixed(2)),
         };
       };
       ```

    3. Create `src/utilities/quote/parseObj.js` — same shape, using OBJLoader. Traverse the parsed `THREE.Group`, collect every mesh's position attribute into a flat positions array, then run the same triangle loop:
       ```js
       import { OBJLoader } from 'three/examples/jsm/loaders/OBJLoader.js';
       const loader = new OBJLoader();
       export const parseObj = (text, fileSizeBytes) => {
         let group;
         try { group = loader.parse(text); }
         catch { throw new Error('PARSE_FAILED'); }
         const positions = [];
         group.traverse((obj) => {
           if (obj.isMesh && obj.geometry?.attributes?.position) {
             const p = obj.geometry.attributes.position.array;
             for (let i = 0; i < p.length; i++) positions.push(p[i]);
           }
         });
         if (positions.length === 0) throw new Error('PARSE_FAILED');
         // ...identical signed-tetrahedra + bbox loop as parseStl, applied to `positions`...
         // (extract a shared helper function `volumeAndBbox(positions)` returning { triangleCount, volumeCm3, volumeIn3, bbox } if it reduces duplication; keep it inline if simpler)
         return { mode: '3d', triangleCount, volumeCm3, volumeIn3, bbox, fileSizeMB: Number((fileSizeBytes / 1024 / 1024).toFixed(2)) };
       };
       ```

    4. Create `src/utilities/quote/parseSvg.js` — DOMParser + offscreen-host trick. Import nothing. Implement `parseUnit` for `mm/cm/in/pt/px/`(empty) → mm:
       ```js
       const PX_PER_MM = 96 / 25.4; // SVG default DPI per CSS spec — Pitfall 4
       const parseUnit = (val) => {
         if (!val) return null;
         const m = val.match(/^([\d.]+)\s*(mm|cm|in|pt|px|)?$/);
         if (!m) return null;
         const n = parseFloat(m[1]);
         switch (m[2] || '') {
           case 'mm': return n;
           case 'cm': return n * 10;
           case 'in': return n * 25.4;
           case 'pt': return n * 25.4 / 72;
           case 'px':
           case '':  return n / PX_PER_MM;
           default:  return null;
         }
       };
       export const parseSvg = (text, fileSizeBytes) => {
         const doc = new DOMParser().parseFromString(text, 'image/svg+xml');
         const svg = doc.documentElement;
         if (!svg || svg.tagName !== 'svg' || doc.querySelector('parsererror')) {
           throw new Error('NOT_SVG');
         }
         const vb = svg.getAttribute('viewBox')?.split(/\s+|,/).map(Number);
         const widthMm = parseUnit(svg.getAttribute('width'));
         let scale = 1;
         let unitWarning = false;
         if (vb && widthMm && vb[2] > 0) scale = widthMm / vb[2];
         else if (!widthMm && !vb) unitWarning = true;
         const host = document.createElement('div');
         host.style.cssText = 'position:absolute;left:-99999px;top:-99999px;width:1px;height:1px;overflow:hidden';
         document.body.appendChild(host);
         let pathLengthMm = 0;
         let bbox = { w: 0, h: 0 };
         try {
           host.appendChild(svg.cloneNode(true));
           const liveSvg = host.querySelector('svg');
           liveSvg.querySelectorAll('path').forEach((p) => {
             pathLengthMm += p.getTotalLength() * scale;
           });
           // OPTIONAL D-16: include implicit shapes — uncomment one line if planner accepts:
           // liveSvg.querySelectorAll('rect, circle, ellipse, line, polyline, polygon').forEach((el) => { pathLengthMm += el.getTotalLength() * scale; });
           const box = liveSvg.getBBox();
           bbox = { w: Number((box.width * scale).toFixed(1)), h: Number((box.height * scale).toFixed(1)) };
         } catch {
           throw new Error('PARSE_FAILED');
         } finally {
           host.remove();
         }
         return {
           mode: 'laser',
           bbox,
           pathLengthMm: Number(pathLengthMm.toFixed(0)),
           unitWarning,
           fileSizeMB: Number((fileSizeBytes / 1024 / 1024).toFixed(2)),
         };
       };
       ```
       **Decision for D-16:** Include implicit shapes (`rect, circle, ellipse, line, polyline, polygon`) in 03-01 — Open Question 2 in RESEARCH.md recommends it; cost is one line and prevents wrong-zero readings on shape-only designs. Uncomment the line above.

    5. Create `src/utilities/quote/formatErrors.js` — pure mapper from error objects to UI-SPEC strings:
       ```js
       export const formatError = (err) => {
         switch (err.code) {
           case 'WRONG_FORMAT_3D':
             return `That looks like a ${err.ext} file. The 3D tab takes STL or OBJ — try one of those.`;
           case 'WRONG_FORMAT_LASER':
             return `That looks like a ${err.ext} file. The laser tab takes SVG — try one of those.`;
           case 'TOO_LARGE':
             return `This file is ${err.sizeMB}MB — we cap uploads at 25MB so the page doesn't lock up. Try compressing or decimating the mesh.`;
           case 'MALFORMED_STL':
             return "We couldn't read this STL — it might be corrupted or empty. Try re-exporting it from your CAD tool.";
           case 'MALFORMED_OBJ':
             return "We couldn't read this OBJ — it might be corrupted or missing geometry. Try re-exporting it from your CAD tool.";
           case 'MALFORMED_SVG':
             return "We couldn't read this SVG — check that it's a valid file with at least one path.";
           default:
             return 'Something went sideways reading this file. Try a different one, or contact us and we\'ll take a look.';
         }
       };
       ```
  </action>
  <verify>
    <automated><![CDATA[
      # Unit tests covering parser contracts. Tests live alongside the source — Jest discovers them automatically (no config change).
      pnpm test -- --watchAll=false src/utilities/quote
    ]]></automated>
  </verify>
  <done>
    - `pnpm-lock.yaml` includes `three@^0.184.0`; package.json `dependencies.three` present
    - `grep -q "from 'three/examples/jsm/loaders/STLLoader.js'" src/utilities/quote/parseStl.js && echo OK` returns `OK` (subpath import only — Anti-pattern: `import * as THREE` would be a fail)
    - `grep -E "import \* as THREE" src/utilities/quote/parseStl.js src/utilities/quote/parseObj.js` returns nothing
    - Unit-cube test passes: write `src/utilities/quote/parseStl.test.js` that synthesizes a 12-triangle binary STL of a 10×10×10 mm cube (header + count=12 + 12×50 bytes of triangle records) and asserts `parseStl(buf, buf.byteLength).volumeCm3` is between 0.99 and 1.01
    - Reversed-winding test passes: same cube with all triangle vertex order reversed → `volumeCm3` still > 0
    - Malformed test passes: `parseStl(new ArrayBuffer(8), 8)` throws `PARSE_FAILED`
    - SVG test passes: a synthesized `<svg viewBox="0 0 100 50" width="100mm" height="50mm"><path d="M0 0 L100 0"/></svg>` returns `bbox.w === 100`, `bbox.h === 0`, `pathLengthMm === 100`, `unitWarning === false`
    - SVG NOT_SVG test passes: `parseSvg('<not-svg>', 13)` throws `NOT_SVG`
    - SVG unit-warning test passes: a synthesized SVG with no `width` and no `viewBox` returns `unitWarning === true`
    - `formatError({ code: 'TOO_LARGE', sizeMB: '30.0' })` includes the literal substring `30.0MB`
  </done>
</task>

<task type="auto">
  <name>Task 2: Build /quote page, tabs, dropzone, geometry summary; wire route + sitemap + nav + service-page CTAs</name>
  <files>src/pages/Quote.jsx, src/components/quote/QuoteTabs.jsx, src/components/quote/FileDropzone.jsx, src/components/quote/GeometrySummary.jsx, src/App.js, src/components/shared/AppHeader.jsx, src/pages/Projects.jsx, src/pages/ProjectSingle.jsx, scripts/generate-sitemap.cjs</files>
  <read_first>
    - src/App.js (existing lazy-load + Routes block — insert next to existing lazies)
    - src/components/shared/AppHeader.jsx (NAV_ITEMS lines 15-26 — append entry; navLinkClasses already supplies active border via `match: '/quote'`)
    - src/pages/Projects.jsx (lines 40-51 — CTA insertion point between TrustCopyBlock and MaterialsSection)
    - src/pages/ProjectSingle.jsx (lines 51-63 — same CTA insertion point in the detail composition)
    - src/components/contact/ContactForm.jsx (lines 11-52 — pre-fill cascade idiom to mirror verbatim per D-05)
    - src/components/services/FAQ.jsx (panel rhythm `bg-secondary-section-dark p-6 rounded-xl` — analog for GeometrySummary container)
    - src/components/services/MaterialsSection.jsx (empty-state shape with `/contact` link — analog for fallback states)
    - src/components/reusable/FormInput.jsx (input + label rhythm — anchor for dropzone label styling)
    - src/components/shared/SEOHead.jsx (props contract for SEOHead)
    - scripts/generate-sitemap.cjs (line 17 STATIC_ROUTES — append '/quote')
    - .planning/phases/03-auto-pricing-quote-tool/03-UI-SPEC.md ("Tab UI", "File upload dropzone", "Geometry readout panel", "Submit affordance + states Plan 03-01 placeholder", "Top-level CTA placement", "Copywriting Contract" exact strings)
    - .planning/phases/03-auto-pricing-quote-tool/03-RESEARCH.md (Pattern 3 WAI-ARIA tabs lines 352-389; Pattern 4 native dropzone lines 396-461; Pitfall 6 dragover preventDefault; Pitfall 7 bundle measurement)
    - .planning/phases/03-auto-pricing-quote-tool/03-PATTERNS.md (lines 43-91 Quote.jsx page shell; lines 139-194 QuoteTabs; lines 198-285 FileDropzone; lines 287-322 GeometrySummary; lines 437-460 AppHeader nav; lines 488-505 service-page CTA)
    - public/index.html (NO change required by this plan; do not touch — Plan 03-03 modifies it)
  </read_first>
  <action>

    **Step A — Quote.jsx page shell (`src/pages/Quote.jsx`).** Mirror `src/pages/Contact.jsx` lines 1-46 shape: SEOHead + framer-motion wrapper + composition.
    ```jsx
    import { motion } from 'framer-motion';
    import SEOHead from '../components/shared/SEOHead';
    import QuoteTabs from '../components/quote/QuoteTabs';

    const Quote = () => (
      <>
        <SEOHead
          title="Get a Quote"
          description="Upload your STL, OBJ, or SVG and get an instant ballpark estimate. We give you a quick range — final price comes after we look at your file."
          ogUrl="https://shapesmith.studio/quote"
          ogImage={{ url: '/og-default.png', altText: 'Shapesmith Studio quote tool' }}
        />
        <motion.div
          initial={{ opacity: 0 }}
          animate={{ opacity: 1 }}
          transition={{ ease: 'easeInOut', duration: 0.5, delay: 0.1 }}
          className="container mx-auto"
        >
          <section className="py-12 sm:py-24">
            <div className="text-center max-w-2xl mx-auto px-4 mb-12">
              <h1 className="font-display font-black text-3xl sm:text-4xl text-primary-dark dark:text-primary-light mb-6">
                Get an instant ballpark.
              </h1>
              <p className="text-base text-ternary-dark dark:text-ternary-light">
                We give you a quick range so you can decide if it's worth a real quote. Final price comes after we look at your file.
              </p>
            </div>
            <QuoteTabs />
          </section>
        </motion.div>
      </>
    );

    export default Quote;
    ```

    **Step B — QuoteTabs.jsx (`src/components/quote/QuoteTabs.jsx`).** WAI-ARIA tabs, mirror ContactForm pre-fill cascade per D-05. Each panel renders a `FileDropzone` (config from tab) and a `GeometrySummary` (rendered after `geometry !== null`). Uses dynamic `import('../../utilities/quote/parseStl')` etc. **inside the file handler** — keeps Three.js out of the synchronous render path of the chunk so the parser code is fetched only after the visitor drops a file.
    ```jsx
    import { useEffect, useState, useCallback, lazy, Suspense } from 'react';
    import { AnimatePresence, motion } from 'framer-motion';
    import { SERVICES } from '../../data/services';
    import FileDropzone from './FileDropzone';
    import GeometrySummary from './GeometrySummary';
    import { formatError } from '../../utilities/quote/formatErrors';

    const TAB_CONFIG = {
      print: {
        label: '3D Printing',
        accepted: ['stl', 'obj'],
        wrongFormatCode: 'WRONG_FORMAT_3D',
        malformedCode: (ext) => ext === 'stl' ? 'MALFORMED_STL' : 'MALFORMED_OBJ',
      },
      laser: {
        label: 'Laser Cutting',
        accepted: ['svg'],
        wrongFormatCode: 'WRONG_FORMAT_LASER',
        malformedCode: () => 'MALFORMED_SVG',
      },
    };
    const MAX_BYTES = 25 * 1024 * 1024; // D-12 — 25MB cap

    const QuoteTabs = () => {
      const [activeKey, setActiveKey] = useState('print'); // UI-SPEC default
      const [filesByTab, setFilesByTab] = useState({ print: null, laser: null });
      const [geometryByTab, setGeometryByTab] = useState({ print: null, laser: null });
      const [errorByTab, setErrorByTab] = useState({ print: '', laser: '' });
      const [busyByTab, setBusyByTab] = useState({ print: false, laser: false });

      // Pre-fill cascade — mirrors ContactForm.jsx D-24 verbatim per D-05.
      useEffect(() => {
        const queryService = new URLSearchParams(window.location.search).get('service');
        if (queryService) {
          const m = SERVICES.find((s) => s.key === queryService || s.urlSegment === queryService);
          if (m) { setActiveKey(m.key); return; }
        }
        const referrer = (typeof document !== 'undefined' && document.referrer) || '';
        const rule = SERVICES.find((s) => referrer.includes(`/${s.urlSegment}`));
        if (rule) setActiveKey(rule.key);
      }, []);

      const onFile = useCallback(async (tabKey, file) => {
        const cfg = TAB_CONFIG[tabKey];
        const ext = file.name.split('.').pop().toLowerCase();
        setErrorByTab((prev) => ({ ...prev, [tabKey]: '' }));
        setBusyByTab((prev) => ({ ...prev, [tabKey]: true }));
        setFilesByTab((prev) => ({ ...prev, [tabKey]: { name: file.name, sizeMB: (file.size / 1024 / 1024).toFixed(1) } }));
        setGeometryByTab((prev) => ({ ...prev, [tabKey]: null }));
        try {
          let result;
          if (ext === 'stl') {
            const buf = await file.arrayBuffer();
            const { parseStl } = await import('../../utilities/quote/parseStl');
            result = parseStl(buf, file.size);
          } else if (ext === 'obj') {
            const text = await file.text();
            const { parseObj } = await import('../../utilities/quote/parseObj');
            result = parseObj(text, file.size);
          } else if (ext === 'svg') {
            const text = await file.text();
            const { parseSvg } = await import('../../utilities/quote/parseSvg');
            result = parseSvg(text, file.size);
          }
          setGeometryByTab((prev) => ({ ...prev, [tabKey]: result }));
        } catch (err) {
          const code = err.message === 'PARSE_FAILED' || err.message === 'NOT_SVG'
            ? cfg.malformedCode(ext)
            : 'GENERIC';
          setErrorByTab((prev) => ({ ...prev, [tabKey]: formatError({ code }) }));
          setFilesByTab((prev) => ({ ...prev, [tabKey]: null }));
        } finally {
          setBusyByTab((prev) => ({ ...prev, [tabKey]: false }));
        }
      }, []);

      const onError = useCallback((tabKey, err) => {
        const cfg = TAB_CONFIG[tabKey];
        const code = err.code === 'WRONG_FORMAT' ? cfg.wrongFormatCode : err.code;
        setErrorByTab((prev) => ({ ...prev, [tabKey]: formatError({ ...err, code }) }));
      }, []);

      const handleArrowNav = (e) => {
        const order = ['print', 'laser'];
        const i = order.indexOf(activeKey);
        if (e.key === 'ArrowRight') setActiveKey(order[(i + 1) % order.length]);
        else if (e.key === 'ArrowLeft') setActiveKey(order[(i - 1 + order.length) % order.length]);
        else if (e.key === 'Home') setActiveKey(order[0]);
        else if (e.key === 'End') setActiveKey(order[order.length - 1]);
      };

      return (
        <div className="max-w-3xl mx-auto px-4">
          <p className="text-sm text-ternary-section-dark mb-2">Pick what you're making — 3D printed or laser cut.</p>
          <div role="tablist" aria-label="Quote tool service" className="flex gap-8 mb-8 border-b border-secondary-section-dark">
            {Object.entries(TAB_CONFIG).map(([key, cfg]) => (
              <button
                key={key}
                role="tab"
                id={`tab-${key}`}
                aria-selected={activeKey === key}
                aria-controls={`panel-${key}`}
                tabIndex={activeKey === key ? 0 : -1}
                onClick={() => setActiveKey(key)}
                onKeyDown={handleArrowNav}
                type="button"
                className={
                  activeKey === key
                    ? 'text-primary-light font-general-medium border-b-2 border-accent pb-2 -mb-px'
                    : 'text-ternary-section-dark hover:text-primary-light pb-2 -mb-px'
                }
              >
                {cfg.label}
              </button>
            ))}
          </div>
          <AnimatePresence mode="wait">
            <motion.div
              key={activeKey}
              role="tabpanel"
              id={`panel-${activeKey}`}
              aria-labelledby={`tab-${activeKey}`}
              initial={{ opacity: 0, y: 8 }}
              animate={{ opacity: 1, y: 0 }}
              exit={{ opacity: 0, y: -8 }}
              transition={{ duration: 0.2 }}
            >
              <p className="text-sm text-ternary-section-dark mb-2">
                <strong className="font-general-medium text-ternary-light">Max 25MB.</strong> Files are read in your browser — they're not uploaded anywhere unless you submit.
              </p>
              <div className="flex gap-2 mb-3" aria-label="Accepted formats">
                {TAB_CONFIG[activeKey].accepted.map((ext) => (
                  <span key={ext} className="inline-block px-2 py-1 rounded bg-secondary-section-dark text-primary-light text-xs font-general-medium uppercase tracking-wide">
                    {ext}
                  </span>
                ))}
              </div>
              <FileDropzone
                acceptedExtensions={TAB_CONFIG[activeKey].accepted}
                maxBytes={MAX_BYTES}
                file={filesByTab[activeKey]}
                error={errorByTab[activeKey]}
                onFile={(f) => onFile(activeKey, f)}
                onError={(err) => onError(activeKey, err)}
              />
              {(busyByTab[activeKey] || geometryByTab[activeKey]) && (
                <GeometrySummary geometry={geometryByTab[activeKey]} loading={busyByTab[activeKey]} />
              )}
              {/* Plan 03-01 placeholder submit affordance per D-03 — disabled CTA, kept honest. Real submission lands in Plan 03-03. */}
              {geometryByTab[activeKey] && (
                <div className="mt-8">
                  <button
                    type="button"
                    disabled
                    className="font-general-medium px-5 py-2.5 text-white bg-accent/40 cursor-not-allowed rounded-md"
                  >
                    Submit for confirmation — coming soon
                  </button>
                  <p className="mt-3 text-sm text-ternary-section-dark">
                    We're still building the submission flow. In the meantime, you can <a href="/contact" className="hover:text-accent">contact us</a> directly.
                  </p>
                </div>
              )}
            </motion.div>
          </AnimatePresence>
        </div>
      );
    };

    export default QuoteTabs;
    ```

    **Step C — FileDropzone.jsx (`src/components/quote/FileDropzone.jsx`).** Native HTML5 dropzone per RESEARCH.md Pattern 4. **CRITICAL — `e.preventDefault()` on BOTH `onDragOver` AND `onDrop` per Pitfall 6 (without this, the browser opens the dropped file as a new tab).**
    ```jsx
    import { useRef, useState } from 'react';
    import { FiUploadCloud, FiFile } from 'react-icons/fi';

    const FileDropzone = ({ acceptedExtensions, maxBytes, file, error, onFile, onError }) => {
      const inputRef = useRef(null);
      const [dragActive, setDragActive] = useState(false);

      const validate = (f) => {
        const ext = f.name.split('.').pop().toLowerCase();
        if (!acceptedExtensions.includes(ext)) return { code: 'WRONG_FORMAT', ext };
        if (f.size > maxBytes) return { code: 'TOO_LARGE', sizeMB: (f.size / 1024 / 1024).toFixed(1) };
        return null;
      };

      const handleFile = (f) => {
        const err = validate(f);
        if (err) { onError(err); return; }
        onFile(f);
      };

      // Compact summary card after successful drop (UI-SPEC §"After successful drop")
      if (file) {
        return (
          <div className="border border-secondary-section-dark bg-ternary-dark rounded-xl p-4 flex items-center gap-3">
            <FiFile className="text-2xl text-ternary-section-dark" />
            <span className="text-base text-primary-light font-general-medium">{file.name}</span>
            <span className="text-sm text-ternary-section-dark">· {file.sizeMB} MB</span>
            <button
              type="button"
              onClick={() => inputRef.current?.click()}
              className="ml-auto text-sm text-ternary-light hover:text-primary-light underline-offset-4 hover:underline"
            >
              Replace file
            </button>
            <input
              ref={inputRef}
              type="file"
              accept={acceptedExtensions.map((e) => `.${e}`).join(',')}
              onChange={(e) => e.target.files?.[0] && handleFile(e.target.files[0])}
              className="sr-only"
            />
            {error && <p role="alert" className="mt-2 text-sm text-red-400 w-full">{error}</p>}
          </div>
        );
      }

      return (
        <>
          <div
            role="region"
            aria-label="File upload"
            onDragOver={(e) => { e.preventDefault(); setDragActive(true); }}
            onDragLeave={() => setDragActive(false)}
            onDrop={(e) => {
              e.preventDefault();
              setDragActive(false);
              const f = e.dataTransfer.files?.[0];
              if (f) handleFile(f);
            }}
            className={dragActive
              ? 'border-2 border-solid border-accent bg-ternary-dark rounded-xl p-12 text-center min-h-[12rem] sm:min-h-[14rem] flex items-center justify-center'
              : 'border-2 border-dashed border-secondary-section-dark bg-ternary-dark/40 rounded-xl p-12 text-center cursor-pointer hover:border-ternary-light min-h-[12rem] sm:min-h-[14rem] flex items-center justify-center'}
          >
            <label htmlFor="quote-file" className="cursor-pointer block">
              <FiUploadCloud className="mx-auto text-5xl text-ternary-section-dark mb-3" />
              {dragActive ? (
                <p className="text-base font-general-medium text-primary-light">Drop the file to upload.</p>
              ) : (
                <p className="text-base text-primary-light">
                  <strong className="font-general-medium">Drop your {acceptedExtensions.map((e) => e.toUpperCase()).join(' or ')} here.</strong>
                  {' '}Or click to choose a file. (max {maxBytes / 1024 / 1024}MB)
                </p>
              )}
            </label>
            <input
              ref={inputRef}
              id="quote-file"
              type="file"
              accept={acceptedExtensions.map((e) => `.${e}`).join(',')}
              onChange={(e) => e.target.files?.[0] && handleFile(e.target.files[0])}
              className="sr-only"
            />
          </div>
          {error && <p role="alert" className="mt-2 text-sm text-red-400">{error}</p>}
        </>
      );
    };

    export default FileDropzone;
    ```

    **Step D — GeometrySummary.jsx (`src/components/quote/GeometrySummary.jsx`).** Definition-list panel rendering 3D OR laser readouts based on `geometry.mode`. Loading state replaces heading with "Reading your file…" and shows shimmer placeholders. Surfaces SVG `unitWarning` as a footnote inside the panel under bbox row.
    ```jsx
    const Shimmer = () => (
      <span className="inline-block w-16 h-4 bg-secondary-section-dark/50 rounded animate-pulse" />
    );

    const GeometrySummary = ({ geometry, loading }) => {
      const heading = loading || !geometry ? 'Reading your file…' : 'Geometry summary';
      return (
        <div className="bg-ternary-dark rounded-xl p-6 mt-6">
          <h3 className="font-display font-bold text-2xl sm:text-3xl text-ternary-dark dark:text-ternary-light mb-6">
            {heading}
          </h3>
          {(!geometry || loading) ? (
            <dl className="grid grid-cols-[max-content_1fr] gap-x-6 gap-y-3">
              {[0, 1, 2, 3].map((i) => (
                <span key={i} className="contents">
                  <dt className="text-sm font-general-regular text-ternary-section-dark"><Shimmer /></dt>
                  <dd className="text-base font-general-medium text-primary-light"><Shimmer /></dd>
                </span>
              ))}
            </dl>
          ) : geometry.mode === '3d' ? (
            <dl className="grid grid-cols-[max-content_1fr] gap-x-6 gap-y-3">
              <dt className="text-sm font-general-regular text-ternary-section-dark">Volume</dt>
              <dd className="text-base font-general-medium text-primary-light">
                {geometry.volumeCm3.toFixed(1)} cm³
                <div className="text-sm text-ternary-section-dark">{geometry.volumeIn3.toFixed(2)} in³</div>
              </dd>
              <dt className="text-sm font-general-regular text-ternary-section-dark">Bounding box</dt>
              <dd className="text-base font-general-medium text-primary-light">
                {geometry.bbox.w} × {geometry.bbox.d} × {geometry.bbox.h} mm
              </dd>
              <dt className="text-sm font-general-regular text-ternary-section-dark">Triangles</dt>
              <dd className="text-base font-general-medium text-primary-light">{geometry.triangleCount.toLocaleString()}</dd>
              <dt className="text-sm font-general-regular text-ternary-section-dark">File size</dt>
              <dd className="text-base font-general-medium text-primary-light">{geometry.fileSizeMB} MB</dd>
            </dl>
          ) : (
            <dl className="grid grid-cols-[max-content_1fr] gap-x-6 gap-y-3">
              <dt className="text-sm font-general-regular text-ternary-section-dark">Cut area</dt>
              <dd className="text-base font-general-medium text-primary-light">
                {geometry.bbox.w} × {geometry.bbox.h} mm
                {geometry.unitWarning && (
                  <div className="text-sm text-ternary-section-dark mt-2 border-l-2 border-ternary-section-dark pl-3">
                    We're reading this file as <strong className="font-general-medium">millimeters</strong>. If your design is in inches or pixels, the numbers above won't be accurate.
                  </div>
                )}
              </dd>
              <dt className="text-sm font-general-regular text-ternary-section-dark">Total cut length</dt>
              <dd className="text-base font-general-medium text-primary-light">
                {geometry.pathLengthMm > 100 ? Math.round(geometry.pathLengthMm) : geometry.pathLengthMm.toFixed(1)} mm
              </dd>
              <dt className="text-sm font-general-regular text-ternary-section-dark">File size</dt>
              <dd className="text-base font-general-medium text-primary-light">{geometry.fileSizeMB} MB</dd>
            </dl>
          )}
        </div>
      );
    };

    export default GeometrySummary;
    ```

    **Step E — App.js route registration.** Insert lazy import alphabetically between `Projects`/`ProjectSingle` and `Shop`, and a `<Route path="/quote" element={<Quote />} />` BEFORE the catch-all `<Route path="*">` (line 64 in current App.js):
    ```jsx
    const Quote = lazy(() => import('./pages/Quote'));
    // ...
    <Route path="/quote" element={<Quote />} />
    ```

    **Step F — AppHeader.jsx nav entry.** Append `{ to: '/quote', label: 'Get a Quote', match: '/quote' }` to `NAV_ITEMS` (after `/shop`, before the existing closing `]`). The active-state border-b-2 border-accent automatically applies via the existing `navLinkClasses` helper.

    **Step G — Service-page hero CTAs.** In **both** `src/pages/Projects.jsx` and `src/pages/ProjectSingle.jsx`, insert this block between `<TrustCopyBlock ... />` and `<MaterialsSection ... />`:
    ```jsx
    import { Link } from 'react-router-dom';
    // ...
    <div className="container mx-auto py-6 sm:py-8 text-center">
      <Link
        to={`/quote?service=${service.urlSegment}`}
        className="inline-block bg-accent hover:bg-accent-highlight text-white font-general-medium px-5 py-2.5 rounded-md duration-300"
      >
        Estimate this in our quote tool
      </Link>
    </div>
    ```
    `Projects.jsx` already calls `useServices()` (line 31) so `service.urlSegment` is in scope. `ProjectSingle.jsx` `ServiceDetailComposition` already destructures `service` (line 20) — same in scope. Add the `Link` import where missing.

    **Step H — `scripts/generate-sitemap.cjs`.** Modify line 17 to append `'/quote'`:
    ```cjs
    const STATIC_ROUTES = ['/', '/styles', '/3d-printing', '/about', '/contact', '/shop', '/quote'];
    ```

  </action>
  <verify>
    <automated><![CDATA[
      # Build first so the lazy chunk is generated and we can measure its size.
      pnpm build 2>&1 | tail -25
      # 1. /quote route registered
      grep -q "lazy(() => import('./pages/Quote'))" src/App.js && \
      grep -q '<Route path="/quote"' src/App.js && \
      # 2. AppHeader nav entry (anywhere on the line — accent active state is automatic)
      grep -q "to: '/quote'" src/components/shared/AppHeader.jsx && \
      grep -q "Get a Quote" src/components/shared/AppHeader.jsx && \
      # 3. Service-page CTAs (Projects + ProjectSingle)
      grep -q "/quote?service=" src/pages/Projects.jsx && \
      grep -q "/quote?service=" src/pages/ProjectSingle.jsx && \
      grep -q "Estimate this in our quote tool" src/pages/Projects.jsx && \
      grep -q "Estimate this in our quote tool" src/pages/ProjectSingle.jsx && \
      # 4. Sitemap STATIC_ROUTES updated
      grep -q "'/quote'" scripts/generate-sitemap.cjs && \
      # 5. /quote present in built sitemap
      grep -q "<loc>https://shapesmith.studio/quote</loc>" build/sitemap.xml && \
      # 6. Lazy chunk exists (Quote-* chunk file present in build/static/js)
      ls build/static/js/Quote-*.chunk.js >/dev/null 2>&1 && \
      # 7. No `import * as THREE` anywhere (subpath-only contract per RESEARCH Anti-Pattern + Pitfall 7)
      ! grep -rE "import \* as THREE" src/ && \
      # 8. e.preventDefault on BOTH dragover AND drop in FileDropzone (Pitfall 6)
      grep -E "onDragOver=\{[^}]*e\.preventDefault" src/components/quote/FileDropzone.jsx && \
      grep -E "onDrop=\{[^}]*e\.preventDefault|preventDefault\(\);[[:space:]]*setDragActive\(false\)" src/components/quote/FileDropzone.jsx && \
      # 9. Smoke test still passes (App.test.js)
      pnpm test -- --watchAll=false src/App.test.js && \
      # 10. Bundle size measurement — record /quote chunk size; main chunk should NOT have grown materially
      echo "--- /quote chunk size (record in SUMMARY) ---" && \
      du -h build/static/js/Quote-*.chunk.js && \
      echo "--- main chunk size (should remain ~293KB raw — no Three.js inflation) ---" && \
      du -h build/static/js/main.*.js
    ]]></automated>
  </verify>
  <done>
    - `pnpm build` succeeds with no warnings about Three.js root imports
    - `/quote` is reachable in the built app; visiting renders the H1 "Get an instant ballpark." with two tabs and the dropzone for the active tab
    - `/quote?service=laser` opens the Laser tab on first paint; `/quote?service=print` opens the 3D tab; `/quote` (no query, no referrer) opens the 3D tab (default per UI-SPEC)
    - The `Quote-*.chunk.js` file exists separately from `main-*.js` (lazy split confirmed)
    - The recorded `/quote` chunk gzip size is documented in 03-01-SUMMARY.md (target ≤120 KB gzip per RESEARCH §Bundle math; if over, flag a follow-up to revisit standalone parsers)
    - Dropping a 3D-format file on the Laser tab produces the friendly cross-format error message; dropping a >25MB file produces the friendly size-cap message
    - `/quote` appears in `build/sitemap.xml`
    - `pnpm test` (smoke test) still passes
    - All 6 UI-SPEC accent reservations honored in this plan (CTA buttons, active tab `border-b-2 border-accent`, drag-over `border-accent`, focus-ring on inputs, hover link `hover:text-accent`, dropzone-summary "Replace file" stays muted not accent)
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Visitor browser → SPA bundle | The visitor uploads a file from their local disk via the File API; the file stays in the browser, never crosses a network boundary in this plan |
| User-uploaded SVG → DOMParser → offscreen DOM host | The parsed SVG cloneNode is appended to an offscreen `<div>` to evaluate `getTotalLength`/`getBBox`; this is the only point where untrusted SVG markup touches the live document |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01-01 | Denial of Service | `FileDropzone.jsx` + parsers | mitigate | Hard 25MB cap (D-12) enforced in `validate()` BEFORE any `arrayBuffer()`/`text()` read or parser call. Wrong-size files are rejected with a friendly message in `O(1)` time. (QTE-09 partial — memory-bounded parsing.) |
| T-03-01-02 | Denial of Service | `parseSvg.js` getTotalLength loop | mitigate | The 25MB upstream cap bounds total path data. The `<path>` querySelector + getTotalLength loop is `O(paths)`; even pathological SVGs with thousands of paths complete in ms. If a future profiling run shows freezes, Web Worker becomes a follow-up (Open Question 3). |
| T-03-01-03 | Tampering / XSS | User-uploaded SVG → offscreen host | mitigate | The parsed SVG is appended via `cloneNode(true)` to an `position:absolute;left:-99999px` offscreen `<div>`, NEVER to a visible container. The host is removed in `finally{}` so an exception during parse cannot leak the host. DOMPurify is NOT yet in this plan (D-22) because no visible SVG render exists; if a future plan adds visible SVG preview, it MUST land DOMPurify first per RESEARCH §Anti-Patterns. The offscreen-host pattern is documented as an explicit precondition for SVG preview in Plan 03-03 / future plans. |
| T-03-01-04 | Information Disclosure | Drag-and-drop browser default | mitigate | `e.preventDefault()` called in BOTH `onDragOver` AND `onDrop` per RESEARCH Pitfall 6 — without both, the browser's default drop opens the file as a tab, which would not leak data outside the visitor's machine but would trash the UX and trick visitors into thinking they uploaded the file when they did not. |
| T-03-01-05 | Spoofing | Wrong-extension files | accept | The 25MB-cap + format whitelist via file extension is a UX guard, not a security boundary. A visitor renaming `evil.svg` to `evil.stl` and dropping it on the 3D tab will cause the STL parser to throw `PARSE_FAILED` (binary STL header check fails); the resulting error is the "Malformed STL" friendly message. No code execution path. |
| T-03-01-06 | Repudiation | n/a | accept | This plan does not submit anything; no repudiation surface. (Addressed in Plan 03-03 when submission lands.) |
| T-03-01-07 | Elevation of Privilege | Three.js loader execution | accept | `STLLoader.parse(arrayBuffer)` and `OBJLoader.parse(text)` from `three/examples/jsm` are well-vetted (Three.js team-maintained). Three.js's known historical CVEs are in renderer/scene code, not parsers; subpath imports keep the renderer/scene code out of the bundle. No native module loading, no eval. |
</threat_model>

<verification>
- All 13 must-have truths observable
- All 7 artifacts (page, tabs, dropzone, summary, parseStl/Obj/Svg) exist in the listed files with at least the documented min_lines
- Key links verified by grep patterns in Task 2 acceptance
- Unit-cube test (1cm³) verifies signed-tetrahedra math
- Bundle measurement recorded in SUMMARY (RESEARCH §Bundle math soft target ≤120 KB gzip /quote chunk)
- Smoke test (`App.test.js`) still passes — no regressions to existing routes
</verification>

<success_criteria>
- A visitor on production can navigate from `/styles`, `/3d-printing`, or the AppHeader to `/quote`, drop or pick an STL/OBJ on the 3D tab or an SVG on the laser tab, see geometry readouts (volume + bbox + triangles + size for 3D; bbox + path length + size for laser), and see a clearly-labeled "coming soon" submit affordance — without the page locking up or breaking on any tested input
- Plan 03-01 leaves the site deployable per D-02; the placeholder submit affordance is honest per D-03
- All 4 of QTE-01, QTE-02, QTE-03, and the memory-bounded parsing aspect of QTE-09 are met by this plan as defined in REQUIREMENTS.md
</success_criteria>

<output>
After completion, create `.planning/phases/03-auto-pricing-quote-tool/03-01-SUMMARY.md` recording:
- The measured `/quote` chunk size (gzip + raw) from `build/static/js/Quote-*.chunk.js` and the main chunk size for comparison
- Whether the chunk-size soft target (≤120 KB gzip) was met; if not, document for the follow-up to evaluate standalone parsers
- The implicit-shape D-16 decision (whether `<rect>`/`<circle>`/etc. were enabled in `parseSvg`)
- Any unit-cube parse-time observation (single-laptop ballpark for Open Question 3 calibration)
- All deviations from the plan with the §Deviations format established in Phase 2 SUMMARYs
</output>