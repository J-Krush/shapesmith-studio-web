---
phase: 260512-r23
plan: 01
subsystem: netlify-functions
tags: [resend, email, dns, spf, transactional]
requirements_completed:
  - R23-mail-subdomain-sender
dependency_graph:
  requires:
    - DNS records for mail.shapesmith.studio (Resend-verified, owner-prep)
  provides:
    - Transactional sender on dedicated subdomain (orders@, quotes@)
  affects:
    - netlify/functions/snipcart-order-webhook (order-completed emails)
    - netlify/functions/submit-quote (quote-submission emails)
tech_stack:
  added: []
  patterns:
    - Sender domain isolation from apex via transactional subdomain
key_files:
  created: []
  modified:
    - netlify/functions/snipcart-order-webhook/snipcart-order-webhook.js
    - netlify/functions/submit-quote/submit-quote.js
decisions:
  - Send transactional mail from mail.shapesmith.studio, keep inbound/reply_to on apex
metrics:
  duration_minutes: 4
  completed_date: 2026-05-12
  tasks_completed: 1
  files_changed: 2
---

# Phase 260512-r23 Plan 01: Switch Resend sender to mail.shapesmith.studio Summary

Switched the Resend `from:` address in both Netlify Functions (`snipcart-order-webhook` and `submit-quote`) from the Proton-managed apex `shapesmith.studio` to the dedicated transactional subdomain `mail.shapesmith.studio`, eliminating the SPF conflict with Proton's inbound configuration.

## What Was Done

- **`netlify/functions/snipcart-order-webhook/snipcart-order-webhook.js` (line 158):** Changed `from: 'Shapesmith Studio <orders@shapesmith.studio>'` → `from: 'Shapesmith Studio <orders@mail.shapesmith.studio>'`. Updated the inline comment block to record the SPF-conflict rationale.
- **`netlify/functions/submit-quote/submit-quote.js` (line 106):** Changed `from: 'Shapesmith Studio <quotes@shapesmith.studio>'` → `from: 'Shapesmith Studio <quotes@mail.shapesmith.studio>'`. Same comment-block update.
- **Untouched (intentional):** `to: ['jkrush@shapesmith.studio']` in both files; `reply_to: payload.content?.email ?? 'noreply@shapesmith.studio'` in the webhook; `reply_to: payload.email` in submit-quote. Inbound + reply addressing stays on the Proton-managed apex.

## Commits

- `6dd0767` — fix(05): use mail.shapesmith.studio transactional subdomain for Resend sender

## Verification

| Check | Result |
| --- | --- |
| `grep "orders@mail.shapesmith.studio" snipcart-order-webhook.js` | line 158 (match) |
| `grep "quotes@mail.shapesmith.studio" submit-quote.js` | line 106 (match) |
| `grep "from: 'Shapesmith Studio <orders@shapesmith.studio>'" netlify/` | no matches |
| `grep "from: 'Shapesmith Studio <quotes@shapesmith.studio>'" netlify/` | no matches |
| `to: ['jkrush@shapesmith.studio']` preserved | both files |
| `reply_to:` preserved on apex | both files |
| `pnpm build` | exit 0, sitemap.xml regenerated, no warnings |

## Deviations from Plan

None — plan executed exactly as written. Two `Edit` calls (one needed an indentation correction after a first-attempt mismatch on tab depth, no semantic change), build verified, one atomic commit.

## Out-of-Band Verification Required (Post-Deploy)

The plan's success criterion #5 is post-deploy and manual:
- Trigger a real Snipcart `order.completed` event in the deploy preview / production.
- Confirm: webhook returns 200, Resend dashboard logs "Delivered", Proton inbox `jkrush@shapesmith.studio` receives the formatted email.
- Trigger a quote submission via `/quote` to exercise `submit-quote`; confirm the same delivery chain.

## Self-Check: PASSED

- `netlify/functions/snipcart-order-webhook/snipcart-order-webhook.js` — FOUND (modified line 158 has `orders@mail.shapesmith.studio`)
- `netlify/functions/submit-quote/submit-quote.js` — FOUND (modified line 106 has `quotes@mail.shapesmith.studio`)
- Commit `6dd0767` — FOUND in `git log --oneline`
- `pnpm build` exit code — 0
- Negative greps — both clean (no apex sender strings remain under `netlify/`)
